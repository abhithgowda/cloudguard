# CloudGuard — Build Progress Log

> Running log of what's been done, decisions made, and lessons learned.
> Updated at the end of every STEP. Read this at the start of every new session.

---

## Current Status

- **Last completed STEP:** 21.5 — Finding deduplication via deterministic finding_id + upsert (idempotent writes). Triggered by user inspection of the 2026-05-31 daily report which showed 50 findings for ~14 unique real-world issues (every issue duplicated ~4x because the scanner runs every 6h and the original uuid finding_id made each scan write a new row). Refactored `shared.dynamo_client` to expose `compute_finding_id` (SHA-256 of category|resource_id|check_name, 32-char hex) + `upsert_finding` (Query-by-PK → UpdateItem on existing SK / PutItem on miss) + `batch_upsert_findings`. Same Security Hub-style FirstObservedAt / LastObservedAt pattern. Report now filters on `last_seen` and renders "Seen N times since YYYY-MM-DD" when occurrence_count > 1. Cost-data + remediation-log tables unchanged (already idempotent / event-history respectively).
- **Next up:** STEP 22 — Add CloudWatch Dashboard (Lambda invocations + errors + duration, SFN execution count/status, DynamoDB RCU/WCU, plus 3 alarms: Lambda error rate > 5%, Lambda duration > 80% of timeout, SFN execution failed). STEP 18.5 (IAM hardening) and STEP 22.5 (AWS Config) remain slotted ahead of STEP 23.
- **Last updated:** 2026-05-31
- **Environment focus:** `dev` (region: `ap-south-1`)
- **AWS account:** Personal (free tier, own card) — account `810278669055`, IAM user `abhithV1`
- **Test suite:** `pytest tests/ -v` → 121 passed, 0 failed, ~6.7 s (was 109; +12 from STEP 21.5: 4× compute_finding_id, 3× upsert_finding, 2× batch_upsert_findings, 2× html_builder occurrence_count rendering, 1× cost_scanner deterministic-id-across-runs). Mocks only (no moto, no AWS).
- **Live infrastructure:** 63 managed resources in remote state (was 57; +6 from STEP 21 OIDC: 1 OIDC provider + 2 IAM roles + 1 inline policy + 2 managed-policy attachments). `terraform plan` is `No changes`. State bucket: `cloudguard-tf-state-abhithcogni`. Total monthly cost estimate at idle: ~$1 (one CMK) — everything else free-tier.
- **First live data in DynamoDB (after STEP 20's successful re-run):** `cloudguard-dev-findings` = 13 rows (12 security + 1 cost — the cost row is a known false positive on microscopic S3 spend, see STEP 20 Bug #2); `cloudguard-dev-cost-data` = 290 rows (30d × ~20 services); `cloudguard-dev-remediation-log` = 0 rows (no zombies in personal account). Real security findings already include an open SG on port 8080 to 0.0.0.0/0 in the user's actual account — CloudGuard works.
- **Manual follow-ups (both completed 2026-05-28):**
  1. ✅ **SNS email subscription confirmed** — clicked the AWS confirmation link sent to `abhithgowdaa@gmail.com`. Verified via `aws sns get-subscription-attributes --subscription-arn <full-arn>` → `PendingConfirmation: "false"`. **Diagnostic gotcha discovered in-session:** the aggregate signals `get-topic-attributes` (`SubscriptionsConfirmed`) and `list-subscriptions-by-topic` (`SubscriptionArn` field) are both eventually-consistent and can lag by minutes-to-hours for email subscriptions — they reported `0 confirmed` and `SubscriptionArn = "Deleted"` *while the subscription was actually live*. The AWS Console correctly showed `✓ Confirmed`. **The authoritative source is `get-subscription-attributes` against the specific UUID ARN.** Trust that, not the aggregate counters, when diagnosing email-subscription state.
  2. ✅ **SES identity verified** — created `Email address` identity for `abhithgowdaa@gmail.com` in the SES console (ap-south-1), clicked the verification link, status now `VerificationStatus: Success`. One verification covers both sender and recipient because `local.ses_sender_email` falls back to `var.alert_email` when `ses_sender_email` is empty (the default). Sandbox SES + same-address-both-ends = single click suffices.
- **🔁 Execution order (2026-05-25 reorder, now executed):** 16 ✅ → 17 ✅ → 17.5 ✅ → 19 ✅ → 18 ✅ → **20 ✅** → **21 ⏭️** → 18.5 → 22 → 22.5 → 23 → 24. STEP 19 implementation differs slightly from the original 2026-05-25 plan: each Lambda's deploy artefact lives in `src/<lambda>/build/` (not `package/`), and the packaging script does NOT pip-install (every `requirements.txt` is `boto3`-only and the runtime already ships it). See STEP 19's "Key decisions" for the full reasoning.
- **Decision closed (2026-05-25):** Slack alerting OUT for v1 — user does not have Slack. Email-only via SNS subscription is the production path. `shared.notification.send_slack_webhook` stays in `src/shared/` (unit-tested, available for future) but is NOT wired into any Lambda or Terraform resource.
- **Decision closed (2026-05-27):** Auto-remediate STAYS config-gated for v1 (env var + event-input two-gate from STEP 12). The scheduled 6-hour EventBridge rule passes `auto_remediate=false` so every cron-driven run is dry-run. Actual remediation requires (a) flipping the Lambda env var via Terraform PR AND (b) a human manually starting Step Functions from the AWS Console with `auto_remediate=true`. **True async approval** (per-resource Approve/Reject email links via `.waitForTaskToken`) deferred to **STEP 25** (post-DoD stretch).
- **Decision closed (2026-05-27, STEP 17.5):** Report cadence is **daily (24h window) + weekly (168h window)** only — no per-scan reports. SFN scans run every 6 h and write findings silently to DynamoDB. Two emails per typical week (7 daily + 1 weekly = 8 total). The 6-hourly `GenerateReport` was producing content-identical output to the daily rule (both use a 24h findings window) — pure email noise. Removed.

---

## How to Resume in a New Session

1. Read this file top to bottom.
2. Read `PROJECT_BLUEPRINT.md` section for the next STEP.
3. Activate the venv: `cloudguard-env\Scripts\activate` (from repo root in PowerShell)
4. For any Terraform work: `cd terraform\environments\dev` — this is where `terraform plan/apply` runs.
5. The remote backend is already initialised — no need to re-run `setup_backend.sh`.
6. **Use PowerShell** for all file/directory operations (Bash resets cwd in this env).
7. Terraform IS in Claude Code's shell PATH (confirmed STEP 5) — `terraform plan` can be run directly without needing the user's terminal.

---

## STEP Completion Log

### ✅ STEP 1 — Set Up Development Environment
*Completed: 2026-05-14*

- **Tools installed (versions):**
  - Python: `3.13.7` — blueprint pins 3.12 (Lambda runtime). Risk noted: local/Lambda mismatch. Will catch in STEP 14 and STEP 19.
  - Terraform: `1.14.6` — installed via Cognizant software portal. At `C:\tools\terraform`, added to user PATH.
  - AWS CLI: `2.27.17`
  - Git: `2.51.0`
  - VS Code: Python, HashiCorp Terraform, GitLens, YAML (Red Hat) extensions installed.
- **Virtual environment:** `cloudguard-env/` at repo root. boto3 `1.43.7`, pytest `9.0.3` installed inside.
- **AWS account:** Personal free tier. IAM user `abhithV1` with admin access (dev only).
- **AWS CLI default region:** `ap-south-1`
- **Surprises:** Cognizant software portal used for Terraform + AWS CLI. Python 3.12 unavailable; proceeded with 3.13.7.

---

### ✅ STEP 2 — Set Up Repository Structure
*Completed: 2026-05-14*

- **Repo URL:** `https://github.com/abhithcogni/cloudguard.git`
- **Local path:** `C:\Users\2406667\Programming\AWS project\cloudguard`
- **Commit hash:** `a4953ed` (64 files, full folder tree)
- **Folder structure:** Full tree per `PROJECT_BLUEPRINT.md` section 5 — 18 directories, all module + source stubs.
- **`.gitignore` covers:** Python (`__pycache__`, venv, `.pyc`), Terraform (`.terraform/`, `*.tfstate`, `terraform.tfvars`), Lambda zips, IDE files, secrets.
- **Key decisions:**
  - Separate folders per env (`dev/`, `prod/`) over Terraform workspaces — safety: impossible to accidentally apply prod.
  - `terraform.tfvars.example` committed (safe template); `terraform.tfvars` gitignored (real values).
  - Real stub files (not `.gitkeep`) — VS Code shows the full project map immediately.
- **Surprises:** Bash shell in Claude Code resets cwd to git worktree — use PowerShell for all file ops.

---

### ✅ STEP 3 — Set Up Terraform Remote Backend
*Completed: 2026-05-14*

- **S3 bucket:** `cloudguard-tf-state-abhithcogni` (region: `ap-south-1`)
  - Versioning: ✅ Enabled
  - Encryption: ✅ SSE-S3 (AES256) — chose over SSE-KMS because free, zero config; KMS adds audit trail but costs ~$1/month per key, overkill for personal project state.
  - Public access block: ✅ All 4 settings = true
- **DynamoDB lock table:** NOT created. Terraform >= 1.10 supports native S3 locking via `use_lockfile = true`. User has 1.14.6 — DynamoDB is unnecessary.
- **Files written:**
  - `scripts/setup_backend.sh` — documents and automates the S3 bucket creation steps
  - `terraform/environments/dev/backend.tf` — S3 backend config with `use_lockfile = true`, `required_version >= 1.10`, AWS provider `~> 5.0`
  - `terraform/environments/dev/variables.tf` — 4 variables: `aws_region`, `environment`, `project`, `alert_email`
  - `terraform/environments/dev/terraform.tfvars.example` — safe template (no real values)
  - `terraform/environments/dev/main.tf` — AWS provider block with `default_tags`
- **`terraform init` result:** ✅ Success. AWS provider `v5.100.0` installed. Backend "s3" configured.
- **Lock file:** `terraform/environments/dev/.terraform.lock.hcl` — pins AWS provider at `v5.100.0`. **Committed** (Terraform recommends this; ensures identical provider versions on every `init`).
- **Bug fixed:** `.terraform.lock.hcl` was incorrectly added to `.gitignore` in STEP 2. Removed in this STEP.
- **GitHub Actions fix:** Both `ci.yml` and `deploy.yml` changed from `on: push` to `on: workflow_dispatch` — stops spurious emails on every commit. Will be properly built in STEP 20.
- **Surprises:**
  - Terraform not in Claude Code's shell PATH (only in user's PowerShell). Workaround: user runs `terraform` commands in their own terminal and pastes output.
  - `.terraform.lock.hcl` should be committed, not gitignored — corrected.

---

### ✅ STEP 4 — Build the IAM Terraform Module
*Completed: 2026-05-14*

- **Files written:**
  - `terraform/modules/iam/main.tf` — 4 roles, 4 inline policies, 4 managed-policy attachments. Uses data sources for account_id + region; locals for naming + assume-role policy + resource ARNs.
  - `terraform/modules/iam/variables.tf` — `environment`, `project`
  - `terraform/modules/iam/outputs.tf` — 4 role ARNs + 4 role names
  - `terraform/environments/dev/main.tf` — wired up the iam module
  - `terraform/environments/dev/outputs.tf` — surfaces `iam_role_arns` map
  - `terraform/environments/dev/terraform.tfvars` — created locally with real `alert_email` (gitignored — not committed)
- **Role naming convention:** `${project}-${environment}-${function}-role` (e.g. `cloudguard-dev-cost-scanner-role`)
- **Permissions design — key decisions:**
  - **Inline policies** (not customer-managed): each policy is unique to one role, inline makes ownership obvious and policy auto-deletes with role.
  - **`AWSLambdaBasicExecutionRole`** (AWS-managed) attached to every role for CloudWatch Logs perms — don't reinvent AWS-blessed patterns.
  - **Resource scoping**:
    - DynamoDB → scoped to specific table ARNs built from naming convention (tables don't exist yet, but ARNs are deterministic)
    - S3 reports bucket → scoped to `arn:aws:s3:::cloudguard-dev-reports/*`
    - SNS alerts topic → scoped to `arn:aws:sns:<region>:<account>:cloudguard-dev-alerts`
    - `ce:*`, `ec2:Describe*`, `rds:Describe*`, `iam:List*`, `ses:SendEmail` → `Resource = "*"` (AWS API limitation — these actions don't support resource-level perms)
    - **`ec2:DeleteVolume`, `ec2:ReleaseAddress`** → `Resource = "*"` with WARNING comment. In production, scope with Condition on tag `ec2:ResourceTag/AutoCleanup = true`. Hardening TODO.
- **`terraform plan` result:** ✅ `Plan: 12 to add, 0 to change, 0 to destroy.` — 4 roles + 4 inline policies + 4 managed-policy attachments. No apply yet (per STEP 4 blueprint).

---

### ✅ STEP 5 — Build the DynamoDB Terraform Module
*Completed: 2026-05-14*

- **Files written:**
  - `terraform/modules/dynamodb/main.tf` — 3 DynamoDB tables with all schema, GSIs, TTL, PITR, KMS
  - `terraform/modules/dynamodb/variables.tf` — `project`, `environment`
  - `terraform/modules/dynamodb/outputs.tf` — 6 outputs: name + ARN for each of the 3 tables
  - `terraform/environments/dev/main.tf` — wired up the dynamodb module
  - `terraform/environments/dev/outputs.tf` — added `dynamodb_table_names` and `dynamodb_table_arns` outputs
- **Table names (match IAM module ARNs exactly):**
  - `cloudguard-dev-findings` — PK `finding_id`, SK `timestamp`; GSI: `severity-index`, `category-index`; TTL: `expires_at`
  - `cloudguard-dev-cost-data` — PK `date`, SK `service_name`; no GSIs
  - `cloudguard-dev-remediation-log` — PK `remediation_id`, SK `timestamp`; GSI: `status-index`
- **Key decisions:**
  - **PAY_PER_REQUEST over provisioned:** Scanner runs every 6 hours — traffic is bursty, not steady. Provisioned capacity requires you to predict RCUs/WCUs and either over-provision (wastes money) or under-provision (throttles). PAY_PER_REQUEST scales automatically and costs nothing when idle — correct for an infrequent automated workload.
  - **KMS with AWS-managed key (`aws/dynamodb`):** Blueprint requires KMS encryption. Using the AWS-managed DynamoDB key (`enabled = true`, no custom key ARN) — it's free, zero config, and still encrypts at rest with KMS. Customer-managed key costs $1/month per key and adds management overhead; not justified for a personal dev environment.
  - **GSI sort key = `timestamp`:** All GSIs use `timestamp` as the sort key so queries return results in chronological order by default. The alternative (no sort key) would return results in arbitrary order and make "latest N critical findings" queries less efficient.
  - **TTL only on findings table:** Cost data and remediation logs are operational records worth keeping indefinitely (or until PITR recovery window). Findings are ephemeral — a 90-day auto-expiry matches the IAM key rotation policy window and keeps the table lean.
  - **PITR on all 3 tables:** Point-in-Time Recovery gives a 35-day rollback window for free on PAY_PER_REQUEST tables. Cheap insurance against accidental writes during testing.
- **`terraform plan` result:** ✅ `Plan: 15 to add, 0 to change, 0 to destroy.` — 12 IAM resources (STEP 4, not yet applied) + 3 DynamoDB tables. No apply yet (blueprint does not require apply until STEP 18).
- **Note (2026-05-15):** STEP 5 currently uses AWS-managed `aws/dynamodb` key. Per blueprint amendment 2026-05-15, a new STEP 6 (KMS CMK Module) has been inserted; the DynamoDB module will be retrofitted in that session to consume `module.kms.key_arn`.

### ✅ STEP 6 — Build the KMS CMK Module
*Completed: 2026-05-16 · Commit: `65381f0`*

- **Files written:**
  - `terraform/modules/kms/main.tf` — 1 `aws_kms_key`, 1 `aws_kms_alias`, 1 `data "aws_iam_policy_document" "kms_key_policy"` with 4 statements
  - `terraform/modules/kms/variables.tf` — `project`, `environment`, `lambda_role_arns` (list)
  - `terraform/modules/kms/outputs.tf` — `key_arn`, `key_id`, `alias_arn`, `alias_name`
- **Files modified (DynamoDB retrofit):**
  - `terraform/modules/dynamodb/variables.tf` — added required `kms_key_arn` input
  - `terraform/modules/dynamodb/main.tf` — replaced all 3 `server_side_encryption` blocks with `{ enabled = true, kms_key_arn = var.kms_key_arn }`; updated header comment
- **Files modified (dev wiring):**
  - `terraform/environments/dev/main.tf` — added `module "kms"` between iam and dynamodb; passed `module.kms.key_arn` into the dynamodb module
  - `terraform/environments/dev/outputs.tf` — added `kms_key_arn` and `kms_alias_name` outputs
- **Key properties on the CMK:**
  - `enable_key_rotation = true` — annual automatic rotation
  - `deletion_window_in_days = 30` — maximum window; if scheduled for deletion in error, there's a month to cancel
  - `key_usage = "ENCRYPT_DECRYPT"` — symmetric encryption (default)
  - `multi_region = false` — single-region key; multi-region is for cross-region DR scenarios
- **Alias:** `alias/cloudguard-dev`
- **Key policy — design decisions:**
  - **3 separate `Sid`s for the Lambda grants** (`AllowLambdasViaDynamoDB`, `AllowLambdasViaS3`, `AllowLambdasViaSNS`) instead of one combined statement with a list of `kms:ViaService` values. More verbose, but each statement reads as a single purpose — easier to audit and to revoke one service later without touching the others.
  - **Root-account admin statement** (`kms:*` for the root principal) is non-negotiable on every CMK. Without it, a misconfigured policy can permanently lock you out — Terraform can't fix a key it can't touch.
  - **`kms:ViaService` Conditions** on every Lambda grant — a compromised role can only call KMS *through* the service it's meant to use. A leaked cost-scanner credential can't call `kms:Decrypt` directly; it has to go via DynamoDB.
  - **`aws_iam_policy_document` data source** instead of inline JSON heredoc — type-checked, references variables cleanly, surfaces typos at plan time.
  - **`bypass_policy_lockout_safety_check = false`** (default kept) — AWS will refuse to create a key with a policy that locks out root, catching the most common foot-gun.
- **`terraform plan` result:** ✅ `Plan: 17 to add, 0 to change, 0 to destroy.` — 12 IAM (STEP 4) + 3 DynamoDB (STEP 5, now with CMK refs) + 2 KMS. No apply (blueprint defers apply to STEP 18).

---

### ✅ STEP 7 — Build the S3 Terraform Module
*Completed: 2026-05-16 · Commit: `79c9ff5`*

- **Files written:**
  - `terraform/modules/s3/main.tf` — 2 buckets (`reports`, `logs`) + versioning, public-access-block, SSE, lifecycle, logging, bucket-policy resources for each, plus 2 `aws_iam_policy_document` data sources
  - `terraform/modules/s3/variables.tf` — `project`, `environment`, `reports_bucket_name`, `kms_key_arn`, `lambda_role_arns`
  - `terraform/modules/s3/outputs.tf` — `reports_bucket_name`, `reports_bucket_arn`, `reports_bucket_domain_name`, `logs_bucket_name`, `logs_bucket_arn`
- **Files modified (IAM retrofit):**
  - `terraform/modules/iam/variables.tf` — added required `reports_bucket_arn` input
  - `terraform/modules/iam/main.tf` — removed the hardcoded `local.reports_bucket_arn`; `report_generator` `s3:PutObject` policy now references `var.reports_bucket_arn`
- **Files modified (dev wiring):**
  - `terraform/environments/dev/main.tf` — added `locals { reports_bucket_name, reports_bucket_arn }` as the single source of truth; passed `reports_bucket_arn` into the iam module and `reports_bucket_name` into the new s3 module
  - `terraform/environments/dev/variables.tf` — added `bucket_suffix` (validated against S3 naming rules; no default)
  - `terraform/environments/dev/terraform.tfvars.example` — example `bucket_suffix = "your-handle"`
  - `terraform/environments/dev/terraform.tfvars` — set `bucket_suffix = "abhithcogni"` (gitignored, matches state-bucket convention from STEP 3)
  - `terraform/environments/dev/outputs.tf` — added `s3_reports_bucket_name`, `s3_reports_bucket_arn`, `s3_logs_bucket_name`
- **Bucket names produced:**
  - Reports: `cloudguard-dev-reports-abhithcogni`
  - Logs:    `cloudguard-dev-reports-abhithcogni-logs`
- **Reports bucket properties:**
  - Versioning: ✅ Enabled
  - Public access block: ✅ All 4 settings = true
  - Encryption: SSE-KMS with the shared CMK (`module.kms.key_arn`), `bucket_key_enabled = true` (~99% fewer KMS API calls at no cost)
  - Lifecycle: GLACIER_IR @ 90 days, expire @ 365 days; noncurrent → GLACIER_IR @ 30d / expire @ 90d; abort incomplete multipart uploads after 7d
  - Access logging: delivered to logs bucket under `reports-access/` prefix
  - Bucket policy: `Deny` all non-TLS requests (defense-in-depth, also a Checkov requirement) + `Allow` the 4 Lambda role ARNs to `PutObject` / `GetObject` / `ListBucket`
- **Logs bucket properties:**
  - SSE-S3 (AES256), NOT KMS — see decision below
  - Versioning enabled, all public access blocked
  - Lifecycle: expire @ 90 days (access logs balloon fast)
  - Bucket policy: Deny non-TLS + Allow `logging.s3.amazonaws.com` to `PutObject`, scoped by `aws:SourceArn = reports bucket ARN` and `aws:SourceAccount = this account` — prevents log-injection from other accounts
- **Key decisions:**
  - **Bucket-name uniqueness via suffix variable (NOT random_id):** S3 bucket names are globally unique across all AWS accounts. Used `bucket_suffix` from gitignored tfvars (same pattern as the state bucket in STEP 3) — produces deterministic names that don't change between plans. `random_id` would force IAM to consume the bucket ARN as an output and break the clean "construct once in locals" pattern.
  - **Bucket name as a `local` in `dev/main.tf`, not reconstructed in each module:** The IAM module builds the bucket ARN for `s3:PutObject`; the S3 module creates the bucket. If each rebuilt the name from inputs, a future drift (someone changes the format in one module but not the other) silently breaks the permission. One `local`, two consumers.
  - **Logs bucket = SSE-S3, reports bucket = SSE-KMS:** S3 access-log delivery is performed by the `logging.s3.amazonaws.com` service principal; if the target bucket is SSE-KMS, the service needs `kms:GenerateDataKey` on the CMK, adding another grant for log metadata that contains no payload data. SSE-S3 is AWS's recommended path for log destinations.
  - **`GLACIER_IR` over `GLACIER`:** Reports may be linked from an audit ticket months later — Glacier Flexible Retrieval (hours to restore) blocks that workflow. GLACIER_IR keeps the same archive-tier pricing with millisecond retrieval.
  - **`Deny aws:SecureTransport = false` on both buckets:** Forces HTTPS for every S3 request. Checkov flags this as missing on every bucket without it. Belt-and-braces — TLS is the default in modern SDKs, but the explicit Deny makes it impossible to disable.
  - **Bucket policy on reports bucket allows the Lambda role ARNs directly (not `*` with a Condition):** Identity-policy + resource-policy = AND, so a leaked credential outside those 4 roles is blocked at the bucket even if its IAM policy says otherwise. Listing the principals explicitly makes the audit trivial — `cat bucket-policy.json` shows exactly who can write.
  - **`bucket_key_enabled = true`:** S3 bucket keys are envelope encryption at the bucket level. Each PutObject would normally call KMS `GenerateDataKey`; with bucket keys, one key per ~5-minute window is reused for all uploads. ~99% fewer KMS calls = ~99% lower KMS request bill. No security trade-off.
  - **`SourceArn` + `SourceAccount` conditions on the logs-bucket Allow:** Without these the policy would (theoretically) let any S3 logging service in any account write to this bucket. Scoping by source ARN and account is the AWS-recommended pattern for service-principal Allow statements (the "confused deputy" prevention).
- **`terraform plan` result:** ✅ `Plan: 30 to add, 0 to change, 0 to destroy.` — 12 IAM (STEP 4, now with `reports_bucket_arn` input) + 3 DynamoDB (STEP 5) + 2 KMS (STEP 6) + 13 S3 (this STEP). No apply (blueprint defers apply to STEP 18).

### ✅ STEP 8 — Build the SNS Terraform Module
*Completed: 2026-05-16 · Commit: `a01bf04`*

- **Files written:**
  - `terraform/modules/sns/main.tf` — 1 `aws_sns_topic` (KMS-encrypted), 1 `aws_sns_topic_subscription` (email), 1 `aws_sns_topic_policy` from an `aws_iam_policy_document` with 3 statements
  - `terraform/modules/sns/variables.tf` — `project`, `environment`, `kms_key_arn`, `alert_email` (regex-validated), `lambda_role_arns`
  - `terraform/modules/sns/outputs.tf` — `topic_arn`, `topic_name`, `email_subscription_arn`
- **Files modified (dev wiring):**
  - `terraform/environments/dev/main.tf` — added `module "sns"` consuming `module.kms.key_arn`, `var.alert_email`, and the 4 Lambda role ARNs
  - `terraform/environments/dev/outputs.tf` — added `sns_topic_arn`, `sns_topic_name`, `sns_email_subscription_arn`
- **Topic name produced:** `cloudguard-dev-alerts` (matches the ARN already granted in the IAM module's `local.alerts_topic_arn` — no IAM retrofit needed).
- **Topic properties:**
  - SSE-KMS via the shared CMK (`module.kms.key_arn`) — the KMS policy's `AllowLambdasViaSNS` Sid (with `kms:ViaService = sns.<region>.amazonaws.com`, written in STEP 6) covers Lambda Publish-time encrypt calls. No KMS policy change in this STEP.
  - One email subscription. AWS sends a confirmation email on `apply`; subscription stays in `PendingConfirmation` until the recipient clicks the link (3-day validity).
- **Topic policy — 3 statements:**
  1. `EnableRootAccountAdmin` — `sns:*` for `arn:aws:iam::<account>:root`. Same lockout-protection pattern as the KMS key policy in STEP 6.
  2. `DenyInsecureTransport` — `Deny sns:Publish/Subscribe` when `aws:SecureTransport = false`. Defense-in-depth against plaintext Publish from a misconfigured client.
  3. `AllowLambdaRolesPublish` — `sns:Publish` allowed only for the 4 Lambda execution role ARNs. Listed explicitly; no wildcards.
- **Key decisions:**
  - **Single fan-out topic over per-severity / per-category topics:** SNS's native model is one topic, many subscribers; routing CRITICAL-only later means a `FilterPolicy` on that subscription, not a second topic. Also keeps the IAM ARN list to one entry per role.
  - **Email-only this STEP, Slack deferred:** Keeps scope small. Slack would require Secrets Manager (not yet built) and an HTTPS subscription with retry policy — a future enhancement, not a STEP 8 requirement.
  - **Topic policy enumerates 4 Lambda role ARNs (not `Principal = "*"` + `aws:PrincipalArn` Condition):** Same defense-in-depth pattern as the S3 reports bucket. `cat topic-policy.json` shows exactly who can Publish. Identity-policy AND resource-policy must both Allow.
  - **`aws_iam_policy_document` data source for the topic policy** (not raw JSON heredoc): type-checked, references variables cleanly, typos surface at plan time. Same pattern used in the KMS and S3 modules.
  - **Email regex validation on the `alert_email` variable:** Catches typos at `plan` time instead of `apply` time when AWS rejects an invalid endpoint format. The regex is intentionally permissive (RFC 5322-strict is impractical and would reject valid edge cases).
  - **No SES hardening in this STEP** (open TODO). SES isn't actually called until the report_generator Lambda (post-STEP 13); scoping `ses:SendEmail` to a verified-identity ARN now would require editing the IAM module without a way to test the change. Deferred to the post-STEP 13 hardening pass.
- **`terraform plan` result:** ✅ `Plan: 33 to add, 0 to change, 0 to destroy.` — 30 prior + 1 SNS topic + 1 subscription + 1 topic policy. No apply (blueprint defers apply to STEP 18).
- **Note on email confirmation:** The first `terraform apply` (STEP 18) will trigger a `Subscription Confirmation` email from AWS. Until the link is clicked, the subscription's `pending_confirmation = true` and no alerts will deliver. This is intentional AWS UX — Terraform cannot self-confirm an email subscription (anti-spam design).

---

### ✅ STEP 9 — Build the Lambda Terraform Module (Reusable)
*Completed: 2026-05-16 · Commit: `dc75eeb`*

- **Files written:**
  - `terraform/modules/lambda/main.tf` — `data "archive_file"` + `aws_cloudwatch_log_group` + `aws_lambda_function` with `tracing_config`, `kms_key_arn`, `reserved_concurrent_executions`, `dynamic "environment"` block
  - `terraform/modules/lambda/variables.tf` — 14 inputs with type + range validation: `function_name` (regex), `project`, `environment`, `handler`, `runtime` (default `python3.12`), `role_arn`, `source_dir`, `environment_variables`, `timeout` (1–900, default 300), `memory_size` (128–10240 step 64, default 256), `layers`, `reserved_concurrent_executions` (default 5), `tracing_mode` (default `Active`), `kms_key_arn`, `log_retention_days` (default 30)
  - `terraform/modules/lambda/outputs.tf` — `function_arn`, `function_name`, `function_invoke_arn`, `log_group_name`, `log_group_arn`, `source_code_hash`
- **Files modified (KMS retrofit):**
  - `terraform/modules/kms/main.tf` — added 2 statements to the key policy:
    - `AllowLambdasViaLambda` — Lambda role ARNs get Encrypt/Decrypt/ReEncrypt*/GenerateDataKey*/DescribeKey scoped by `kms:ViaService = lambda.<region>.amazonaws.com` (env-var decrypt at cold start)
    - `AllowCloudWatchLogsEncrypt` — Service principal `logs.<region>.amazonaws.com` gets the encrypt suite scoped by `kms:EncryptionContext:aws:logs:arn` to `arn:aws:logs:<region>:<account>:log-group:/aws/lambda/${project}-${env}-*`
- **Files modified (dev wiring):**
  - `terraform/environments/dev/main.tf` — 4 invocations of the reusable lambda module (cost_scanner, security_scanner, resource_cleanup, report_generator). Each receives its own role ARN, source_dir, env vars wired to DynamoDB/SNS/S3, and the shared CMK. `report_generator` gets `memory_size = 512`, `timeout = 600`. `resource_cleanup` env vars include `AUTO_REMEDIATE = "false"` as a safety rail.
  - `terraform/environments/dev/outputs.tf` — added `lambda_function_arns`, `lambda_function_names`, `lambda_log_group_names` (each a map keyed by scanner)
  - `.gitignore` — added `**/.builds/` for `archive_file` scratch dirs
  - `terraform/environments/dev/.terraform.lock.hcl` — added `hashicorp/archive v2.8.0` (new provider dependency from `data "archive_file"`)
- **Defaults baked into the module (all defensible interview answers):**
  - `runtime = "python3.12"` — matches Lambda runtime (local is 3.13, known gap)
  - `timeout = 300` (5 min), `memory_size = 256` MB — sane for boto3-only workloads
  - `reserved_concurrent_executions = 5` — caps runaway-bill blast radius from a misfiring EventBridge schedule; -1 in caller would disable
  - `tracing_mode = "Active"` — X-Ray on every invocation (free up to 100k traces/month; gives Step Functions per-Lambda timeline)
  - `kms_key_arn` REQUIRED — env vars + log groups both encrypted with the shared CMK
  - `log_retention_days = 30` — long enough to debug, short enough to be free
- **Key decisions:**
  - **Explicit `aws_cloudwatch_log_group` with `depends_on` on the function:** Lambda auto-creates a log group with NO retention and NO encryption on first invocation. If we don't declare it in Terraform, the first apply succeeds, the function runs, CW Logs auto-creates an unmanaged group, and the next apply fails trying to create a group that already exists. Declaring explicitly + `depends_on` on the function forces ordering: log group first (managed, encrypted, retained), function second.
  - **`data "archive_file"` (zip at plan time) vs. a packaging script:** Blueprint STEP 9 says use `archive_file`; STEP 19 will add `scripts/package_lambdas.sh` for `pip install -r requirements.txt` bundling. Both can coexist — `archive_file` zips whatever's in `source_dir`. At STEP 19, the source_dir can point at a pre-populated `package/` folder. Module doesn't need to change.
  - **`dynamic "environment"` block:** AWS only encrypts env vars with the CMK if at least one variable is set. Passing an empty map without the dynamic block creates an empty `environment {}` block, which AWS rejects. The dynamic block removes it entirely when there's nothing to inject.
  - **KMS retrofit shape — Lambda env vars vs CloudWatch Logs are different statement structures:** Lambda env-var decrypt is the Lambda execution role calling Decrypt via the Lambda service → principal is the role ARN, condition is `kms:ViaService`. CloudWatch Logs encryption is the Logs service principal encrypting on its own → principal is `logs.<region>.amazonaws.com`, condition is `kms:EncryptionContext:aws:logs:arn` scoped to our log-group ARN pattern. Two separate Sids because the security models are genuinely different.
  - **Log-group ARN pattern scoped to `${project}-${environment}-*` not `*`:** Without the EncryptionContext condition, the grant would let CloudWatch Logs encrypt ANY log group in the account with this key — including log groups outside CloudGuard. The pattern restricts the grant to log groups under `/aws/lambda/cloudguard-dev-*`. Defense-in-depth at the encryption-context layer.
  - **`reserved_concurrent_executions = 5` default:** A bug in a Lambda + EventBridge retry loop has bankrupted real teams. Capping at 5 means even a runaway scheduler can't spawn 1000 concurrent executions burning Cost Explorer API quota. Caller can raise per-function if needed; the safety rail is "deny by default."
  - **`AUTO_REMEDIATE = "false"` env var on resource_cleanup:** The cleanup Lambda has `ec2:DeleteVolume` + `ec2:ReleaseAddress` — destructive perms. Default false means STEP 12 code will scan and log findings but NOT delete unless overridden (by Step Functions input later). Same belt-and-braces logic as the bucket-policy enumerations: code defaults to safe; explicit action required to enable destruction.
  - **`report_generator` gets 512 MB / 600 s** (vs 256 / 300 default): HTML generation + DynamoDB Scan + S3 PutObject runs on a daily/weekly schedule and shouldn't get timed out mid-report. CPU in Lambda is proportional to memory; 512 MB also speeds up Python startup and the templating loop.
  - **`hashicorp/archive` provider:** New dependency introduced by `data "archive_file"`. `terraform init -upgrade` fetched v2.8.0, lock file updated and committed.
- **`terraform plan` result:** ✅ `Plan: 41 to add, 0 to change, 0 to destroy.` — 33 prior + 4 `aws_lambda_function` + 4 `aws_cloudwatch_log_group`. No apply (blueprint defers apply to STEP 18).

### ✅ STEP 10 — Write the Cost Scanner Lambda
*Completed: 2026-05-17 · Commit: `fbdd001`*

- **Files written:**
  - `src/cost_scanner/handler.py` — Lambda entrypoint. Reads `FINDINGS_TABLE`, `COST_DATA_TABLE`, `ENVIRONMENT` env vars (wired in STEP 9). Calls `get_cost_data` → `store_cost_data` → `detect_anomalies` → `store_findings`. Returns `{anomalies_found, total_daily_cost, services_scanned}`.
  - `src/cost_scanner/cost_analyzer.py` — pure helpers split out so STEP 15 unit tests can mock the boto3 clients and call directly without invoking the Lambda runtime: `get_cost_data`, `detect_anomalies`, `store_cost_data`, `store_findings`, `_severity_for`.
  - `src/cost_scanner/requirements.txt` — `boto3` only (already in the Lambda runtime, but listed for the STEP 19 packaging script).
- **Anomaly detection design:**
  - 30-day daily granularity Cost Explorer pull, grouped by `SERVICE` dimension.
  - Baseline = average of the prior N–1 days (excludes the day being evaluated — otherwise the spike dilutes its own baseline).
  - Threshold = 1.5x. `ratio >= 2.0` → `CRITICAL`, `ratio >= 1.5` → `HIGH` (matches blueprint).
  - Skip services with `len(day_costs) < 2` (brand-new services have no baseline — would false-positive every time).
  - Skip services with `avg_cost <= 0` (no div-by-zero, and "$0 → $5" is a turn-on event, not an anomaly).
- **DynamoDB writes:**
  - `cost-data` table: PK `date`, SK `service_name`, attribute `unblended_cost` (Decimal). Re-running the same day overwrites the row — latest data wins, idempotent for end-of-day re-evaluation.
  - `findings` table: PK `finding_id` (uuid), SK `timestamp` (ISO-8601 UTC), `category = "cost"`, `severity`, `resource_id` (service name), `resource_type = "aws_service"`, `check_name = "cost_anomaly_30d_baseline"`, `description` (human-readable), `expected_cost` / `actual_cost` / `ratio` as Decimal, `expires_at` (epoch seconds, +90 days) for the table's TTL attribute.
  - Both tables written via `table.batch_writer()` context manager — auto-batches up to 25 items and retries unprocessed items.
- **Key decisions:**
  - **Pure helpers in `cost_analyzer.py`, side-effects in `handler.py`:** the helpers take a boto3 client/resource as a parameter — STEP 15 mocks the client with `unittest.mock.Mock()` and asserts call args without ever touching AWS. The handler is the thin shell that initializes real clients and wires env vars; nothing to test there beyond an integration test.
  - **`Decimal(str(amount))`, not `Decimal(amount)`:** DynamoDB rejects Python floats (uses Decimal internally). Converting via `str()` preserves the textual precision Cost Explorer sends; `Decimal(0.1)` would produce `Decimal('0.1000000000000000055511151231257827021181583404541015625')` which is correct but ugly in the table.
  - **Pagination via `NextPageToken` loop, not `paginate`:** Cost Explorer's `get_cost_and_usage` uses a non-standard pagination key (`NextPageToken` rather than `NextToken`) so the standard `client.get_paginator()` doesn't apply. Hand-roll the loop; same shape as AWS examples.
  - **`finding_id = uuid.uuid4()` per anomaly per run (not deterministic):** Blueprint specifies uuid. Means re-running the scanner on the same anomaly creates duplicate findings — TTL cleans up at 90 days. A deterministic key like `(date, service_name)` would be idempotent but the blueprint says uuid, so I'm following the blueprint. Trade-off acknowledged.
  - **No SNS publish from this Lambda:** Blueprint puts notification in the Report Generator (STEP 13). Cost scanner only writes to DynamoDB. The `SNS_TOPIC_ARN` env var wired in STEP 9 is unused here — left in place for future use, cheap.
  - **`logger.setLevel(logging.INFO)` at module load:** Lambda's default log level is WARNING. Setting INFO at module init means scanner progress shows up in CloudWatch by default; can be downgraded to WARNING via env var later if it gets noisy.
- **Local sanity testing:** `python -c "import handler; import cost_analyzer"` passes. `detect_anomalies()` smoke-tested with 5 synthetic services covering anomaly, no-anomaly, single-day, zero-baseline, and CRITICAL-vs-HIGH severity paths — all produced the expected output. Real unit tests are STEP 15.
- **Known gap — local Python 3.13 vs Lambda 3.12:** carried forward from STEP 1. Code uses no 3.13-only syntax. Will validate via Lambda runtime on the first STEP 18 apply.


### ✅ STEP 11 — Write the Security Scanner Lambda
*Completed: 2026-05-17 · Commits: `c16a9e2` (main), `c6c9ea8` (config_checker hotfix)*

- **Files written:**
  - `src/security_scanner/handler.py` — Lambda entrypoint. Reads `FINDINGS_TABLE` env var, initializes ec2/s3/iam/dynamodb clients, calls all 4 checkers, stamps `finding_id`/`timestamp`/`expires_at`, batch-writes to DynamoDB. Returns `{total_findings, by_severity, by_check}`.
  - `src/security_scanner/sg_checker.py` — `check_security_groups(ec2_client)`. Severity ladder: `0.0.0.0/0` on all-traffic or SSH(22)/RDP(3389) → CRITICAL; on any other non-80/443 port → HIGH; on 80/443 → skipped (legitimate web). Handles IPv6 `::/0` and the all-protocol sentinel (`IpProtocol = "-1"`).
  - `src/security_scanner/s3_checker.py` — `check_s3_buckets(s3_client)`. 4 sub-checks per bucket: public access block disabled (HIGH), no default encryption (HIGH), `Principal = "*"` in policy (CRITICAL), versioning disabled (LOW). Each per-bucket API wrapped in try/except so one bad bucket can't kill the scan.
  - `src/security_scanner/iam_checker.py` — `check_iam_users(iam_client)`. Per user: access keys > 90d old (MEDIUM), keys unused 90+ days (MEDIUM), no MFA (HIGH), AdministratorAccess attached directly (HIGH).
  - `src/security_scanner/ebs_checker.py` — `check_ebs_encryption(ec2_client)`. **Added beyond literal blueprint spec** — STEP 11 `lambda_handler` lists `check_ebs_encryption()` in its orchestrator but the blueprint did not give it a dedicated file/spec. Severity: unencrypted in-use volume → HIGH, available volume → MEDIUM.
  - `src/security_scanner/requirements.txt` — `boto3` only.
- **Files modified (IAM retrofit):**
  - `terraform/modules/iam/main.tf` — security_scanner role: combined `EC2SecurityGroupsRead` into `EC2SecurityReadOnly` adding `ec2:DescribeVolumes` for the EBS check. Both are Describe* (no resource-level perms possible), so single statement.
- **`terraform plan` result:** ✅ `Plan: 41 to add, 0 to change, 0 to destroy.` — same shape as STEP 9 (nothing applied yet, so the retrofit just changes JSON inside a not-yet-created inline policy). No apply (blueprint defers to STEP 18).
- **Local smoke test:** All 5 modules import cleanly under venv (Python 3.13.7). Synthetic-data run of `check_security_groups` with 5 fake SGs produced exactly the expected 3 findings (CRITICAL all-open, CRITICAL ssh-open, HIGH postgres-open), correctly skipping port-80 public and the internal-10.x SG. Real unit tests are STEP 15.
- **Key decisions:**
  - **One file per resource type (`sg_checker.py`, `s3_checker.py`, `iam_checker.py`, `ebs_checker.py`) + thin handler:** Same pattern as STEP 10's pure-helpers split. Each checker takes its boto3 client as a parameter, so STEP 15 mocks the client and asserts call args without ever touching AWS. The handler is just orchestration + stamping + batch-write — nothing to unit-test there.
  - **Per-resource try/except inside each checker:** Without it, one bucket the scanner can't read (e.g. cross-account ACL block) aborts the entire S3 check. The wrap-and-log pattern keeps the scan running over all the buckets/users/volumes that DO succeed and surfaces the failures in CloudWatch logs.
  - **`finding_id`/`timestamp`/`expires_at` stamped centrally in the handler:** Checkers return "raw" findings — fields the checker knows (resource_id, severity, etc.). Handler adds the cross-cutting fields. Keeps checkers pure and matches the schema invariant across all categories (cost + security + cleanup): same handler responsibility, every Lambda.
  - **CRITICAL threshold on SSH/RDP, HIGH on other public-non-80/443:** Matches blueprint, matches real risk model. SSH-open-to-world is the textbook lateral-movement entry. Other public TCP ports are still wrong but rarely interactive shell access.
  - **EBS check beyond literal spec (recommended option, user-confirmed):** Blueprint's STEP 11 lambda_handler explicitly calls `check_ebs_encryption()` but section 4 does not give it dedicated file detail. Two choices: skip and leave the handler broken, or build it. Built it, with the matching IAM retrofit, because it's a high-value security check (EBS-at-rest encryption is a SOC2/PCI control) and the cost is one file plus one extra Describe action.
  - **`metadata` dict only when non-empty:** DynamoDB rejects empty maps in some paths; popping the key when there's nothing to add keeps the item schema lean. Cost-scanner findings did the same (no metadata field at all).
  - **`logger.setLevel(logging.INFO)` at module load:** Same default as cost_scanner — surfaces per-check counts in CloudWatch by default; can be turned down later.
- **Open TODO (carried, not regressed):**
  - Scope `ses:SendEmail` to verified-identity ARN (from STEP 7).
  - Scope `ec2:DeleteVolume`/`ec2:ReleaseAddress` with tag-based Condition (from STEP 4).

- **Hotfix (same day, in-session catch by user):** `config_checker.py` was left empty after the main commit. Blueprint section 5 lists the file in the security_scanner folder, but STEP 11's per-file spec gives no detail and the `lambda_handler` orchestrator doesn't call it — same kind of internal inconsistency as the EBS checker. Implemented as a 5th checker:
  - `src/security_scanner/config_checker.py` — `check_config_compliance(config_client)`. Pages through `describe_compliance_by_config_rule` filtered to NON_COMPLIANT, then `get_compliance_details_by_config_rule` per rule. Emits one finding per non-compliant resource. Severity: MEDIUM default, with CRITICAL_RULES / HIGH_RULES sets for well-known managed rules (root-mfa, public-S3, encrypted-volumes, vpc-flow-logs, etc.).
  - **Graceful no-op** if AWS Config is not enabled in the account/region — catches `NoSuchConfigurationRecorderException` / `InvalidConfigurationRecorderName` / `AccessDeniedException` and returns `[]`. Critical for dev accounts where Config probably isn't on, AND for the scan to succeed in any region the Lambda runs in.
  - `handler.py` — added `check_config_compliance` to the import block, the boto3 `config` client, the `_run_check` orchestration, and `by_check` summary key.
  - `terraform/modules/iam/main.tf` — security_scanner role's `ConfigCompliance` Sid expanded to include `config:DescribeComplianceByConfigRule` alongside the existing `GetComplianceDetailsByConfigRule`. Both are account-scoped APIs — no resource-level perms possible.
  - `terraform plan` → `Plan: 41 to add, 0 to change, 0 to destroy.` (unchanged shape — nothing applied yet).
  - Imports + handler load verified under the venv.

### ✅ STEP 12 — Write the Resource Cleanup Lambda
*Completed: 2026-05-17 · Commit: `dfa147a`*

- **Files written:**
  - `src/resource_cleanup/zombie_finder.py` — three pure helpers (`find_zombie_ebs_volumes`, `find_unused_elastic_ips`, `find_old_snapshots`) taking an `ec2_client` parameter. Hardcoded ap-south-1 pricing constants (`EBS_PRICE_PER_GB_MONTH` map, `EIP_MONTHLY_COST_USD = 3.65`, `SNAPSHOT_PRICE_PER_GB_MONTH = 0.05`) with a documented effective date.
  - `src/resource_cleanup/handler.py` — Lambda entrypoint. Reads `FINDINGS_TABLE`, `REMEDIATION_LOG_TABLE`, `AUTO_REMEDIATE`, `SNAPSHOT_AGE_DAYS`, `ENVIRONMENT`, `LOG_LEVEL`. Stamps finding_id/timestamp/expires_at, batch-writes findings, runs remediation (or dry-run record) per resource type, batch-writes remediation log. Returns `{volumes_found, eips_found, snapshots_found, findings_written, estimated_monthly_savings_usd, auto_remediate_armed, remediations: {success, failed, skipped_dry_run}}`.
  - `src/resource_cleanup/requirements.txt` — `boto3` only.
- **Files modified (IAM retrofit):**
  - `terraform/modules/iam/main.tf` — `resource_cleanup` role's `EC2ZombieDelete` Sid extended with `ec2:DeleteSnapshot` (was just `DeleteVolume` + `ReleaseAddress`). Updated the WARNING comment at the top of the block to list all three destructive actions.
- **Severity ladder (cleanup category, by estimated monthly $ waste):**
  - `>= $50/mo` → HIGH
  - `>= $10/mo` → MEDIUM
  - else → LOW
- **Auto-remediate gating — two independent gates, both must opt in:**
  1. Env var `AUTO_REMEDIATE == "true"` (per-env hard stop; dev tfvars wires this to `"false"`)
  2. Event input `auto_remediate: true` (per-invocation flag; Step Functions/EventBridge can override)
  When either gate is false the run is dry-run and every action is logged as `SKIPPED_DRY_RUN` in the remediation log so the operator can see what *would* have happened.
- **Remediation log records:** `remediation_id` (uuid), `timestamp`, `resource_id`, `resource_type`, `action` (one of `delete_volume`/`release_address`/`delete_snapshot`), `status` (`SUCCESS`/`FAILED`/`SKIPPED_DRY_RUN`), `linked_finding_id`, `environment`, `error_message` (only when FAILED). PK is `remediation_id`, SK is `timestamp`, GSI on `status` (from STEP 5) supports "show me everything that failed in the last 24h" queries.
- **Key decisions:**
  - **Pure helpers (zombie_finder.py) + thin handler (handler.py):** same shape as cost_scanner + security_scanner. Helpers take a boto3 client param — STEP 15 will mock with `unittest.mock.Mock()` and assert on call args without an AWS round trip. Handler owns env vars, stamping, batch-writes, and the auto-remediate gate.
  - **Two-gate auto-remediate (event AND env, both must opt-in):** the event flag is the per-invocation switch (Step Functions input passes it for manual runs), the env var is the per-environment hard stop (dev `"false"`, prod `"true"` only after detection logic is trusted). One gate is not enough — a buggy event payload shouldn't be able to arm production, and a misconfigured env var shouldn't auto-remediate on every scheduled scan. Belt and braces.
  - **Dry-run records still written to the remediation log:** the operator wants to see "we would have deleted vol-X today" — that's the value of dry-run, not just silence. `SKIPPED_DRY_RUN` status is GSI-queryable from STEP 5.
  - **Hardcoded ap-south-1 pricing constants vs the AWS Pricing API:** Pricing API is us-east-1-only, requires an extra IAM grant + ~500ms per call. Volume pricing changes a few times per year — documented constants with an effective-date comment are auditable in CloudWatch logs and easy to update. Revisit if multi-region scanning is added.
  - **IAM retrofit added `ec2:DeleteSnapshot`:** Blueprint STEP 4 spec for the cleanup role lists `DeleteVolume` + `ReleaseAddress` but not `DeleteSnapshot`. STEP 12 needs it because the orchestrator handles snapshot remediation; leaving it out would make the auto-remediate path for snapshots silently fail. Same pattern as the STEP 11 IAM retrofit for `ec2:DescribeVolumes`. Scoped with the same `Resource = "*"` + tag-Condition hardening TODO as the other two destructive actions.
  - **Archive-tier snapshots skipped from cost estimates:** Archive snapshots have a different pricing model ($0.0125/GB-month vs $0.05/GB-month) and the `describe_snapshots` payload exposes the tier only sometimes. Skipping them is honest about the API limit — the rare Archive snapshot won't appear, but the standard-tier snapshots that dominate dev accounts will. Open TODO to add Archive-tier scanning when a Pricing API integration lands.
  - **`SNAPSHOT_AGE_DAYS` env var (default 180) instead of a hardcoded threshold:** lets a Step Functions input override the cutoff per-environment without a code change. Useful when prod has stricter retention policies than dev.
  - **`ClientError` caught around each delete call, but not the describe calls:** A `delete_volume` that fails (e.g. volume re-attached between scan and delete) shouldn't kill the batch — log the failure to the remediation table, move on. Describe failures already get logged as exceptions in the existing logging path and should surface — those are real bugs, not race conditions.
  - **`LOG_LEVEL` env var (default INFO) on the handler logger:** consistent with the STEP 9 module-level env var. Lets prod drop to WARNING if CloudWatch costs go up.
- **`terraform plan` result:** ✅ `Plan: 41 to add, 0 to change, 0 to destroy.` — same total as STEP 11 (the IAM retrofit only mutates JSON inside a not-yet-created inline policy; no new resources). No apply (blueprint defers to STEP 18).
- **Local smoke test:** Synthetic-data run via `unittest.mock`:
  - EBS volumes: 100 GB gp3 → LOW @ $9.12/mo; 600 GB io1 → HIGH @ $85.20/mo ✅
  - EIPs: 1 unattached found, 1 attached correctly skipped ✅
  - Snapshots: only old standard-tier flagged (MEDIUM @ $25); new + archive tier correctly skipped ✅
  - Auto-remediate gating: armed only when both env var AND event flag are true ✅
- **Open TODO (carried + new):**
  - Scope `ec2:DeleteVolume` / `ec2:ReleaseAddress` / `ec2:DeleteSnapshot` with Condition on tag `ec2:ResourceTag/AutoCleanup = true` (NEW: snapshot added to the tag-Condition surface).
  - Scope `ses:SendEmail` to verified-identity ARN.
  - Replace hardcoded ap-south-1 pricing constants with AWS Pricing API integration when multi-region scanning is added.

### ✅ STEP 13 — Write the Report Generator Lambda
*Completed: 2026-05-24 · Commit: `916d7bf`*

- **Files written:**
  - `src/report_generator/html_builder.py` — pure helpers, zero I/O. `build_report(findings, cost_data, remediations, *, window_hours, environment, generated_at=None)` returns a full self-contained HTML document; `build_email_summary(...)` returns a short email-body HTML with a "View full report" link button. All styling is `style=""`-attribute inline (no `<style>` block) so the same string renders identically in Gmail, Outlook, Apple Mail, and a browser. Severity → colour mapping: CRITICAL red, HIGH orange, MEDIUM yellow, LOW blue. Helper sections: `_executive_summary` (6 metric tiles), `_cost_section` (top-10 services × last-7-days pivot table), `_security_section` (findings grouped by severity), `_remediation_section` (cleanup findings + success/failed/dry-run remediation counts + a per-failure detail table when any failed).
  - `src/report_generator/handler.py` — Lambda entrypoint. Reads env vars (`FINDINGS_TABLE`, `COST_DATA_TABLE`, `REMEDIATION_LOG_TABLE`, `REPORTS_BUCKET`, `SES_SENDER_EMAIL`, `ALERT_EMAIL`, `SNS_TOPIC_ARN`, `REPORT_WINDOW_HOURS`, `ENVIRONMENT`, `LOG_LEVEL`). Event input `{"report_window_hours": N}` overrides the env-var default per-invocation. Orchestration: Scan all three tables with `FilterExpression` on timestamp ≥ cutoff → `build_report` → `s3.put_object` with `ContentType=text/html` and `ServerSideEncryption=aws:kms` → `generate_presigned_url` (TTL 7 days, SigV4 max) → `ses.send_email` with both text + HTML alternatives → `sns.publish`. SES + SNS failures are caught and logged but do NOT raise — the S3 object is the durable output, the email is a convenience channel.
  - `src/report_generator/requirements.txt` — `boto3` only.
- **Files modified (IAM retrofit):**
  - `terraform/modules/iam/main.tf` — `report_generator` role's `S3WriteReports` Sid renamed to `S3WriteAndReadReports`, action set widened from `["s3:PutObject"]` to `["s3:PutObject", "s3:GetObject"]`. **Why:** pre-signed GET URLs are signed by the calling principal; S3 evaluates the URL under the signing principal's identity policy at call time. Without `GetObject` on the role, every recipient clicking the email link gets `AccessDenied` even with a valid signature.
- **Files modified (dev wiring):**
  - `terraform/environments/dev/variables.tf` — added `ses_sender_email` (optional, default `""` — local in main.tf falls back to `alert_email` when empty) and `report_window_hours` (default 24, validated 1–720).
  - `terraform/environments/dev/main.tf` — new `local.ses_sender_email = var.ses_sender_email != "" ? var.ses_sender_email : var.alert_email`. Added `SES_SENDER_EMAIL` and `REPORT_WINDOW_HOURS` to the `report_generator` Lambda's `environment_variables`.
  - `terraform/environments/dev/terraform.tfvars.example` — documented both new optional vars with the rationale that `alert_email` doubles as the default sender so one SES verification click covers both ends.
- **S3 key layout:** `reports/<env>/<YYYY>/<MM>/<DD>/report-<window>h-<YYYYMMDDTHHMMSSZ>.html` — date-partitioned so a future Athena query or a manual `aws s3 ls` lands on the right window quickly. Window in the filename means the daily and weekly artefacts coexist in the same prefix without collision.
- **Pre-signed URL TTL:** `7 * 24 * 3600` seconds — the SigV4 hard cap. After expiry the link returns S3 403; the underlying object stays in S3 (Glacier_IR @ 90d, expire @ 365d per STEP 7 lifecycle), so re-running the Lambda regenerates a fresh URL pointing at the same key.
- **Key decisions:**
  - **HTML over PDF for both the email body AND the archived report:** PDF needs a third-party library (`weasyprint`, `reportlab`) that balloons the Lambda zip by 30–50 MB and forces a Lambda layer. HTML with inline-`style` attributes is zero dependencies, renders identically in browsers and email clients, supports Ctrl-F search in S3 viewing, and is the same string in both contexts — one template, two consumers. Trade-off: PDF would be slightly better for "save and email externally" use cases that don't exist here yet.
  - **Two artefacts (S3 full report + SES summary), one HTML generator:** Gmail clips inline HTML emails over ~100 KB and SES has a 10 MB hard message-size limit. A real CloudGuard report with 200 findings + 30 days × 50 services of cost data blows through both. The pattern matches every cloud-cost tool's daily digest: short summary in the inbox, full archive behind a link. `build_report` produces the archived artefact; `build_email_summary` reuses the same `_executive_summary` block and adds a button-styled link — DRY without coupling.
  - **One Lambda + two EventBridge rules (24h daily, 168h weekly) over two Lambdas:** Same code path, half the deployed-resources surface, one set of unit tests in STEP 15. EventBridge `Input` field passes `{"report_window_hours": 24}` or `{"report_window_hours": 168}`. The Lambda treats the event-input value as authoritative; env var is the fallback for direct-invocation tests.
  - **Scan with `FilterExpression` over Query on a 4th GSI:** Findings table has GSIs on `severity` and `category` only — neither has `timestamp` as a partition key. Time-window queries are the report Lambda's only access pattern, so the choices are (a) Scan + filter (cheap at low volume, $O(table size)$ at scale), or (b) add a 4th GSI on `(environment, timestamp)` to enable Query. At dev-account volumes (≤a few hundred findings/day) Scan is fine and avoids the extra index cost + the schema migration. Documented in the handler docstring as the documented TODO when findings volume grows. Same call as STEP 11's per-resource try/except — be honest about the limit, surface it loudly, defer the fix until it's justified.
  - **SES sender defaults to `alert_email`:** Sandbox SES requires both From and To to be verified identities — using the same address means one verification click covers both. The override path (`ses_sender_email` tfvar) exists for the production case where the From address is a verified domain (e.g. `reports@cloudguard.example.com`) and the To is a personal/distribution-list address that doesn't need its own verification because it's not used as a sender.
  - **SES `send_email` with both `Text` and `Html` alternatives in the same `Body`:** Email clients pick whichever they prefer; the text body is the fallback for clients (or screen readers) that strip HTML. Building both is one extra dict key — cheap insurance for accessibility and for any client that won't render HTML.
  - **SES / SNS failures caught, not raised:** The S3 object is the durable output of the Lambda — if SES rejects the message (sandbox SES, unverified recipient, etc.) the report is still archived and queryable. The handler logs the SES exception with full sender/recipient context so the operator sees it in CloudWatch but the invocation still returns success. SNS-publish gets the same treatment for the same reason. The alternative — re-raise on either failure — would lose the S3 archive on the first SES-not-verified attempt of a new environment.
  - **`ServerSideEncryption="aws:kms"` on the `put_object` call even though the bucket default already encrypts:** Belt-and-braces. The bucket default (STEP 7) applies SSE-KMS to every object, but specifying it explicitly on the PutObject means a future bucket-policy change (or an accidental `ServerSideEncryption=AES256` from another writer) doesn't silently downgrade this report's encryption.
  - **`html.escape()` on every dynamic value going into the HTML:** Resource IDs, descriptions, recommendations all come from DynamoDB and may contain `<`, `>`, `&` (e.g. a recommendation text mentioning `Principal = "*"`). Without escaping, a finding stamped with an HTML-fragment description would break the rendering and could in principle inject a `<script>` tag. None of CloudGuard's writers do this today, but `html.escape` is one line per substitution and removes the risk class entirely.
  - **`Decimal` → `float` coercion at the template boundary:** DynamoDB returns numbers as `decimal.Decimal`. Python's f-string `{value:,.2f}` works on Decimal too, but arithmetic comparisons (sum, max, `>` checks) get verbose. `_coerce_number()` normalises once at the template seam — same pattern as the `Decimal(str(amount))` coercion in cost_scanner, just the inverse direction (DynamoDB → Python instead of Python → DynamoDB).
- **`terraform plan` result:** ✅ `Plan: 41 to add, 0 to change, 0 to destroy.` — unchanged total. The IAM diff (`s3:GetObject` added) and the two new env vars live inside resources that haven't been applied yet, so they're inside the same 41-resource pending plan.
- **Local smoke test:** All imports clean under venv (Python 3.13.7). `build_report` rendered an 8700-char document from a 4-finding / 4-cost-row / 3-remediation synthetic dataset including CRITICAL/HIGH/MEDIUM severities, a SUCCESS + FAILED + SKIPPED_DRY_RUN remediation mix, and Decimal-typed cost values — assertions on `<!DOCTYPE html>`, `CRITICAL`, `sg-0abc`, `85.20`, `AmazonEC2` all pass. `build_email_summary` rendered 2737 chars with the pre-signed URL embedded and `View full report` button present. Empty-input edge case renders 3408 chars with the "No security or cost findings" / "No cost data" placeholders.
- **Manual setup required before first STEP 18 apply:**
  - SES identity verification for `alert_email` (AWS sends a one-click confirmation). Without verification, `send_email` fails with `MessageRejected` — the handler catches this so the report still archives to S3, but no email is sent.
  - SES sandbox lift OR add `alert_email` as a verified identity in the receiving role too (in sandbox, both From and To must be verified — already the case if sender defaults to `alert_email`).
- **Open TODO (carried + new):**
  - GSI on `(environment, timestamp)` once findings volume justifies it — currently a documented Scan+Filter limit.
  - Scope `ses:SendEmail` to verified-identity ARN via `Condition: { StringEquals: { "ses:FromAddress": var.alert_email } }` — same hardening surface flagged in STEP 8.
  - Move text body templating into `html_builder.py` (currently a 4-line f-string in `_send_email`) when it grows beyond a one-liner.

### ✅ STEP 14 — Write the Shared Utilities
*Completed: 2026-05-25 · Commit: `45e1560`*

- **Files written:**
  - `src/shared/aws_helpers.py` — `paginate(client, method_name, result_key, **kwargs)` (yields items across `client.get_paginator()` pages; raises `ValueError` for non-paginatable APIs like Cost Explorer's `NextPageToken`), `get_account_id(sts_client=None)` (STS `GetCallerIdentity`, module-scope cached), `get_all_regions(ec2_client=None)` (opted-in regions only via `AllRegions=False`).
  - `src/shared/dynamo_client.py` — `put_finding`, `batch_put_findings` (uses `table.batch_writer()` — auto-batches 25 items, retries `UnprocessedItems`), `query_findings_by_date` (Scan + FilterExpression on `timestamp`, documented limit carried from STEP 13), `query_findings_by_severity` (real Query against the `severity-index` GSI from STEP 5). All writes pass through `_coerce_decimals()` — recursive float→Decimal via `str()` (matches the `Decimal(str(...))` convention from `cost_analyzer.py`). Module-scope cached `boto3.resource("dynamodb")` for warm-invocation reuse.
  - `src/shared/notification.py` — `send_sns_alert` (dict messages JSON-encoded with `default=str` so `Decimal`/`datetime` survive; Subject capped at 100 chars before SNS truncates), `send_slack_webhook` (stdlib `urllib.request` POST, 5 s timeout, catches `URLError` + bare `Exception` and returns False — a Slack outage must NOT break a Lambda whose primary job is writing findings).
- **Files modified:** None — three STEP 2 stub files populated; no IAM/Terraform/handler changes.
- **No Terraform diff.** STEP 14 is pure Python. The shared package gets pulled into each Lambda zip by the STEP 19 packaging script (`cp -r ../shared ./package/`). At runtime each Lambda has its own bundled copy — no shared runtime across Lambdas (AWS Lambda execution isolation).
- **Local smoke test (13 checks, all pass):**
  - All three modules import cleanly under venv (Python 3.13.7).
  - `paginate`: yields 3 items across 2 mocked pages; raises `ValueError` when `can_paginate=False` (Cost Explorer surface).
  - `get_account_id`: returns `'123456789012'`; second call uses cache (STS `get_caller_identity` invoked exactly once).
  - `get_all_regions`: returns `['ap-south-1', 'us-east-1']` from a mocked `describe_regions` response.
  - `_coerce_decimals`: nested floats → `Decimal`; non-floats (str, int) untouched.
  - `batch_put_findings`: 2 items written via `table.batch_writer()` context manager; floats coerced to `Decimal('1.5')` / `Decimal('2.5')`; empty input returns 0 (no-op, no client call).
  - `put_finding`: single `PutItem` call with Decimal coercion.
  - `query_findings_by_severity`: paginates GSI Query — 2 `table.query` calls across `LastEvaluatedKey`, `IndexName='severity-index'` confirmed.
  - `query_findings_by_date`: Scan with `FilterExpression='#ts BETWEEN :start AND :end'` and `ExpressionAttributeNames={'#ts': 'timestamp'}` confirmed (reserved-word safety).
  - `send_sns_alert`: 150-char Subject truncated to exactly 100; dict message with `Decimal('1.5')` JSON-encoded via `default=str` → `"1.5"`.
  - `send_slack_webhook`: `URLError("connection refused")` caught, returns `False` (no raise propagates).
- **Key decisions:**
  - **`paginate` is `get_paginator`-only — refuses Cost Explorer:** Cost Explorer's `get_cost_and_usage` uses `NextPageToken` (not `NextToken`) and `boto3.client('ce').can_paginate('get_cost_and_usage')` returns `False`. Rather than silently fall back to a hand-rolled loop here, `paginate` raises `ValueError` with the service/method name — so the failure is loud and the caller (already correctly hand-rolling in `cost_analyzer.py`) doesn't accidentally call it. The alternative — implementing both paginator and NextPageToken loops in one function — would muddy the interface for the one API that needs it.
  - **`get_account_id()` uses a module-level cache:** Account ID is invariant for the lifetime of a Lambda execution environment. STS `GetCallerIdentity` is free but adds ~30 ms cold-start latency; caching it via a module-level global (`_ACCOUNT_ID_CACHE = None` flipped on first call) means warm invocations skip the call entirely. The `sts_client=None` injection point is the test seam — pass a `Mock()` in unit tests, leave it `None` in production.
  - **`query_findings_by_date` uses Scan + FilterExpression (documented limit):** Findings table PK is `finding_id` (uuid); no GSI has `timestamp` as a partition key. Real Query is not possible against the current schema. Two options: deviate from blueprint and add a 4th GSI on `(environment, timestamp)` now, or honour the schema and Scan + filter (cost O(table size); acceptable at dev volumes ≤ a few hundred findings/day). Chose Scan to match the documented limit already flagged in STEP 13's interview prep — function signature stays the same when the GSI lands later.
  - **`query_findings_by_severity` IS a real Query:** GSI `severity-index` (STEP 5) has PK `severity`, SK `timestamp`. `Key("severity").eq(severity)` is a proper Query — results return chronologically by SK by default. Paginated internally with `LastEvaluatedKey` until exhausted.
  - **`_coerce_decimals` is recursive, not flat:** Findings have nested dicts (e.g. `metadata: {sg_id: ..., port: ...}`) and could grow lists; a flat coercion would miss floats inside nested structures. Recursive walk is one extra `isinstance(item, dict)` + `isinstance(item, list)` branch; covers anything DynamoDB might receive without surprises.
  - **`send_slack_webhook` uses stdlib `urllib.request`, not `requests`:** Adding `requests` to every Lambda zip costs ~100 KB × 4 functions; stdlib `urllib.request` is in every Python runtime. The Slack webhook payload is JSON-only — no advanced HTTP features (sessions, retries, multipart) that `requests` would justify. Trade-off: slightly more verbose error handling for the network-tolerance contract.
  - **`send_slack_webhook` catches bare `Exception`, returns False — never raises:** A Lambda's primary durable output is the DynamoDB write or S3 archive. Slack is a convenience channel. If Slack is down, returning False (logged, visible in CloudWatch) is correct behaviour — re-raising would fail the entire invocation for a notification-channel issue. Same pattern as the SES/SNS error handling in STEP 13's `report_generator/handler.py`.
  - **`send_sns_alert` JSON-encodes with `default=str`:** SNS message attribute consumers downstream may parse the message as JSON. Findings dicts contain `Decimal` (from DynamoDB) and `datetime` (timestamps) — both non-JSON-native. `default=str` converts them to string representations rather than raising `TypeError`. Subject is capped at 100 chars on our side (rather than relying on SNS to truncate silently) so the truncation shows up in the caller's logs if it happens.
  - **All public functions take their boto3 client as an optional parameter (`sts_client=None`, `ec2_client=None`, `dynamodb_resource=None`, `sns_client=None`):** Same dependency-injection seam used in `cost_analyzer.py`, `sg_checker.py`, `zombie_finder.py`. Test code passes `Mock()` and asserts on `call_args`; production code passes nothing and gets the module-scope cached client. One pattern across the codebase — STEP 15 unit tests for `src/shared/` reuse the same mocking shape as the existing scanner tests will.
  - **No `requirements.txt` for `src/shared/`:** All imports are stdlib (`logging`, `decimal`, `json`, `urllib.request`) or `boto3` (already in every Lambda's requirements). Adding a separate requirements file would just duplicate what each scanner already declares — and the STEP 19 packaging script bundles `shared/` into each scanner's zip alongside its own `requirements.txt`-installed dependencies.
- **Refactor gap flagged (NEW Open TODO — see below):** The blueprint has no explicit STEP to refactor the 4 existing scanner Lambdas (cost_scanner, security_scanner, resource_cleanup, report_generator) to consume `src/shared/`. They were written in STEPs 10–13 before `shared/` existed, so each inlines its own `batch_writer()` loop, its own `Decimal(str(...))` coercion, its own SNS publish pattern. Recommended slot: first sub-task of STEP 15 (before writing unit tests), so tests cover the refactored code path. Bundled into the Open TODOs list below as **post-STEP 14**.

### ✅ STEP 15 — Write Unit Tests *(15a refactor + 15b tests both complete)*

#### ✅ STEP 15a — Lambda → shared utilities refactor (partial scope)
*Completed: 2026-05-25 · Commit: `545fdfc`*

- **Scope decided (user call):** adopt only `shared.dynamo_client.batch_put_findings` across the 4 Lambdas. The other STEP 14 helpers (`paginate`, `send_sns_alert`, `send_slack_webhook`, `query_findings_by_date`, `query_findings_by_severity`) stay available in `src/shared/` and will be unit-tested in 15b, but are NOT wired into the existing scanners. They're 1-line wrappers that would have been interface theatre — saved no real lines, added an import per call site, and coupled 4 Lambdas to a shared module for no functional gain.
- **Files written:**
  - `src/shared/__init__.py` — one-line docstring, makes `shared` a regular package (vs implicit PEP 420 namespace). Required by the STEP 19 packaging script's `cp -r ../shared ./package/` flow so the import path is `from shared.dynamo_client import …`.
- **Files modified:**
  - `src/cost_scanner/cost_analyzer.py` — `store_cost_data` and `store_findings` now build complete lists then call `batch_put_findings(table_name, items)`. Removed inline `with table.batch_writer():` blocks, removed manual `Decimal(str(...))` on three fields (helper's recursive `_coerce_decimals` handles it). Removed unused `from decimal import Decimal` import. Signature changed from `(table, ...)` to `(table_name, ...)`.
  - `src/cost_scanner/handler.py` — removed `dynamodb = boto3.resource("dynamodb")` and the two `dynamodb.Table(...)` lookups; passes env-var strings straight to the refactored helpers.
  - `src/security_scanner/handler.py` — deleted the local `_write_findings` wrapper, deleted `dynamodb = boto3.resource(...)` + the Table lookup, replaced the call site with `batch_put_findings(findings_table_name, all_findings)`.
  - `src/resource_cleanup/handler.py` — same shape as security_scanner for the findings write; in `_remediate`, replaced the inline `with remediation_log_table.batch_writer():` with `batch_put_findings(remediation_log_table_name, records)`. Removed the empty-records guard because `batch_put_findings` already no-ops on an empty list.
- **Latent bug fixed for free:** `resource_cleanup/handler._write_findings` was previously passing `metadata: {"monthly_cost_usd": float, ...}` straight to `batch.put_item`. boto3's DynamoDB resource raises `TypeError: Float types are not supported` on this. The bug never surfaced because STEP 18 (first apply) hasn't run. The refactor's recursive `_coerce_decimals` converts the nested float at the boundary, so the cleanup Lambda will now actually work on first invocation. Verified with a mocked `dynamodb_resource`:
  ```
  Type of monthly_cost_usd: Decimal
  Value: 85.2
  size_gb stayed int: int
  PASS — nested float coerced to Decimal recursively
  ```
- **Smoke tests (4/4 passed) — venv Python with `PYTHONPATH=['src/<lambda>','src']`:**
  - `import handler` succeeds for cost_scanner, security_scanner, resource_cleanup, report_generator.
  - report_generator is intentionally unchanged in 15a — it has no `batch_writer` writes (only Scans for the report-window queries from STEP 13).
- **Terraform plan:** ✅ `Plan: 41 to add, 0 to change, 0 to destroy.` — Python-only refactor; no IaC diff.
- **Net LOC saved:** ~30 lines across 4 files. Real win is consistency: three different Decimal-coercion styles (`cost_analyzer` coerced 3 specific fields, `resource_cleanup` coerced nothing, `security_scanner` coerced inline) collapsed to one recursive helper.
- **Key decisions:**
  - **Partial refactor over full:** Adopting `paginate` would have saved ~6 lines across security_scanner / resource_cleanup paginators; `send_sns_alert` would have saved ~2 lines in report_generator. Each adoption costs an import line and couples the Lambda to `shared/` at runtime. For helpers that save 1–2 lines, the coupling is interface theatre — keeping the wrappers in place obscures more than it shares. `batch_put_findings` is the exception: it consolidates three drifted Decimal-coercion styles into one recursive helper, fixes a latent float bug, and the call sites have identical shapes across all 4 Lambdas.
  - **Helper takes table NAME, not Table object:** Forced by `shared.dynamo_client.batch_put_findings`'s signature (which caches the `boto3.resource("dynamodb")` at module scope for warm-invocation reuse). Call-site cleanup is a net win — handlers no longer build Table objects, env-var lookups go straight to the helper.
  - **`__init__.py` is one line, not implicit namespace package:** Implicit (PEP 420) namespace packages work in Python 3 without `__init__.py`, but the explicit file makes it unambiguous which directories are packages and removes any edge-case risk in the Lambda runtime's importer.
  - **report_generator deliberately untouched:** It has no batch writes — its DynamoDB access is Scan-only (the documented STEP 13 limit with `Attr("timestamp").gte(cutoff_iso)`). `shared.dynamo_client.query_findings_by_date` could replace `_scan_with_filter`, but the filter expressions are different shapes (report_generator filters on `timestamp` for findings/remediation tables but on `date` for cost_data) — wrapping would require either a generic helper or three calls with different attr names. Defer to 15b as a test-driven decision: if `query_findings_by_date` ends up perfectly mirroring `_scan_with_filter`, refactor; if not, keep them separate.
- **New blocker for STEP 18 (documented loudly in Current Status):** `archive_file` in the Lambda Terraform module zips only `src/<lambda>/`. After 15a, every Lambda imports from `shared/` which is NOT in the source_dir. The deployed Lambda would fail with `ModuleNotFoundError: No module named 'shared'` on first invocation. Resolution paths:
  1. **STEP 19 first** — write `scripts/package_lambdas.sh` which does `cp -r ../shared ./package/` plus pip-install, then point the Lambda module's `source_dir` at the pre-built `package/`. This matches the blueprint's STEP 19 intent and PROGRESS's existing STEP 9 decision note.
  2. **Quick fix in STEP 18** — change the Lambda module to zip a 2-element list of source dirs, or add a `local-exec` provisioner that copies `shared/` before `archive_file` runs.
  Recommendation: option 1 (STEP 19 first). Update the linear order so STEP 19 runs before STEP 18 — this also matches the natural "package before deploy" mental model better than the blueprint's STEP 18 → STEP 19 ordering.

#### ✅ STEP 15b — Write the actual unit tests
*Completed: 2026-05-25 · Commit: `f6fb225`*

- **Files written:**
  - `tests/conftest.py` — wires `sys.path` to mirror the Lambda runtime (each Lambda's source dir + `src/` so `from shared.X import Y` resolves). Loads each Lambda's `handler.py` under a unique module name (`{lambda}_handler`) via `importlib.util.spec_from_file_location` so the 4 same-named `handler.py` files don't clobber each other in `sys.modules`. Autouse fixture resets the module-scope client caches in `shared/*` between tests so a previous test's `Mock()` doesn't leak into the next.
  - `tests/test_shared.py` — 22 tests across 8 classes. Covers `paginate` (success, kwargs forwarding, non-paginatable `ValueError`), `get_account_id` (success, cache reuse), `get_all_regions`, `_coerce_decimals` (float, nested dict, nested list, str passthrough, Decimal passthrough), `put_finding`, `batch_put_findings` (empty-list no-op, items via `batch_writer`, the recursive-Decimal fix for the latent `metadata.monthly_cost_usd` float bug), `query_findings_by_date` (FilterExpression shape + reserved-word alias + LastEvaluatedKey pagination), `query_findings_by_severity` (GSI Query), `send_sns_alert` (string + dict messages with Decimal `default=str` handling, 100-char Subject truncation), `send_slack_webhook` (HTTP 2xx, non-2xx, URLError, bare Exception — all four paths must return False without raising).
  - `tests/test_cost_scanner.py` — 19 tests. `detect_anomalies`: 3x → anomaly, 1.8x → anomaly, 1.4x → no anomaly, <2-day skip, zero-baseline skip, baseline-excludes-self path, multi-service independence. `_severity_for`: parametrised across 6 ratio/severity pairs. `get_cost_data`: single-page parse, NextPageToken pagination (the hand-rolled loop because Cost Explorer doesn't support `get_paginator`). `store_cost_data`: pivots to one row per day per service. `store_findings`: CRITICAL severity for ratio ≥ 2.0, HIGH for 1.5–2.0, uuid + 90-day TTL stamping, empty-list path.
  - `tests/test_security_scanner.py` — 23 tests across 5 checkers. SG checker: SSH/RDP/all-traffic CRITICAL, non-admin-port HIGH, port 80 not flagged, internal-CIDR not flagged, IPv6 `::/0` SSH CRITICAL. S3 checker: no PAB / PAB-partially-disabled / no-default-encryption HIGH, `Principal = "*"` CRITICAL, versioning-disabled LOW, clean-bucket no findings, one-bad-bucket-doesn't-kill-the-scan resilience. IAM checker: old key MEDIUM, unused key MEDIUM, no MFA HIGH, AdministratorAccess attached HIGH, clean user no findings. EBS checker: unencrypted in-use HIGH, available MEDIUM, encrypted no finding. Config checker: graceful no-op on `NoSuchConfigurationRecorder` and `AccessDenied`, CRITICAL_RULES promotion (root-mfa-enabled), HIGH_RULES promotion (encrypted-volumes), unknown-rule default MEDIUM.
  - `tests/test_resource_cleanup.py` — 17 tests. Zombie-finder: 100 GiB gp3 LOW @ $9.12, 600 GiB io1 HIGH @ $85.20, `Filters=[status=available]` correctly passed to paginator. EIP: unattached LOW @ $3.65, attached skipped. Snapshot: old standard MEDIUM @ $25, new skipped, archive-tier skipped, default-tier treated as standard. **Two-gate auto-remediate matrix (the unique interview talking point):** parametrised across all 4 env-var × event-flag combinations — only `[true, true]` returns True; missing env var defaults to False; non-dict event treated as no-flag. `_remediate`: dry-run produces `SKIPPED_DRY_RUN` records and zero delete calls; success produces a SUCCESS record + the `delete_volume(VolumeId=...)` call; `ClientError` produces a FAILED record with `error_message` carried through; unknown `resource_type` produces no record (no half-finished remediation).
  - `tests/test_report_generator.py` — 14 tests. `build_report`: full HTML structure (DOCTYPE, lang, generated-at stamp, severity badges, resource IDs, cost-trend table, failed-remediation error table); empty-input placeholders ("No security or cost findings", "No cost data", "No cleanup activity"); `html.escape` defends against a finding `description = "<script>..."` (the escaped form is present, the raw form is not); estimated-savings reads cleanup-finding `metadata.monthly_cost_usd`. `build_email_summary`: "View full report" button + pre-signed URL embedded; URL with `&` correctly escaped to `&amp;`. `_coerce_number`: Decimal → float, None → 0.0, str → float, unparseable → 0.0. `_scan_with_filter`: LastEvaluatedKey pagination. `_build_s3_key`: `reports/<env>/<YYYY>/<MM>/<DD>/report-<window>h-<UTC stamp>.html` shape verified for both 24h and 168h windows. **The fail-tolerance contract (interview-grade):** `_send_email` returns False on `ClientError` and does NOT raise (the S3 archive is the durable signal); `_publish_sns` swallows ClientError the same way; subject line gains `— N CRITICAL` only when count > 0.
- **Test totals:** `pytest tests/ -v` → **106 passed, 0 failed in 7.52 s.**
- **No production code changes.** STEP 15b is pure test surface — no IaC diff, no source diff, no terraform plan recount.
- **Key decisions:**
  - **`unittest.mock` only — no `moto` dependency** (yet): blueprint STEP 21 lists `moto` in the CI pipeline's `pip install` line, but STEP 15 says `unittest.mock`. moto's full-fidelity AWS emulation is overkill for unit tests of pure helpers — we're asserting on call args, not round-trip behaviour. moto becomes the right tool when an integration-test layer lands, which is post-STEP-20.
  - **One test file per Lambda + one for `src/shared/`:** matches the blueprint shape; matches the 1:1 source-to-test-file mental model an interviewer will look for. The shared-helpers tests live in their own file because the helpers are consumed by multiple Lambdas — putting them in any one scanner's file would make ownership unclear.
  - **Test helpers consume `MagicMock()`, not `boto3.client(...)`:** every public function in `src/shared/`, `src/cost_scanner/cost_analyzer.py`, `src/security_scanner/*_checker.py`, `src/resource_cleanup/zombie_finder.py` already takes its boto3 client as an optional parameter (the dependency-injection seam from STEP 10 onwards). Tests inject `MagicMock()`s and assert on `call_args`/`call_args_list`. No `patch("boto3.client")` global monkeypatching; no AWS credentials in any test.
  - **`importlib.util.spec_from_file_location` for the 4 `handler.py` files:** four Lambdas, each with its own `handler.py`. Importing two of them in the same pytest session would otherwise cache the first into `sys.modules['handler']` and the second import would get the wrong file. Loading them under unique names (`resource_cleanup_handler`, `report_generator_handler`) sidesteps the collision and lets one pytest run cover all four.
  - **Autouse fixture to reset shared module caches:** `shared.aws_helpers._ACCOUNT_ID_CACHE`, `shared.dynamo_client._DDB_RESOURCE`, `shared.notification._SNS_CLIENT` are module-scope globals that production callers benefit from (one cached client per Lambda execution environment). In tests, they need to be cleared between cases so a previous test's STS account ID doesn't leak into a later test's `get_account_id()` call.
  - **Auto-remediate matrix is the unit test that pays off the two-gate decision from STEP 12:** the interview answer for "how do you stop a buggy scanner from deleting prod resources?" is "two independent gates, both must opt in" — and the matrix test is the evidence in CI that the gating actually works across all 4 combinations. Without the test, the next refactor could quietly turn an `and` into an `or` and STEP 12's whole safety story would collapse.
  - **SES/SNS fail-tolerance asserted as the unique handler-level contract for report_generator:** every other testable surface in that Lambda is covered by `html_builder` tests. The handler's unique contract is "if SES rejects (the most likely sandbox-SES failure mode), the report is still in S3 and the Lambda returns success." A future refactor that re-raises on ClientError would break that contract; the test pins it.
  - **No tests against `lambda_handler` itself:** the four `lambda_handler` entry points are thin orchestration (env-var reads + helper calls + return summary). The interesting logic lives in the helpers, which are tested directly. End-to-end coverage lands at STEP 20 (live Step Functions execution against a deployed dev stack).

### ✅ STEP 16 — Build the Step Functions Workflow
*Completed: 2026-05-25 · Commit: `b1acf07`*

- **Files written:**
  - `terraform/modules/step-functions/main.tf` — 1 `aws_sfn_state_machine` (STANDARD), 1 `aws_cloudwatch_log_group` (CMK-encrypted, 30-day retention), 1 `aws_iam_role` (assume by `states.amazonaws.com`), 3 `aws_iam_role_policy` (lambda-invoke, vended-logs-delivery, x-ray). ASL written as an HCL object passed through `jsonencode()` so Terraform validates structure and Lambda ARNs interpolate natively.
  - `terraform/modules/step-functions/variables.tf` — 11 inputs with validation: `project`, `environment`, 4 Lambda ARNs, `kms_key_arn`, `log_retention_days` (CW-valid list), `logging_level` (ALL/ERROR/FATAL/OFF), `include_execution_data` (default true), `tracing_enabled` (default true), `scanner_retry_max_attempts` (default 2), `scanner_retry_backoff_rate` (default 2.0), `report_retry_max_attempts` (default 2).
  - `terraform/modules/step-functions/outputs.tf` — `state_machine_arn`, `state_machine_name`, `role_arn`, `role_name`, `log_group_name`, `log_group_arn`.
- **Files modified (KMS retrofit):**
  - `terraform/modules/kms/main.tf` — added `local.sfn_log_group_arn_pattern` (`/aws/vendedlogs/states/cloudguard-${env}-*`) and added it to the `values` list on the existing `AllowCloudWatchLogsEncrypt` Sid's `kms:EncryptionContext:aws:logs:arn` `ArnLike` condition. Same Sid, widened scope — no new statement, no new principal.
- **Files modified (dev wiring):**
  - `terraform/environments/dev/main.tf` — added `module "step_functions"` consuming the 4 `module.<scanner>.function_arn` + `module.kms.key_arn`.
  - `terraform/environments/dev/outputs.tf` — added `step_functions_state_machine_arn`, `step_functions_state_machine_name`, `step_functions_role_arn`, `step_functions_log_group_name`.
- **State machine name:** `cloudguard-dev-workflow`
- **Log group name:** `/aws/vendedlogs/states/cloudguard-dev-workflow-Logs` (AWS-recommended prefix — console groups these under the SFN service view).
- **ASL shape (matches blueprint, with two judgment-call additions):**
  - `ParallelScanners` runs cost / security / cleanup in parallel; each branch has `Retry` (`MaxAttempts=2`, `BackoffRate=2.0`, `IntervalSeconds=1`) + `Catch` → a `Pass` state emitting `{status: FAILED, scanner: <name>}` so a single failure does not block the report.
  - **`ResultPath = "$.scanner_results"`** on `ParallelScanners` (beyond blueprint) — preserves the original input (e.g. `{report_window_hours: 168}` from EventBridge in STEP 17) and stores the 3-branch array under `scanner_results`. Without this, the `GenerateReport` Lambda receives a bare array and `event.get("report_window_hours")` raises AttributeError.
  - **`ResultPath = "$.error"` on each Catch** (beyond blueprint) — keeps the error payload reachable in execution history; the Pass result still controls the branch's output shape.
  - `GenerateReport` final state, `Retry MaxAttempts=2`, `End=true`.
- **Key decisions:**
  - **STANDARD over EXPRESS workflow:** STANDARD supports up to 1-year executions, stores complete history in the SFN console (the visible execution graph the blueprint's STEP 20 verification depends on), and bills per state transition. EXPRESS caps at 5 minutes, bills per request, and is the right tool for high-TPS event-driven workloads — not a 6-hourly scan. The single-line `type = "STANDARD"` is the only thing distinguishing them, but the cost/usability trade-off matters in interviews.
  - **ASL as `jsonencode(HCL object)`, not a `templatefile()`-loaded JSON file:** Terraform validates the structure at plan time, Lambda ARNs interpolate as native HCL references (`var.cost_scanner_arn` not `${cost_scanner_arn}`), and the Retry parameters surface as module variables for per-env tuning (prod could raise `MaxAttempts` to 3 without touching a JSON file). Trade-off: the ASL is no longer "copy-pasteable into the SFN console designer." Acceptable because the source of truth is Terraform.
  - **`ResultPath` on `ParallelScanners` to preserve workflow input:** the blueprint ASL omits this, so its default behaviour is "Parallel output replaces input" — and the report_generator Lambda's input contract from STEP 13 (`event.get("report_window_hours")`) requires the original input shape. Setting `ResultPath = "$.scanner_results"` is the standard SFN idiom for "merge parallel output into the existing input." Honest deviation from the blueprint, documented loudly.
  - **Catch → Pass per branch (not Catch → Fail):** A single scanner crashing must not block the daily/weekly report. The Pass node emits a structured `{status: FAILED, scanner: …}` so the report can render a "scanner X failed" banner over partial results. Alternative — Catch → Fail — would abort the workflow on any failure, losing the partial signal.
  - **STANDARD workflow's `Retry MaxAttempts=2, BackoffRate=2.0, IntervalSeconds=1`:** waits 1s → 2s → 4s between tries (3 total attempts including the original). Matches the blueprint exactly. The `IntervalSeconds=1` was added explicitly because the blueprint's JSON omits it and SFN's default is 1 anyway — explicit is more readable in execution-history JSON.
  - **`aws_iam_role` + three separate `aws_iam_role_policy` resources:** matches the existing IAM module's pattern (one inline policy per concern: lambda-invoke, logs-delivery, x-ray). Splitting by concern means a future "drop X-Ray" or "downgrade logging to ERROR" is one policy resource change — no need to re-author a combined blob. Alternative — one combined `aws_iam_role_policy` — works but obscures the three independent grants and makes blast-radius reasoning harder.
  - **`lambda:InvokeFunction` listed against the 4 ARNs + `<arn>:*` (versions/aliases), no wildcards:** A future Lambda accidentally named `cloudguard-dev-anything` cannot be invoked by THIS workflow without an IAM change. Wildcard form (`arn:aws:lambda:*:*:function:cloudguard-${env}-*`) would have allowed it implicitly. Same defense-in-depth posture used in the S3 bucket policy (STEP 7) and the SNS topic policy (STEP 8).
  - **Vended-logs delivery permissions (`logs:CreateLogDelivery` + 7 friends) on `Resource = "*"`:** AWS-documented requirement for SFN logging — these APIs do not support resource-level perms (they manage account-scope log-delivery configurations, not specific log groups). The narrow grant is via the log GROUP's KMS encryption context, not via the IAM resource ARN.
  - **`tracing_configuration.enabled = true` + 4 X-Ray actions:** Combined with the Lambda module's `tracing_mode = "Active"` (STEP 9) this produces an end-to-end X-Ray timeline — state transitions and per-Lambda invocation latency are visible on one trace map. Free up to 100k traces/month; CloudGuard's volume is ~1 execution per 6 hours.
  - **`logging_configuration { level = "ALL", include_execution_data = true }`:** ALL captures every state transition plus input/output payloads. At dev volume (~120 executions/month) this stays inside the CloudWatch free tier. Prod can downgrade to ERROR if log volume becomes an issue. `include_execution_data = true` is intentional for the dev environment — the only PII risk in payloads is the alert email address, which is already in plenty of other places.
  - **KMS retrofit shape — widened the existing `AllowCloudWatchLogsEncrypt` Sid's ArnLike pattern list, didn't add a new Sid:** The CloudWatch Logs service principal is the same in both cases (Lambda log groups + SFN log groups). The encryption-context ARN pattern is the only differentiator. ArnLike with a list of patterns evaluates as OR — adding `/aws/vendedlogs/states/cloudguard-${env}-*` alongside the existing Lambda pattern is the minimum change. Alternative — add a second `AllowCloudWatchLogsEncryptSFN` Sid — would have been more verbose with no security benefit.
  - **`depends_on` on the state machine for the 3 inline policies:** Step Functions can accept a state machine create call before the role's policies have propagated; first invocation would then fail with `AccessDenied`. Explicit `depends_on` forces Terraform to attach the policies first, then create the state machine.
- **`terraform plan` result:** ✅ `Plan: 47 to add, 0 to change, 0 to destroy.` — 41 prior + 1 state machine + 1 log group + 1 IAM role + 3 inline policies. KMS retrofit is a JSON-internal change to a not-yet-applied key policy (no resource diff). No apply (blueprint defers to STEP 18).
- **What's explicitly NOT in this STEP:**
  - EventBridge schedule (STEP 17 — `rate(6 hours)` + daily-report `cron(30 2 * * ? *)`).
  - Passing `auto_remediate: true` from EventBridge/manual start (STEP 17 / STEP 18 plumbing). The state machine's input passes verbatim into each Task, so adding it later is purely an EventBridge `Input` field change.
  - Live `terraform apply` (STEP 18 — and only after STEP 19's packaging script runs first).
- **Interview Prep Note:** "Why Step Functions instead of chaining Lambdas with SNS/SQS?" — chaining via SNS gives you fan-out but loses orchestration: no retry policy per step, no central failure visualisation, no parallel-with-aggregate semantics, no per-state input/output transformation. Step Functions gives you all four with one declarative ASL. The cost is per-state-transition billing (cheap at our scale) and being locked into AWS. Also be ready to explain (a) Parallel state's ResultPath behaviour and why we use `$.scanner_results`, (b) why each branch has Catch → Pass rather than Catch → Fail (partial results > all-or-nothing), (c) why STANDARD over EXPRESS for this workflow, (d) the difference between SFN's Retry block and a Lambda's reserved-concurrent-executions cap.

### ✅ STEP 17 — Build the EventBridge Terraform Module
*Completed: 2026-05-27 · Commit: `32c9b54`*

- **Files written:**
  - `terraform/modules/eventbridge/main.tf` — 3 × `aws_cloudwatch_event_rule` (scan_schedule, daily_report, weekly_report), 3 × `aws_cloudwatch_event_target`, 1 × `aws_iam_role` (`events.amazonaws.com` trust), 1 × `aws_iam_role_policy` (`states:StartExecution` on the one state machine ARN), 2 × `aws_lambda_permission` (resource-based perms allowing each Lambda-targeted rule to invoke `report_generator`).
  - `terraform/modules/eventbridge/variables.tf` — 12 inputs: `project`, `environment`, `state_machine_arn`, `report_lambda_arn`, `report_lambda_name`, 3 × schedule expression (defaults: `rate(6 hours)`, `cron(30 2 * * ? *)`, `cron(30 2 ? * MON *)`), `auto_remediate` (bool, default `false`), 2 × window-hours tunables (24/168, range-validated 1–720), 3 × per-rule `enabled` toggles for non-destructive pause.
  - `terraform/modules/eventbridge/outputs.tf` — `scan_rule_name`/`scan_rule_arn`, `daily_report_rule_name`/`arn`, `weekly_report_rule_name`/`arn`, `role_name`, `role_arn`.
- **Files modified (dev wiring):**
  - `terraform/environments/dev/main.tf` — added `module "eventbridge"` consuming `module.step_functions.state_machine_arn` + `module.report_generator.function_arn` + `module.report_generator.function_name`. `auto_remediate = false` set explicitly with the same safety-rail comment style as the STEP 12 module wiring.
  - `terraform/environments/dev/outputs.tf` — added `eventbridge_rule_names` (map), `eventbridge_rule_arns` (map), `eventbridge_role_arn`.
- **Rule names produced:**
  - `cloudguard-dev-scan-schedule` → state machine, every 6 hours, Input: `{auto_remediate:false, source:"eventbridge_scan_schedule"}`
  - `cloudguard-dev-daily-report` → report_generator Lambda, 02:30 UTC (08:00 IST) daily, Input: `{report_window_hours:24, source:"eventbridge_daily_report"}`
  - `cloudguard-dev-weekly-report` → report_generator Lambda, 02:30 UTC Mondays (08:00 IST Monday), Input: `{report_window_hours:168, source:"eventbridge_weekly_report"}`
- **Key decisions:**
  - **Weekly report rule (3rd rule, beyond literal blueprint).** Blueprint STEP 17 specifies only the daily rule. STEP 13's report Lambda was designed for both 24h and 168h windows ("one Lambda + two EventBridge rules") and reads `event.get("report_window_hours")` as authoritative. Adding the weekly rule completes the matching half of the STEP 13 design — same code path, half the deployed-resources surface that two separate report Lambdas would cost. Honest deviation, called out loudly in this entry.
  - **`auto_remediate=false` on the periodic scan rule.** The Lambda's two-gate design (STEP 12) means both `env_var AUTO_REMEDIATE=true` AND `event.auto_remediate=true` must be set for actual deletion. Setting the EventBridge gate to false means **no automated cron run can ever delete resources**, regardless of what the env var is — the cleanup Lambda always sees `auto_remediate=false` from a scheduled invocation. Remediation requires (a) env var flip via Terraform PR, AND (b) human manually starting SFN from console with `auto_remediate=true`. True async per-resource approval is the STEP 25 stretch goal.
  - **Two different invocation paths in one module.** EventBridge → Step Functions uses an `aws_iam_role` (`role_arn` on the target) carrying `states:StartExecution`. EventBridge → Lambda uses `aws_lambda_permission` (resource-based policy on the Lambda) — AWS resolves direct-Lambda invocations through the function's policy, not through the rule's IAM role. Using both is the AWS-documented pattern; one would have been wrong for one target type. Documented in the module header so a future reader doesn't try to consolidate them.
  - **`aws_lambda_permission.source_arn` scoped to the specific RULE ARN, not the function ARN, not "*".** A second EventBridge rule in the account cannot invoke the report Lambda without an explicit `aws_lambda_permission` resource granting it. Combined with `aws_lambda_permission.source_account = data.aws_caller_identity.current.account_id` this defends against the "confused deputy" pattern where another account's EventBridge could in principle invoke this Lambda — same hardening shape as the S3 logs-bucket policy from STEP 7.
  - **`states:StartExecution` enumerated on the one state machine ARN, no wildcards.** A future state machine accidentally named `cloudguard-${env}-anything` cannot be started by this role. Same defense-in-depth posture as the SFN role's `lambda:InvokeFunction` (STEP 16) and the S3/SNS bucket-policy enumerations (STEPs 7–8).
  - **AWS cron is 6 fields (with YEAR), not standard 5-field cron.** `cron(30 2 * * ? *)` = minute 30, hour 2, any day-of-month, any month, **`?` for day-of-week** (mutually exclusive with day-of-month), any year. The `?` placeholder is required where `*` would otherwise collide between DoM and DoW — pick one to specify, use `?` on the other. Documented in `variables.tf` for the next reader who reaches for standard cron syntax and gets a plan-time error.
  - **All times in UTC; 08:00 IST = 02:30 UTC.** EventBridge cron is always UTC; no DST in either UTC or IST so this conversion is stable year-round. Comment in `variables.tf` carries the arithmetic so a future maintainer doesn't have to re-derive it.
  - **Per-rule `enabled` boolean toggles.** Setting a rule to `DISABLED` keeps it in state at zero cost (EventBridge bills nothing for disabled rules). Useful for: (a) freezing scheduled runs while debugging in dev, (b) pausing prod during a known maintenance window. Cheaper than destroying + recreating, and the toggle is reversible with one `terraform apply`.
  - **`source` field in the EventBridge Input JSON.** Each rule's Input includes a `source: "eventbridge_<rule>"` discriminator. The Lambda doesn't read it today, but CloudWatch / X-Ray traces (STEP 9 / 16 enabled both) carry the event payload — being able to grep `eventbridge_weekly_report` in a trace is a "future me" gift that costs nothing now.
  - **`source_account` on `aws_lambda_permission`.** Without it, the policy technically allows any account's EventBridge service principal to construct a rule with a matching `source_arn` pattern and invoke. Scoping to `data.aws_caller_identity.current.account_id` makes the grant strictly intra-account. Same `aws:SourceAccount` defense-in-depth pattern as the S3 logs bucket policy (STEP 7).
  - **No `depends_on` from `aws_cloudwatch_event_target` → `aws_iam_role_policy`.** Terraform infers the dependency via the implicit reference (`aws_iam_role.eventbridge.arn` on the target). The SFN module's explicit `depends_on` was a guard against a known SFN-vs-policy-propagation race that doesn't apply here — EventBridge doesn't pre-validate the role's permissions at create time, it surfaces failures on first rule fire (visible in CloudWatch). Belt without braces, judged sufficient.
- **`terraform plan` result:** ✅ `Plan: 57 to add, 0 to change, 0 to destroy.` — 47 prior + 3 event_rules + 3 event_targets + 1 IAM role + 1 inline policy + 2 lambda_permissions = 10 new resources. Verified by counting `# module.eventbridge.*` lines in the plan output. No apply (blueprint defers to STEP 18).
- **Interview Prep Note:** "How do you trigger your scanning workflow and stop the automation from doing something destructive?" — Two EventBridge rules drive everything: a `rate(6 hours)` rule that calls `StartExecution` on the Step Functions state machine (via an IAM role with a single least-privilege grant), and a `cron(30 2 * * ? *)` daily rule that invokes the report_generator Lambda directly (via an `aws_lambda_permission` resource policy). The destructive-action guard is **two independent gates** the cleanup Lambda checks: the per-environment `AUTO_REMEDIATE` env var (set via Terraform PR, code-reviewed) AND the per-invocation `auto_remediate` field in the event payload (the scheduled rule hardcodes this to `false`, so scheduled runs are always dry-run). Real remediation requires a human flipping the env var AND a human manually starting the workflow from the Console with `auto_remediate=true`. True async per-resource approval — operator clicks Approve/Reject in an SES email and the Step Functions `.waitForTaskToken` resumes — is the post-DoD stretch goal. Also be ready to explain: why AWS cron uses 6 fields with `?`, why `aws_lambda_permission` is needed for direct-Lambda targets but not for SFN targets, and why `source_arn` + `source_account` on the Lambda permission prevent the confused-deputy attack.

### ✅ STEP 17.5 — Remove GenerateReport from SFN Workflow (kill 6-hourly report redundancy)
*Completed: 2026-05-27 · Commit: `60c1634`*

- **The problem (user-spotted, 2026-05-27):** With STEP 17 wired as-is, three different schedules ended up invoking `report_generator`:
  - **6-hourly** via the SFN `GenerateReport` final state (4 invocations/day, falls back to env-var `REPORT_WINDOW_HOURS=24` because the SFN target Input doesn't pass `report_window_hours`)
  - **Daily** at 08:00 IST via the EventBridge `daily_report` rule, window 24h
  - **Weekly** at Monday 08:00 IST via the EventBridge `weekly_report` rule, window 168h
  - **Net effect:** the 6-hourly report and the daily report use IDENTICAL 24h windows — same DynamoDB Scan, same `build_report` output. The user would receive 4 content-identical emails per day plus the morning briefing plus the weekly digest = **~36 emails/week**, of which only ~8 carry distinct information (1/day daily + 1/week weekly). The rest is pure SES noise.
- **Why the blueprint had it this way:** STEPs 16 and 17 were spec'd independently. STEP 16's ASL puts `GenerateReport` as the workflow's final state ("scan and report end-to-end" — sensible for a manually-triggered ad-hoc run). STEP 17 schedules `report_generator` independently ("predictable morning inbox briefing"). Neither STEP resolved the overlap when both run on a cron. I didn't flag it in STEP 17; the user did before commit dust settled. Honest write-up belongs here.
- **Path chosen:** Daily + weekly EventBridge reports kept (different windows = different content). `GenerateReport` removed from the SFN. Scans now write findings silently to DynamoDB every 6 hours; reports are fetched out-of-band by the EventBridge-scheduled report Lambda. Result: **~8 emails/week, all carrying distinct content.**
- **Files modified:**
  - `terraform/modules/step-functions/main.tf` — removed `GenerateReport` Task state, set `End = true` on `ParallelScanners` instead of `Next = "GenerateReport"`. Shrunk inline policy `lambda_invoke`: dropped `var.report_generator_arn` from the `Resource` list (3 scanner ARNs only), renamed the Sid from `InvokeCloudGuardLambdas` → `InvokeCloudGuardScanners`. Header comment + `Comment` field on the ASL updated to reflect the new shape.
  - `terraform/modules/step-functions/variables.tf` — removed `report_generator_arn` variable (no longer referenced) and `report_retry_max_attempts` variable (no longer referenced). Left tombstone comments pointing back to STEP 17.5 so a future reader doesn't reintroduce them.
  - `terraform/environments/dev/main.tf` — removed `report_generator_arn = module.report_generator.function_arn` from the `module "step_functions"` call. Updated the module-call header comment to explain the new scan-only shape and why.
- **What stayed:**
  - Lambda module call for `report_generator` — still deployed because the daily + weekly EventBridge rules invoke it directly.
  - EventBridge module — untouched. `scan_schedule`, `daily_report`, `weekly_report` rules + their IAM/permission plumbing all stay.
  - `report_generator/handler.py` and `html_builder.py` — zero Python changes. The Lambda's contract is unchanged: read `report_window_hours` from event input, query DynamoDB, build HTML, archive to S3, SES email, SNS publish. Now it just gets called from EventBridge only.
  - The 106-test unit-test suite — still passes. Nothing in `tests/` references SFN orchestration; the report Lambda's input contract is unchanged.
- **Key decisions:**
  - **Path 3 (daily + weekly) over Path 1 (daily only).** Path 1 would have given 1 email/day for everything; Path 3 keeps a weekly digest that uses a 168h window (different content than the daily 24h window). User-confirmed preference: both daily and weekly are tolerable; the 6-hourly was the noise source. Path 3 also keeps the weekly rule's interview-talking-point value ("different cadence, different scope, same Lambda, EventBridge Input field selects the window").
  - **Removed the variable `report_generator_arn` entirely (not just marked deprecated).** Per CLAUDE.md ("no dead code, no backwards-compat hacks"). A tombstone comment preserves the audit trail; the actual variable is gone so a future reader can't accidentally pass it.
  - **Renamed the IAM Sid from `InvokeCloudGuardLambdas` → `InvokeCloudGuardScanners`.** The principle of least surprise: the Sid now reflects what the role actually does. A grep for "InvokeCloudGuardLambdas" returns the old name in PROGRESS.md only, so the move is loud.
  - **Did NOT touch report_generator's env vars or IAM role.** The Lambda is still deployed and still has all the perms it needs for the EventBridge-invoked path. Trimming the `SES_SENDER_EMAIL` / `REPORT_WINDOW_HOURS` env vars would be a separate cleanup and they're harmless to leave.
  - **`Plan: 57 to add` unchanged.** STEP 17.5 is content-only: the SFN state machine resource still exists, the lambda_invoke policy still exists, just with a smaller `definition` JSON and a smaller `Resource` list inside. EventBridge module untouched. The diff is a Terraform "change" if applied, not "add/destroy" — and since nothing has been applied yet (STEP 18 still pending), the plan count is identical.
- **`terraform plan` result:** ✅ `Plan: 57 to add, 0 to change, 0 to destroy.` — matches STEP 17 total. Verified the SFN policy no longer references `module.report_generator.function_arn` and the state machine's `definition` no longer contains `GenerateReport` (greps clean in plan output).
- **Interview Prep Note:** "Why doesn't your Step Functions workflow include the report step like every architecture diagram online suggests?" — Because the workflow is **schedule-driven, not on-demand**. A 6-hourly scan that ALSO emails a report produces 4 daily content-identical emails next to the one I actually want at 08:00 IST. The right separation is: SFN owns the scan (writes findings to DynamoDB → durable, queryable), EventBridge owns the report cadence (daily 24h + weekly 168h windows → meaningfully different content per rule). This makes the scan's failure semantics simpler (no fan-out to a noisy notification on every cron tick) and lets reports be re-run idempotently against historical data without re-running the scanners. Trade-off: I lose "fresh report immediately after a manual scan" — if I need that, I invoke `report_generator` directly from the Console or add a synchronous `.waitForTaskToken` step gated by the EventBridge `source` field. Also be ready to explain: (a) why `Plan: 57 to add` stayed flat despite the cleanup (content-only edit to existing resources), (b) why I kept the weekly rule (different window = different content; daily is a strict subset of weekly), (c) why this is a STEP 17.5 sub-step rather than a STEP 18 hotfix (caught BEFORE first apply — cleanest moment to fix).

### ✅ STEP 19 — Create the Lambda Packaging Script
*Completed: 2026-05-28 · Commit: `35283a3`*

- **Files written:**
  - `scripts/package_lambdas.sh` — bash, for STEP 21 CI on `ubuntu-latest`. Resolves the repo root from `$BASH_SOURCE` so cwd doesn't matter. For each of the 4 Lambdas: clean `src/<lambda>/build/`, copy `*.py`, copy `src/shared/`, strip `__pycache__/`.
  - `scripts/package_lambdas.ps1` — PowerShell, native Windows. Same contract as the bash script (the `bash sibling`-style header comment cross-references it). Uses `$MyInvocation.MyCommand.Path` for cwd-independent path resolution.
- **Files modified (dev wiring):**
  - `terraform/environments/dev/main.tf` — 4 `source_dir` lines switched from `src/<lambda>/` to `src/<lambda>/build/` (cost_scanner / security_scanner / resource_cleanup / report_generator). Updated the lambda-modules section header comment to call out the script-must-run-first contract.
- **Files NOT modified:**
  - `terraform/modules/lambda/main.tf` — no change. The module is parameterised on `source_dir`; STEP 19 is purely a caller change.
  - `.gitignore` — no change needed. Line 15 already had `build/` (Python setuptools convention) which matches at any depth, so `src/<lambda>/build/` is auto-ignored.
- **Build directory layout (verified for `cost_scanner`):**
  ```
  src/cost_scanner/build/
  ├── cost_analyzer.py
  ├── handler.py
  └── shared/
      ├── __init__.py
      ├── aws_helpers.py
      ├── dynamo_client.py
      └── notification.py
  ```
  At runtime AWS Lambda extracts the zip and Python sees `shared/` as a top-level package next to `handler.py`, so `from shared.dynamo_client import batch_put_findings` resolves cleanly.
- **File counts after a clean run:** cost_scanner 6, security_scanner 10, resource_cleanup 6, report_generator 6.
- **Key decisions:**
  - **Deviated from blueprint: skip `pip install -r requirements.txt`.** Blueprint STEP 19 bash explicitly installs deps into `package/`. CloudGuard's `requirements.txt` is `boto3` only on every Lambda, and boto3 ships in the Python 3.12 Lambda runtime. Vendoring it would (a) add ~15 MB per zip × 4 functions, (b) eat into the 50 MB zipped / 250 MB unzipped Lambda quotas, (c) slow cold starts (Lambda has to download/extract the bigger package). When a real third-party dep ever lands, insert `pip install --target build/ <pkg>` at the documented point in the script. Trade-off acknowledged; deviation is honest because the bundled boto3 version (`>=1.34` in Python 3.12 runtime as of 2026) covers every API CloudGuard uses.
  - **`build/` next to source (option A), not a separate `zips/` tree (option B).** Keeps Terraform's `archive_file` + `source_code_hash` drift detection intact: edit a `.py`, re-run the script, plan shows a code-update diff. Option B (script makes the zip, Terraform just uploads it) would have removed `archive_file` from the Lambda module and forced manual `source_code_hash = filebase64sha256(...)` wiring; cleaner-looking but loses the "Terraform notices the file changed" feedback loop.
  - **Strip `__pycache__/` AFTER copying, not via `--exclude` flags.** PowerShell 5.1's `Copy-Item -Exclude` only filters at the source-name level, not directory traversal — using it would have silently missed nested `__pycache__/`. The bash equivalent (`rsync --exclude`) is cleaner but adds an `rsync` dependency that GitHub Actions runners have but other CI environments may not. Copy-then-prune is the lowest-common-denominator pattern that works everywhere. Caught in-session: first dry-run produced 10 files for cost_scanner where 6 was expected — `__pycache__/` from local pytest runs got vendored.
  - **Two scripts (bash + PowerShell), not one polyglot or one with WSL invocation.** The user runs Windows PowerShell locally; CI runs on `ubuntu-latest` with bash. Forcing WSL on Windows or PowerShell-Core on Linux would add a tool dependency for zero functional benefit. The two scripts duplicate ~30 lines of `cp`/`rm` plumbing — acceptable for a script that's read once per environment.
  - **`build/` is a directory under the Lambda's source folder, not a flat top-level `build/` per Lambda.** Keeps each Lambda's deploy artifact co-located with its source — `cd src/cost_scanner && ls` shows the source AND the build output. The alternative (a single `build/cost_scanner/`, `build/security_scanner/`, …) would be slightly tidier but requires the Terraform `source_dir` paths to traverse upward — cosmetic call.
  - **Script resolves repo root from its own location, not from cwd.** Lets the user run it from anywhere — `.\scripts\package_lambdas.ps1` from the repo root, `..\scripts\package_lambdas.ps1` from `terraform/environments/dev`, or `bash /full/path/to/package_lambdas.sh` from a CI runner. The bash version uses `$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)`, the PowerShell version uses `$MyInvocation.MyCommand.Path` + `Resolve-Path`. Both are the canonical idioms.
- **`terraform plan` result:** ✅ `Plan: 57 to add, 0 to change, 0 to destroy.` — unchanged from STEP 17.5 (only `source_dir` paths changed and a `source_code_hash` is now driven by `build/` instead of the raw Lambda dir; resource count is identical since nothing has been applied yet).
- **What's NOT in this STEP:**
  - First `terraform apply` (STEP 18). The deployment path is now: (1) run packaging script, (2) `terraform plan` to inspect, (3) `terraform apply` to upload. STEP 18 is the next session.
  - CI integration of the script (STEP 21). The bash version is ready to drop into `.github/workflows/deploy.yml` as a `bash scripts/package_lambdas.sh` step before `terraform apply`.
- **Interview Prep Note:** "How does your Lambda code actually get into AWS?" — Three players. (1) `scripts/package_lambdas.{sh,ps1}` is the *vendoring* step — it pre-copies `src/shared/` into each Lambda's `build/` directory so the package is self-contained. No pip-install — boto3 is in the Lambda runtime. (2) Terraform's `archive_file` data source zips `build/` at plan time and computes a SHA256; that hash is wired to `aws_lambda_function.source_code_hash`. (3) `terraform apply` calls the AWS Lambda `UpdateFunctionCode` API with the zip bytes. So edit `.py` → run script → `terraform plan` shows source_code_hash diff → `terraform apply` uploads. The honest design call here is the `build/` indirection: it exists *only* because `archive_file` zips one directory and our Lambdas import from a sibling package. Be ready to defend skipping `pip install -r requirements.txt` (boto3 already in runtime, 15 MB×4 of dead bytes) — and to defend why we don't use a Lambda layer for `shared/` (one layer per Lambda is overkill at this scale, vendoring is simpler and the cold-start cost of a 4 KB module is unmeasurable).

### ✅ STEP 18 — Wire Everything Together in Dev Environment (FIRST `terraform apply`)
*Completed: 2026-05-28 · Commit: `c8d668d`*

- **The moment:** First time CloudGuard infrastructure exists in AWS. The plan that had been sitting at "57 to add, 0 to change, 0 to destroy" since STEP 17.5 is now applied — 57 managed resources live in account `810278669055`, region `ap-south-1`.
- **Procedure followed:**
  1. Re-ran `scripts/package_lambdas.ps1` to guarantee fresh `src/<lambda>/build/` artefacts (counts 6 / 10 / 6 / 6 as expected).
  2. `terraform init` — providers reused from lock file (`hashicorp/aws v5.100.0`, `hashicorp/archive v2.8.0`).
  3. `terraform plan -out=tfplan` — confirmed `Plan: 57 to add`.
  4. User confirmation: `apply`.
  5. `terraform apply` cycle: 5 invocations total. First 4 hit apply-time validation errors that `terraform plan` could not detect (the validators run server-side at create-time). After each error: hotfix → re-apply (Terraform resumed from partial state). Apply #5 finished clean.
- **Four real bugs caught at apply time (none surfaced in plan):**

  | # | Where | What | Why plan missed it | Fix |
  |---|---|---|---|---|
  | 1 | 5 × IAM Role `description` + 1 × KMS Key `description` + 2 × resource tag values + 1 × SFN ASL `Comment` | Unicode em-dash `—` (U+2014) used throughout the project's prose-style descriptions. AWS IAM enforces the regex `[	
 -~¡-ÿ]` (printable ASCII + tab/CR/LF + Latin-1 supplement). Em-dash is U+2014 → outside the allowed range. S3 / SNS / KMS apply similar but slightly different rules. | Plan-time validation is client-side only — the AWS provider does not run the IAM regex. | Replace ` — ` with ` - ` in 9 AWS-bound strings across IAM, KMS, S3, SNS, SFN modules. Terraform-local variable descriptions (the help text shown by `terraform plan`) are untouched — they never leave the user's machine. |
  | 2 | SNS topic `Purpose` tag value | `"...for cost, security, cleanup, report-generator notifications"` — multiple commas. SNS tag-value rules are stricter than S3 / IAM: SNS allows `[a-zA-Z0-9_.:/=+\-@\s]*`. Comma is not in the set. (KMS Key Description happily accepted commas in the same apply.) | Same as above — per-service tag-value validation is server-side. | Rewrite to use ` / ` separators instead of commas: `"...for cost / security / cleanup / report-generator notifications"`. |
  | 3 | All 4 Lambda functions, `reserved_concurrent_executions = 5` | Personal AWS accounts have a **total** Concurrent Executions quota of 10 (Service Quotas L-B99A9384, verified with `aws service-quotas get-service-quota`). The blueprint default of 5 reserved × 4 functions = 20 → would push `UnreservedConcurrentExecutions` to -10, but AWS enforces a floor of 10 on unreserved. | Plan-time has no view of account-level quotas. | Changed the Lambda module default to `reserved_concurrent_executions = -1` (no reservation) with an updated `description` explaining the new-account constraint. The account-wide quota of 10 itself becomes the runaway-bill cap. When the quota is raised (Service Quotas request → minutes for personal accounts, hours for enterprise), individual functions can opt-in to a per-function reservation by passing a positive value at the caller. |
  | 4 | SNS Topic Policy | Statement 1 used `actions = ["sns:*"]` for the root-account-admin grant. SNS rejects wildcards in resource-based topic policies because `sns:*` expands to include account-level actions (`sns:CreateTopic`, `sns:ListTopics`, etc.) that aren't topic-scoped. Error: `Policy statement action out of service scope!` | Plan-time produces valid JSON for `aws_iam_policy_document`; the action-scope check is SNS-server-side at SetTopicAttributes. | Enumerated the 13 valid topic-scoped actions explicitly (AddPermission, DeleteTopic, GetDataProtectionPolicy, GetTopicAttributes, ListSubscriptionsByTopic, ListTagsForResource, Publish, PutDataProtectionPolicy, RemovePermission, SetTopicAttributes, Subscribe, TagResource, UntagResource). Also restored `sns:Subscribe` in the DenyInsecureTransport statement — my first guess that Subscribe was invalid was wrong; the wildcard was the actual culprit. |

- **Files modified (the 4 hotfixes):**
  - `terraform/modules/iam/main.tf` — 4 role descriptions, em-dash → hyphen
  - `terraform/modules/kms/main.tf` — CMK description, em-dash → hyphen
  - `terraform/modules/s3/main.tf` — reports bucket `Purpose` tag, em-dash → hyphen
  - `terraform/modules/sns/main.tf` — topic `Purpose` tag (em-dash + commas → hyphen + slashes); topic policy `sns:*` enumerated; restored `sns:Subscribe` in DenyInsecureTransport with corrected comment
  - `terraform/modules/step-functions/main.tf` — SFN role description + ASL `Comment`, em-dash → hyphen
  - `terraform/modules/lambda/variables.tf` — `reserved_concurrent_executions` default 5 → -1, with full reasoning in the variable description
- **Files NOT modified:**
  - `terraform/environments/dev/main.tf` — already wired up across STEPs 4–17.5; nothing to change for STEP 18.
  - All Python source — unchanged.
  - All `variables.tf` HCL descriptions outside the Lambda module (they have em-dashes but never leave the user's machine).
- **`terraform state list` count:** 77 entries total = 57 managed resources + 20 data sources (`data.aws_caller_identity.current` × 4 modules, `data.aws_region.current` × 5 modules, `data.aws_iam_policy_document.*` × ~7, `data.archive_file.source` × 4). The 57/57 managed count matches the plan exactly. `terraform plan` after the apply yields `No changes`.
- **Post-apply verification (read-only, all green):**
  - 4 Lambdas live, all `python3.12`, memory/timeout matrix correct (256MB/300s for the 3 scanners, 512MB/600s for report_generator).
  - 3 DynamoDB tables present.
  - SNS topic KMS-wired, 1 subscription `PendingConfirmation` (expected — user needs to click email).
  - Step Functions `cloudguard-dev-workflow` is `ACTIVE`, type `STANDARD`, logging `ALL`, X-Ray on.
  - 3 EventBridge rules ENABLED (`cloudguard-dev-scan-schedule rate(6 hours)`, `cloudguard-dev-daily-report cron(30 2 * * ? *)`, `cloudguard-dev-weekly-report cron(30 2 ? * MON *)`).
  - SES has no verified identities yet — manual step still pending.
- **Key decisions (this STEP, not just the hotfixes):**
  - **No deferral on the em-dash fix even though comments would have worked.** The HCL `description` field on `aws_iam_role` is AWS-bound (sent to IAM, shown in the console). Moving the em-dashes to HCL `#` comments would have erased a real piece of human-readable metadata from AWS, which is the point of the description. Hyphen-space-hyphen reads almost identically and crosses every AWS validator.
  - **`reserved_concurrent_executions = -1` is a temporary regression of the safety rail, not a permanent one.** The STEP 9 decision (default 5) was correct for an account with a default 1000 unreserved pool; it fails specifically on accounts capped at 10. Documented loudly in the variable description so a future reader doesn't take the -1 as "concurrency caps are useless." When the user raises the quota in Service Quotas (one ticket, usually approved in minutes for personal accounts and hours for enterprise), the right move is to pass `reserved_concurrent_executions = 2` per scanner Lambda at the call site in `dev/main.tf` — restores the per-function cap without re-touching the module.
  - **The SNS `sns:*` rejection is the most interview-grade gotcha in this STEP.** Most engineers (myself included until 5 minutes ago) treat `sns:*` as obviously valid in any SNS policy. The actual SNS service makes a distinction between *topic-scoped* actions (which can be in a topic policy) and *account-scoped* actions (which cannot). `CreateTopic` and `ListTopics` are account-scoped — there's no specific topic to reference — so `sns:*` expanding to include them is a category error. AWS rejects the policy outright rather than silently dropping the unsupported actions.
  - **Five `terraform apply` invocations across one STEP is a lot, but each apply was a learning event.** Terraform's partial-state model handles this gracefully: the 9 resources from the first apply, plus all the new ones from applies #2–4, were correctly tracked across the resume cycle. Nothing had to be tainted, nothing imported. Each apply iterated forward by the resources it could create until the next blocker.
- **What broke vs what could have broken (and didn't):**
  - **Broke:** the 4 apply-time errors listed above.
  - **Didn't break:** S3 bucket creation (eventual-consistency window survived), KMS key 20-second creation latency, DynamoDB-with-CMK chained dependency (the table refused to create before the KMS key was active, but Terraform's implicit dependency graph handled the ordering), SNS topic policy creation took 1 minute (because of KMS-encrypted topic + IAM policy propagation), Step Functions state-machine creation waited correctly on all 3 IAM inline policies.
- **Open TODO (deferred from this STEP):**
  - Click the SNS confirmation email (user action — Terraform cannot self-confirm by AWS design).
  - Verify the SES identity in the ap-south-1 console (user action — would need an additional `aws_ses_email_identity` Terraform resource; punted for now because the verification email still needs a manual click).
  - Restore `reserved_concurrent_executions` at call sites once the account concurrency quota is raised (Service Quotas request → 1000 default for established accounts).
  - Scope `ses:SendEmail` to verified-identity ARN — still carried from STEP 8 / STEP 11 hardening pass. STEP 18 didn't touch this.
- **Interview Prep Note:** "What broke the first time you applied your Terraform plan?" — Four bugs the plan couldn't catch because they're server-side validations: (1) IAM Role descriptions used em-dashes (Unicode U+2014) and IAM only accepts Latin-1 (`[	
 -~¡-ÿ]*`); (2) SNS tag values reject commas where other services allow them (per-service tag-value rule drift); (3) the personal-account Lambda concurrency quota is 10, so any `reserved_concurrent_executions > 0` × 4 functions pushed unreserved below the AWS-enforced floor of 10; (4) SNS topic policies reject `sns:*` because the wildcard expands to include account-level actions (`CreateTopic`, `ListTopics`) that aren't topic-scoped — must enumerate the 13 topic-scoped actions explicitly. The honest meta-lesson: `terraform plan` validates schema and types but cannot model every AWS service's per-API constraints; `terraform apply` is still the moment of truth, and the right posture is to expect partial-state cleanup mid-deploy rather than treat a green plan as a green apply. Also be ready to explain (a) why Terraform's partial-state model made the iterative fix safe (each apply resumed from the last-good state, no `taint` or `state rm` needed), (b) why per-environment quota awareness matters in cloud governance work (CloudGuard is *exactly* the kind of tool whose own deployment hits the same quota limits it tries to detect — meta-irony noted), and (c) why I didn't switch to `reserved_concurrent_executions = 0` instead of `-1` (`= 0` means "throttle this function to zero" — bricks the function; `-1` means "no reservation, use the unreserved pool" — the correct AWS idiom).

### ✅ STEP 20 — Test the System End-to-End (first live invocation; 2 real bugs surfaced and resolved/logged)
*Completed: 2026-05-29 · Commit: `930b5ef`*

- **The moment:** First time Lambda code ran against real AWS resources. Two bugs surfaced that `terraform plan` and the 106-test unit suite both missed — exactly what STEP 20 is for.
- **Procedure followed:**
  1. Manually started SFN `cloudguard-dev-workflow` from the AWS Console (ap-south-1) with input `{}`.
  2. First execution: CostScanner + SecurityScanner branches → `AccessDeniedException` on `dynamodb:BatchWriteItem` → both went into `CostScanFailed` / `SecurityScanFailed` Catch-to-Pass states. ResourceCleanup branch → green (no zombies in personal account → `batch_put_findings([])` short-circuits, never touched DynamoDB).
  3. Diagnosed the IAM gap (see Bug #1 below), applied a 3-line fix in `terraform/modules/iam/main.tf`, re-ran `terraform plan` (`0 add / 3 change / 0 destroy`), `terraform apply` succeeded in <2 s (IAM-only — no Lambda repackage / re-zip / re-deploy needed).
  4. Re-ran SFN with `{}`: all 3 branches green. Duration breakdown: SecurityScanner 12.5 s (long pole; IAM `ListUsers` walk + per-bucket S3 introspection), CostScanner 2.7 s, ResourceCleanup 1.4 s.
  5. Invoked `cloudguard-dev-report-generator` manually from the Lambda Console with test event `{"report_window_hours": 24}` → succeeded → SES email landed in inbox at `abhithgowdaa@gmail.com` with "View full report" pre-signed URL button + parallel SNS notification email.
- **DynamoDB state after the successful execution (verified by `aws dynamodb scan ... --select COUNT`):**
  - `cloudguard-dev-findings`: 13 rows (12 security + 1 cost — see Bug #2)
  - `cloudguard-dev-cost-data`: 290 rows (30 days × ~20 services, idempotent overwrite on re-run per STEP 10 design)
  - `cloudguard-dev-remediation-log`: 0 rows (no zombies found = no SKIPPED_DRY_RUN records to write)
- **Real findings in the user's actual AWS account** (all flagged by security_scanner):
  - Security group `sg-0f815d19630ec7643` (default SG in `vpc-0105d0373b7b46575`) — **TCP port 8080 open to 0.0.0.0/0** → HIGH
  - S3 bucket `rentbook-bucket-hosting-2025` — versioning disabled → LOW
  - S3 bucket `iron-log-frontend-abhith` — versioning disabled → LOW
  - 9 more findings (sample-only verification — likely IAM users without MFA, additional SG ingress rules, unencrypted EBS — these surface in the SES report)
- **Two real bugs caught (both server-side validation that plan-time and unit tests cannot model):**

  | # | Where | What | Why plan + unit tests missed it | Fix |
  |---|---|---|---|---|
  | 1 | IAM module — 3 writer roles | `terraform/modules/iam/main.tf` granted `dynamodb:PutItem` only (blueprint STEP 4 spec exactly). Actual code path is `shared.dynamo_client.batch_put_findings` → `table.batch_writer()` → boto3 calls **`BatchWriteItem`** (distinct API action from `PutItem`; IAM does NOT imply one from the other). | Plan validates JSON syntax, not runtime API coverage. STEP 15b's 106 tests inject `Mock()` for the boto3 resource — never an AWS round-trip — so the IAM-evaluation gap never appeared. Drift between blueprint STEP 4 (written before STEP 14 introduced `shared.dynamo_client`) and the actual call site. | Added `"dynamodb:BatchWriteItem"` to 3 inline policy Sids: `DynamoDBWriteFindingsAndCostData` (cost_scanner, line 107-111), `DynamoDBWriteFindings` (security_scanner, line 202), `DynamoDBWriteFindingsAndRemediation` (resource_cleanup, line 267). `report_generator` is read-only (`Scan` + `Query`) so its policy was untouched. |
  | 2 | `src/cost_scanner/cost_analyzer.py` — `detect_anomalies` design gap | First successful run flagged a CRITICAL anomaly: **Amazon S3 service, actual_cost=$0, expected_cost=$0, ratio=73.876x ("7287.6% above prior-period average of $0.0").** The displayed dollar values are zero (rounding to 2 dp); the underlying Decimals are microscopic (e.g. baseline ≈ $0.000012, today ≈ $0.000888) → ratio of negligibles is mathematically large but operationally meaningless. | STEP 10 guard `if avg_cost <= 0: skip` catches the exact-zero baseline; it does NOT catch positive-but-microscopic baselines. STEP 15b's `test_cost_scanner.py` used clean synthetic values like `avg=100.0, today=200.0` — ratio math made sense, the noise edge case was never tested. Real-world tf-state bucket usage at fractions of a cent slipped through the design as documented. | **Not fixed in this STEP** — logged as a new Open TODO slotted to a STEP 21 hardening sub-task: add `MIN_ANOMALY_DOLLARS` env-var floor (default $1.00), skip findings where `actual_cost < MIN_ANOMALY_DOLLARS`, add a unit test for the microscopic-cost case to close the gap. One-line code change + one new test + one env var; deferred from this STEP because (a) STEP 20 is verification not new feature work, (b) CI pipeline (STEP 21) is the right slot for the matching unit test. |

- **Files modified in this STEP:**
  - `terraform/modules/iam/main.tf` — three Sids updated. `DynamoDBWriteFindingsAndCostData` Action list now `["dynamodb:PutItem", "dynamodb:BatchWriteItem", "dynamodb:Query"]`. `DynamoDBWriteFindings` and `DynamoDBWriteFindingsAndRemediation` Action lists now `["dynamodb:PutItem", "dynamodb:BatchWriteItem"]`.
- **Files NOT modified:**
  - All Python source — zero changes. Bug #2 (microscopic-cost false positive) deferred to STEP 21 sub-step.
  - `terraform/environments/dev/main.tf` — unchanged.
  - `tests/` — 106 tests still pass; new unit test for microscopic-cost noise is part of the deferred Bug #2 fix.
- **`terraform plan` / `apply` after the IAM hotfix:**
  - Plan diff: `0 to add, 3 to change, 0 to destroy`. Three `aws_iam_role_policy` updates in-place — exactly the cost_scanner / security_scanner / resource_cleanup policies. Verified by hand-reading the plan output (`+ "dynamodb:BatchWriteItem"` in each policy diff).
  - Apply: completed in <2 s. IAM inline policy propagation is sub-5-second; re-running SFN immediately after `apply complete!` produced the all-green run.
  - `terraform plan` after the apply: `No changes`. State + reality reconciled.
- **Key decisions:**
  - **Fix the IAM grant, NOT the Lambda code.** The natural alt-path was "rewrite `shared.dynamo_client.batch_put_findings` to loop with single `PutItem` calls so the existing IAM grant is sufficient." Rejected: (a) `BatchWriteItem` is the correct boto3 idiom — 25 items per call vs 25 round-trips, auto-retry for `UnprocessedItems`. Rewriting away from it would be tail-wagging-dog: making correct code worse to match a stale IAM grant. (b) The blueprint's STEP 4 IAM spec pre-dates STEP 14's shared-helpers introduction — the blueprint is the thing that's stale, not the code. (c) IAM-only fix means no Lambda repackage / archive_file re-hash / `terraform apply` on Lambda functions → 2-second apply vs 30-second redeploy cycle. Honest deviation from the literal blueprint spec.
  - **Defer Bug #2 (microscopic-cost false positive) to STEP 21, not patched in STEP 20.** STEP 20's purpose is "test end-to-end" — finding a bug satisfies the STEP, fixing every bug is not in scope. Rolling new code into STEP 20 would: (a) break the one-STEP-per-session rule, (b) blur the diff so a reviewer can't tell what was "verification" vs "new feature," (c) push the matching unit test into STEP 20 when STEP 21 (CI pipeline) is the more natural slot for "test was added because CI gap was discovered." The bug is now a slotted Open TODO with explicit acceptance criteria.
  - **Manually-invoked report_generator with `{"report_window_hours": 24}` (Path B from the STEP 20 plan), not "wait for the 02:30 UTC EventBridge rule" (Path A).** Same code path — EventBridge's `daily_report` rule passes the same input. Path B closes the STEP in one session vs waiting overnight, exercises the SES verification that was completed at the end of STEP 18, and means PROGRESS.md gets a single coherent entry. The EventBridge schedule will fire on its own cadence from tomorrow — that's a passive verification on top of the active one done here.
  - **Did NOT touch the Open SG (port 8080 to 0.0.0.0/0) finding in the user's account during this STEP.** CloudGuard's job is to surface, not to remediate without permission. Remediation is a separate human action (and would test STEP 12's two-gate model — a future session can use this SG as the live target for `auto_remediate=true` end-to-end testing). For STEP 20 purposes, "the scanner found a real exposure in a real account" is the durable signal.
  - **No update needed to STEP 17.5 architectural decision.** The Console-driven manual SFN execution behaved exactly as STEP 17.5 designed: scanners ran in parallel, findings landed in DynamoDB, no report email fired from the SFN execution itself (correct — `GenerateReport` was removed). The two emails the user received both came from the **separate** manual `report_generator` invocation that followed. The cadence-separation story from STEP 17.5's interview-prep note now has live evidence.
- **Open TODOs from this STEP:**
  - **NEW — slotted to STEP 21 hardening sub-step:** add `MIN_ANOMALY_DOLLARS` floor to `cost_analyzer.detect_anomalies` (default $1.00 — anomalies below this absolute dollar amount are skipped regardless of ratio). Add unit test `test_detect_anomalies_skips_microscopic_costs` covering the case `avg=0.00001, today=0.0009, ratio=90x, actual<floor → skipped`. One-line code + one env var (`MIN_ANOMALY_DOLLARS`) + one test. Closes the false-positive gap surfaced here.
  - **NEW — interview note (no action):** the IAM `PutItem` vs `BatchWriteItem` distinction is now a real talking-point asset, not just a hypothetical "I considered least-privilege."
  - Carried forward (unchanged): all hardening items from STEP 18.5 slot remain pending.
- **Definition-of-Done satisfaction (per blueprint STEP 20 checklist):**
  - ✅ SFN execution from `{}` succeeded after IAM hotfix.
  - ✅ All 3 scanners ran in parallel (12.5 s wall-clock, branch durations 2.7 / 12.5 / 1.4 s).
  - ✅ CloudWatch Logs verified — INIT_START → batch_put_findings line → REPORT line for each Lambda, no error stacks after the hotfix.
  - ✅ DynamoDB tables populated (13 findings + 290 cost-data rows; remediation-log empty correctly).
  - ✅ HTML report generated in S3 (path `reports/dev/2026/05/29/report-24h-...html`).
  - ✅ Email arrived (SES report + SNS notification, both at `abhithgowdaa@gmail.com`).
  - ✅ Failures debugged via CloudWatch Logs → IAM `BatchWriteItem` permission added → re-applied → verified.
- **Interview Prep Note:** "What broke the first time you ran your whole system end-to-end, and what did you learn from it?" — Two bugs, two different bug classes, both genuinely useful as interview material. **First**, an IAM permission gap: my Lambdas got `AccessDeniedException` on `dynamodb:BatchWriteItem` because the IAM module granted `dynamodb:PutItem` (per the blueprint spec) but the actual code path used boto3's `table.batch_writer()` context manager, which calls `BatchWriteItem` — a distinct IAM action that `PutItem` does NOT imply. `terraform plan` couldn't catch it (no runtime API view); 106 unit tests couldn't catch it (mocked boto3 — no real IAM evaluation). Fix was 3 lines of HCL, one `terraform apply`, sub-2-second propagation. Meta-lesson: **integration testing surfaces a class of bug that no amount of plan-time validation or mock-based unit testing can model — the IAM evaluator only runs at runtime against real boto3 traffic.** **Second**, a false-positive in cost anomaly detection: scanner flagged Amazon S3 as a CRITICAL anomaly with `actual=$0, expected=$0, ratio=73.876x`. Real values were microscopic Decimals (tf-state bucket usage at fractions of a cent), and my detector's `if avg_cost <= 0: skip` guard only catches *exact* zero baselines, not positive-but-negligible ones. Unit tests used clean synthetic values like `avg=$100, today=$200` so the noise edge case was never tested. Fix is a one-line minimum-absolute-cost floor (`MIN_ANOMALY_DOLLARS`) + a matching unit test, slotted to STEP 21's hardening pass. Meta-lesson: **clean synthetic test fixtures hide real-world edge cases that only show up against actual data; the right CI defense is to add the regression test the moment a production case surfaces it.** Also be ready to explain (a) why I fixed IAM rather than rewriting the code to single-PutItem-in-a-loop (BatchWriteItem is the correct API choice; rewriting away from it would be making correct code worse to match a stale spec), (b) why I deferred Bug #2 to STEP 21 rather than patching it in STEP 20 (verification STEP, not feature STEP — one STEP per session per CLAUDE.md rule), and (c) why every scanner branch in the SFN graph was green even after CostScanner / SecurityScanner failed in run #1 (Catch → Pass per branch from STEP 16 — a "Failed" Lambda invocation transitions to a "Succeeded" Pass state, so the SFN shows green checkmarks; the actual failure is in the Lambda's CloudWatch logs, not the SFN graph — which itself was a useful diagnostic moment).

### ⬜ STEP 18.5 — IAM Hardening Pass (`ses:FromAddress` condition + EC2 destructive-action tag condition) *(slotted from Open TODOs, 2026-05-28 — see Slotting Policy)*

### ✅ STEP 21 — Build the CI/CD Pipeline (with OIDC federation + Checkov hard-fail + STEP 20 Bug #2 fix)
*Completed: 2026-05-29 · Commit: `a135280`*

- **Scope:** CI workflow (`ci.yml`), Deploy workflow (`deploy.yml`), GitHub Actions OIDC trust into AWS, Checkov security scan with documented suppression list, AND the `MIN_ANOMALY_DOLLARS` cost-detector floor surfaced as the deferred STEP 20 Bug #2.
- **Files written:**
  - `src/cost_scanner/cost_analyzer.py` — `detect_anomalies` gained a `min_dollars` parameter (default `1.0`) that skips anomalies whose absolute spike-day cost falls below the floor. Closes the `actual=$0.0009, ratio=90x` false positive that surfaced in the first live run on a personal account.
  - `src/cost_scanner/handler.py` — reads new `MIN_ANOMALY_DOLLARS` env var (default `1.0`), passes it through to `detect_anomalies`. Float coercion at the boundary.
  - `tests/test_cost_scanner.py` — 3 new tests: `test_detect_anomalies_skips_microscopic_costs` (reproduces the STEP 20 bug), `test_custom_floor_allows_small_but_real_anomaly_through` (verifies the floor is a knob, not a hard veto), `test_floor_zero_disables_the_filter` (regression-protects callers that opt out). Full suite: `109 passed`.
  - `terraform/modules/github_oidc/main.tf` + `variables.tf` + `outputs.tf` — new module: 1 OIDC provider + 2 IAM roles (plan + deploy) + 1 inline policy + 2 managed-policy attachments.
  - `terraform/environments/dev/main.tf` — `MIN_ANOMALY_DOLLARS = "1.0"` added to `cost_scanner` Lambda's `environment_variables`; new `module "github_oidc"` block consuming `var.github_org / github_repo / state_bucket_name`.
  - `terraform/environments/dev/variables.tf` — 3 new variables with defaults: `github_org = "abhithcogni"`, `github_repo = "cloudguard"`, `state_bucket_name = "cloudguard-tf-state-abhithcogni"`.
  - `terraform/environments/dev/outputs.tf` — 3 new outputs: `github_oidc_provider_arn`, `github_plan_role_arn`, `github_deploy_role_arn`.
  - `.github/workflows/ci.yml` — full implementation. 4 jobs (`lint`, `test`, `checkov`, `terraform-plan`), OIDC into the plan role, PR-comment with plan diff, hard-fail on Checkov. Replaces the STEP 3 placeholder.
  - `.github/workflows/deploy.yml` — full implementation. Single `deploy-dev` job: tests → package_lambdas → plan→apply with saved plan file, OIDC into the deploy role, concurrency lock to prevent racing applies, plan artifact uploaded for 14-day audit trail.
  - `.checkov.yaml` — Checkov hard-fail config with **13 suppressions, each with a one-line justification** (and the documented STEP that captures the decision). Result locally: **217 passed, 0 failed**.
- **Files reformatted (pre-existing fmt drift that the new CI lint job would have failed):**
  - `terraform/modules/eventbridge/main.tf`, `terraform/modules/github_oidc/main.tf` — `terraform fmt -recursive` applied.
- **Files NOT touched (intentionally):**
  - `scripts/package_lambdas.sh` — already STEP-21-ready (runs on Linux runners in CI as-is).
  - `terraform/environments/dev/terraform.tfvars` — gitignored, untouched.
- **`pytest` result:** ✅ `109 passed in 7.86s` (was 106; +3 new tests).
- **`terraform plan` result:** ✅ `Plan: 6 to add, 1 to change, 0 to destroy.`
  - 6 add: 1 OIDC provider + 2 IAM roles + 1 inline policy + 2 managed-policy attachments
  - 1 change: cost_scanner Lambda env vars gain `MIN_ANOMALY_DOLLARS = "1.0"`
  - Additional outputs: 3 new `github_*` outputs
- **`terraform fmt -check -recursive`:** ✅ exit 0 (clean across the whole tree).
- **Checkov result:** ✅ `Passed checks: 217, Failed checks: 0, Skipped checks: 0` with the documented suppression list. 13 suppressions, each pointing at a documented STEP-decision.

- **Key decisions:**
  - **OIDC federation over long-lived `AWS_ACCESS_KEY_ID` secrets (deviation from blueprint).** Blueprint literally lists static keys; OIDC has been GA since 2021 and is the answer a 12 LPA interviewer expects in 2026. With OIDC: no long-lived secret in GitHub, credentials live ~1 hour, AWS CloudTrail sees the GitHub workflow context (`repo:abhithcogni/cloudguard:ref:refs/heads/main`), and the role's trust policy `sub` condition pins assumption to a specific branch — a malicious PR cannot escalate to deploy even if it edits the workflow file. Trade-off: 30 extra lines of Terraform (provider + 2 roles + trust docs) vs zero-key-rotation forever. Interview talking-point asset.
  - **Two roles, not one** — plan role (any branch/PR, ReadOnlyAccess + state-bucket write) vs deploy role (only `refs/heads/main`, AdministratorAccess). The branch-scoped trust policy IS the security boundary. ReadOnlyAccess alone is insufficient for plan because Terraform's S3 native lock writes a `.tflock` object — added 4 explicit state-bucket actions (`GetObject`, `PutObject`, `DeleteObject`, `ListBucket`).
  - **AdministratorAccess on deploy role with documented TODO** to scope down. Tightening to a CloudGuard-services-only policy is itself a multi-hour exercise (IAM, KMS, DynamoDB, S3, SNS, Lambda, States, Events, Logs, + EC2/RDS/Config describes for the Lambda roles to re-read on plan). Defensible because the trust policy condition prevents PR-level escalation. Hardening tracked.
  - **Checkov hard-fail + suppression list with per-entry justification (Option C from the decision conversation).** Soft-fail = theatrical. Hard-fail-on-everything = will trip on opinionated checks (recursive logs-bucket logging) and get disabled in frustration. The suppression list = documented audit trail of security decisions. Every entry cites the STEP that recorded the decision, and additions require PR review.
  - **Branch-scoped concurrency on deploy.yml (`cancel-in-progress: false`)** — cancelling mid-`terraform apply` leaves state in a half-applied state that needs manual `terraform force-unlock`. Queue, don't cancel.
  - **Plan-then-apply with saved plan file in `deploy.yml`** — apply uses the exact plan output instead of re-planning, eliminating the race between plan and apply (e.g. another commit landing on main between the two steps). Plan file uploaded as a 14-day GitHub artifact for audit.
  - **`environment: dev` on the deploy job** — wires this into the GitHub environment system, which lets you (later) add required-reviewer approval gates without code changes. Free future-proofing.
  - **Two GitHub thumbprints in the OIDC provider** — `6938fd4d…` and `1c58a3a8…`. Since 2023 IAM validates the IdP against built-in root CAs and the thumbprint is cosmetic, but the argument is still required by AWS provider; two values give resilience if either rotates.
  - **`MIN_ANOMALY_DOLLARS` as an env var + parameter (not a hardcoded constant)** — environments scale differently. Dev's $1 floor is too high for a high-spend prod account; prod might want $50 + a stricter ratio. The parameter is the knob, the env var is the prod override path, the constant is the safe default.
  - **`min_dollars=0` is a documented escape hatch** — tests opt out of the floor, and the regression test for that path makes the escape explicit in the contract.
  - **Pre-existing `terraform fmt` drift was fixed** even though it was outside the STEP scope — the new CI `lint` job runs `fmt -check -recursive` and would have failed CI on the first PR otherwise. Scope creep was unavoidable.

- **Manual setup required AFTER this STEP's `terraform apply` (TODO list for the user):**
  1. Run `terraform apply` in `terraform/environments/dev/` to create the 6 OIDC resources.
  2. `terraform output github_plan_role_arn` and `terraform output github_deploy_role_arn` — copy both values.
  3. In GitHub: Repo Settings → Secrets and variables → Actions → **Variables tab** (not Secrets):
     - `AWS_PLAN_ROLE_ARN` = (from output)
     - `AWS_DEPLOY_ROLE_ARN` = (from output)
     - `BUCKET_SUFFIX` = `abhithcogni` (matches the gitignored tfvars)
     - `ALERT_EMAIL` = `abhithgowdaa@gmail.com`
  4. Optionally create a "dev" GitHub environment (Repo Settings → Environments) — required-reviewers stay empty for now; the `deploy.yml` already references `environment: dev`.
  5. Open a no-op PR to verify CI runs green (`lint`, `test`, `checkov`, `terraform-plan` all pass). Merge to main to verify deploy runs.

- **Open TODOs (carried + new):**
  - **NEW:** Tighten `github_deploy` role from AdministratorAccess to a service-scoped custom policy (IAM, KMS, DynamoDB, S3, SNS, Lambda, States, Events, Logs + Describe perms used by Lambda roles).
  - **NEW:** Account-global gotcha: if/when prod lives in the SAME AWS account, the `aws_iam_openid_connect_provider` must be moved to a shared bootstrap state or be looked up via `data` in prod's state — otherwise prod apply will fail with "provider already exists". Recommended: separate AWS accounts per environment.
  - **NEW:** Re-triage `.checkov.yaml` after every module change. The current 13 suppressions match Checkov 3.2.530 on the 2026-05-29 codebase.
  - Carried (unchanged): all hardening items from STEP 18.5 slot.

- **STEP 20 Bug #2 — closed:** the `MIN_ANOMALY_DOLLARS` floor + 3 regression tests landed here as planned. The microscopic-cost false-positive surfaced in DynamoDB on the first live run is now suppressed by default and overridable by env var. The next scheduled scan (or a manual SFN execution) will write **0 cost findings** from microscopic spend (vs the 1 false-positive cost row currently in the findings table). The existing 1 stale row will be auto-cleaned by the 90-day TTL.

- **Test-before-declaring-done checklist:**
  - ✅ `pytest tests/ -v` — 109 passed, 0 failed.
  - ✅ `terraform fmt -check -recursive terraform/` — clean.
  - ✅ `terraform validate` in `dev/` — clean.
  - ✅ `terraform plan` in `dev/` — exactly 6 add + 1 change, no surprises.
  - ✅ `python -m checkov.main --config-file .checkov.yaml` — 217 passed, 0 failed, 13 suppressions all justified.
  - ⏭ `terraform apply` — gated on user confirmation per CLAUDE.md rule #6.
  - ⏭ Live CI run on a PR — gated on the manual setup steps above.

- **🔧 STEP 21 hotfix (2026-05-30, commit `a040a15`):** First end-to-end test of the deploy workflow surfaced two real bugs. Both fixed in one apply cycle, second deploy (CI-driven, commit `ffa89185` on main) was fully green. Documented because they're high-value interview material.
  - **Bug 1 — OIDC AssumeRole denied with `Not authorized to perform sts:AssumeRoleWithWebIdentity`.** Root cause: when a GitHub Actions workflow declares `environment: <name>`, GitHub mints the OIDC token with `sub = repo:<org>/<repo>:environment:<name>` — NOT the ref-based `sub = repo:<org>/<repo>:ref:refs/heads/<branch>`. My deploy role's trust policy expected the ref-based format. Two formats never matched.
    - **Fix:** rewrote `deploy_assume_role` data source to require `sub = repo:abhithcogni/cloudguard:environment:dev` AND a separate StringEquals on the `ref` claim = `refs/heads/main` (defense in depth — even a misconfigured GitHub Environment cannot deploy from a non-main branch). Three conditions total now (aud + sub + ref).
    - **Interview talking-point:** "GitHub OIDC token claims change shape when you opt into an Environment. Trust policy has to be designed around the actual JWT, not the documentation example."
  - **Bug 2 — Plan refresh reported phantom `+ variables = {...}` drift on all 4 Lambdas; would have hit `AccessDenied` on apply too.** Root cause: Lambda env vars encrypted with the shared CMK. Plan refresh calls `GetFunction`, which needs `kms:Decrypt` to return env-var values. The KMS key policy from STEP 6 granted decrypt to the 4 Lambda execution roles only — the `github_plan_role` and `github_deploy_role` were NOT listed. When the SDK couldn't decrypt, it silently returned empty variables. Terraform saw empty refresh + non-empty config → diff shows everything as "needs adding". Apply would have failed on `kms:GenerateDataKey` (AdministratorAccess on the deploy role does NOT override KMS key policies — KMS is special).
    - **Fix:** added `AllowGitHubActionsViaLambda` Sid to the KMS key policy, granting Encrypt/Decrypt/ReEncrypt*/GenerateDataKey*/DescribeKey to both github roles, scoped via `kms:ViaService = lambda.<region>.amazonaws.com`. New module input `github_actions_role_arns` (defaults to empty list — only the dev env needs to populate it).
    - **Interview talking-point:** "IAM AdministratorAccess does NOT grant access to a CMK whose key policy doesn't list the principal — KMS is the one AWS service where the resource policy is the primary authorization, not a supplement. Anyone running CI/CD touching CMK-encrypted resources needs explicit grants in the key policy."
  - **Validating deploy run (full log archived):** OIDC assume succeeded → terraform plan showed only the expected 4 Lambda `~ source_code_hash` changes (no env-var drift) → terraform apply complete in 28 seconds → plan artifact uploaded for 14-day audit. Total job time ~85 seconds end-to-end.

- **Cosmetic CI warning (not breaking, future cleanup):** GitHub deprecation notice — `actions/checkout@v4`, `setup-python@v5`, `configure-aws-credentials@v4`, `setup-terraform@v3`, `upload-artifact@v4` all run on Node.js 20. GitHub flips the default to Node.js 24 on 2026-06-16. All 5 actions have Node 24 support coming; workflows will continue running without changes. Track as a "STEP 21.5 maintenance" item to bump action versions when upgrades land.

- **Interview Prep Note:** "Walk me through how your CI/CD authenticates to AWS." — Answer the OIDC story specifically: GitHub's OIDC identity provider is registered with AWS (`aws_iam_openid_connect_provider`), the deploy role's trust policy uses three conditions — `StringEquals` on the audience claim (`sts.amazonaws.com`, prevents replay of tokens minted for other audiences), `StringEquals` on the subject claim pinned to `repo:abhithcogni/cloudguard:environment:dev` (so only workflows opting into the dev GitHub Environment can assume — PRs cannot opt into environments), and `StringEquals` on the `ref` claim `refs/heads/main` (defense in depth — even a misconfigured environment can't deploy from a feature branch). GitHub mints a short-lived JWT at workflow start; `aws-actions/configure-aws-credentials` calls STS `AssumeRoleWithWebIdentity` with that token; AWS validates the signature against GitHub's published JWKS, evaluates the trust policy's conditions, and returns ~1-hour temporary credentials. **No `AWS_ACCESS_KEY_ID` lives in GitHub secrets, no rotation chore, no blast radius if a developer's laptop is compromised**. The plan role uses the same mechanism with a looser `sub` condition (`repo:.../:*` — any branch/PR) and a tighter permission set (ReadOnlyAccess + state-bucket write). Follow-up answers ready: (a) why two roles — separation of concern: PRs can never escalate to apply; (b) why hardcode the thumbprints when AWS validates against root CAs now — the argument is still required by the AWS provider and two values give resilience to future cert rotation; (c) why `cancel-in-progress: false` on deploy concurrency — cancelling mid-`apply` leaves Terraform state needing manual `force-unlock`; (d) why Checkov is hard-fail with a suppression list rather than soft-fail — the suppression list IS the security audit trail and forces every silenced check to be justified in code review; (e) plan-then-apply with saved plan file in deploy.yml eliminates the plan-vs-apply race when a second commit lands on main mid-job.


### ✅ STEP 21.5 — Finding Deduplication via Deterministic finding_id + Upsert
*Completed: 2026-05-31 · Commit: `8cafb89` (on `ci-smoke-test` branch)*

- **The trigger (user-spotted, 2026-05-31):** Opening the 2026-05-31 daily SES report showed **50 findings spread across 14 CRITICAL / 16 HIGH / 4 MEDIUM / 16 LOW** — but only ~14 distinct real-world issues existed in the account. The 6-hourly security scanner was writing a brand-new uuid finding for the SAME `sg-0aa84cbcd78d0b0f3 :22 → 0.0.0.0/0` every 6 hours, so a 24h report's `Scan + FilterExpression on timestamp >= cutoff` picked up ~4 copies of every active finding. Same pattern across IAM users, EBS volumes, and S3 buckets.
- **Path chosen (Option A per session decision):** refactor to idempotent upsert with a deterministic `finding_id`. Modelled on AWS Security Hub's `FirstObservedAt` / `LastObservedAt` fields. Same real-world issue → same PK → same row, with `last_seen` and `occurrence_count` bumped on every re-scan. Alternative considered: report-time dedup only (group by category+resource+check in `_scan_with_filter`) — rejected: faster to ship but hides the data inflation, table still grows 4×, weaker interview story.
- **Files written:**
  - `scripts/cleanup_old_findings.py` — argparse one-shot. `--table <name>` defaults to dry-run; `--confirm` actually deletes via `table.batch_writer().delete_item(Key={finding_id, timestamp})`. Run once after deploy so the existing uuid-keyed rows don't linger beside the new deterministic-keyed ones.
- **Files modified:**
  - `src/shared/dynamo_client.py` — new public API: `compute_finding_id(category, resource_id, check_name)` (SHA-256 of pipe-delimited inputs, truncated to 32 hex chars / 128 bits), `upsert_finding(table_name, finding)` (Query by PK → UpdateItem on existing SK / PutItem with `first_seen=last_seen=now` + `occurrence_count=1` on miss), `batch_upsert_findings(table_name, findings)` (sequential loop returning `{inserted, updated, total}`). Module docstring expanded to document the two write paths (append vs upsert).
  - `src/cost_scanner/cost_analyzer.py` — `store_findings` switched from `batch_put_findings` to `batch_upsert_findings`; `finding_id` from `uuid.uuid4()` → `compute_finding_id("cost", anomaly["service"], "cost_anomaly_30d_baseline")`. `uuid` import removed. `store_cost_data` unchanged (cost-data table is naturally idempotent on its own PK=date / SK=service composite).
  - `src/security_scanner/handler.py` — `_stamp_findings` uses `compute_finding_id(f["category"], f["resource_id"], f["check_name"])`. `lambda_handler` returns `total_findings` / `new_findings` / `updated_findings` instead of just a write count. `uuid` import removed.
  - `src/resource_cleanup/handler.py` — same shape as security_scanner for the findings write. **Remediation log keeps uuid + `batch_put_findings`** — each remediation attempt IS a discrete event with its own audit row, so append-only is correct.
  - `src/report_generator/handler.py` — findings `_scan_with_filter` switched from `Attr("timestamp").gte(cutoff_iso)` to `Attr("last_seen").gte(cutoff_iso) | Attr("timestamp").gte(cutoff_iso)`. The OR clause is a backward-compat seam for the ~50 old uuid rows that don't have a `last_seen` attribute — they're picked up via `timestamp` until the cleanup script deletes them. Remediation_log filter unchanged.
  - `src/report_generator/html_builder.py` — `_security_section` row template adds `<br>Seen N times since YYYY-MM-DD` when `occurrence_count > 1`. Single-sight findings (count == 1) don't get the noisy caption. `first_seen` falls back to `timestamp` for the date display (handles pre-21.5 rows).
  - `terraform/modules/iam/main.tf` — added `dynamodb:UpdateItem` to all 3 writer roles (cost_scanner / security_scanner / resource_cleanup) and `dynamodb:Query` to the 2 that didn't have it (cost_scanner already had Query from STEP 20). Tombstone comments cite STEP 21.5 so future readers see the upsert origin.
  - `tests/test_shared.py` — +9 tests across `TestComputeFindingId` (4 — deterministic, distinct inputs distinct ids, 32-char hex, delimiter-vs-concat collision) and `TestUpsertFinding` / `TestBatchUpsertFindings` (5 — first-sight PutItem, re-sight UpdateItem preserving SK, missing-count defaults to 2, empty-list no-op, mixed insert+update counts).
  - `tests/test_cost_scanner.py` — uuid-length assertion (36 chars) → 32-char hex hash assertion; added `test_finding_id_is_deterministic_across_runs`; mock return values switched from `int` to `{inserted, updated, total}` dict.
  - `tests/test_report_generator.py` — +2 tests: `test_occurrence_count_rendered_when_greater_than_one` (verifies the "Seen N times since DATE" caption), `test_occurrence_count_one_not_rendered` (no noisy caption on first-sight findings).
- **`pytest tests/ -v` result:** ✅ **121 passed, 0 failed in 6.75 s.** (was 109; +12 from this STEP.)
- **`terraform plan` result:** ✅ `Plan: 0 to add, 7 to change, 0 to destroy.`
  - 3 × `aws_iam_role_policy` updates (UpdateItem + Query grants)
  - 4 × `aws_lambda_function` updates (new `source_code_hash` from updated `build/` content — `shared/dynamo_client.py` + each handler)
- **Schema choice — why upsert over destroy-and-recreate:**
  - Findings table is `PK=finding_id, SK=timestamp` (STEP 5). Dropping SK requires `terraform destroy` + recreate, which loses GSIs (`severity-index`, `category-index`) and ties Terraform up for several minutes.
  - Upsert pattern works with the existing schema by **rewriting the SK semantic**: `timestamp` is now the immutable `first_seen` SK, set once on first-sight; the mutable "is this still active" timestamp lives in the separate `last_seen` attribute.
  - Trade-off: extra round trip per write (1 Query → 1 UpdateItem-or-PutItem instead of 1 batched write). At ~50 findings × 4 scans/day = 200 ops/day, cost is rounding error vs the schema-change pain.
- **Key decisions:**
  - **Deterministic finding_id = SHA-256(category | resource_id | check_name)[:32].** 32 hex chars = 128 bits — collision-resistant for the small finding population CloudGuard generates (would need ~2^64 distinct findings before a collision is plausible). Pipe delimiter avoids the `("foo", "bar", "baz")` vs `("foo", "barbaz", "")` concatenation-ambiguity bug — proven by `test_delimiter_avoids_concat_ambiguity`.
  - **Check_name carries an "intra-resource discriminator" suffix.** First post-fix verification scan caught a real correctness bug: `sg-0f815d19630ec7643` had two open ingress rules (port 22 AND port 8080), each producing a finding with `check_name = "sg_open_to_world"` — identical `(category, resource_id, check_name)` triple → identical finding_id → second finding's `UpdateItem` silently overwrote the first's severity/description. Fixed by promoting the discriminator into `check_name` itself: `sg_open_to_world:tcp:22-22` vs `sg_open_to_world:tcp:8080-8080` produce distinct IDs. Same pattern applied to `iam_checker` (`iam_access_key_old:{key_id}` / `iam_access_key_unused:{key_id}`) for users with multiple access keys. Other checkers (s3, ebs, config, zombie_finder) are naturally unique per `(resource_id, check_name)` and need no discriminator. **Meta-lesson:** the dedup primary key isn't (resource, check) — it's (resource, *what specifically went wrong with this resource*). Caught BEFORE the user saw a broken report by running an `aws dynamodb scan` immediately after the first repopulation. **Live verification is the only way to catch this class of bug** — unit tests would pass because each checker produces correct individual findings; the bug only manifests at the dedup boundary.
  - **UpdateItem with EXISTING SK, not the new scan time.** First-sight write puts `now` as SK; every subsequent re-sight does `UpdateItem` keyed by `(finding_id, existing_SK)`. The SK is therefore the immutable `first_seen` — semantically correct, and means **`Attr("first_seen")` and `Attr("timestamp")` are interchangeable** on a STEP-21.5-era row (the OR clause in the report filter exists for **pre**-STEP-21.5 rows that don't have `first_seen` yet).
  - **`occurrence_count` bumps from `existing.get("occurrence_count", 1) + 1`.** Pre-21.5 uuid rows have no `occurrence_count` field; defaulting to 1 + incrementing means the first re-sight on a legacy row records "2". After the cleanup script + 1 full scan, every row has the field populated correctly.
  - **`UpdateItem` ALSO bumps `severity` and `description`.** A finding that was MEDIUM yesterday and gets reclassified CRITICAL today (e.g. the resource was tagged in a way that triggers a more-severe rule path) must reflect the new severity in the report. Without the bump, the report would show stale severity from the first-sight write. Same logic for `description` (which encodes the actual ratio/count at scan time).
  - **`Attr("last_seen") | Attr("timestamp")` OR clause in the report.** Two seams to mind: (a) old uuid-keyed rows don't have `last_seen`, and (b) DynamoDB `Scan` with `FilterExpression` returns nothing for items missing a referenced attribute — without the OR, those rows disappear from the report before the cleanup script runs. After the cleanup script removes them, this OR becomes vestigial; can be simplified to plain `Attr("last_seen").gte(cutoff_iso)` in a future STEP.
  - **Cost-data + remediation-log tables explicitly untouched.** Cost-data PK=date/SK=service is already idempotent. Remediation-log PK=remediation_id (uuid) is correct — each remediation attempt is a discrete audit event, not a recurring observation. The session decision to scope the refactor narrowly is documented here.
  - **`scripts/cleanup_old_findings.py` ships with `--confirm` gated.** Dry-run by default mirrors the same safety pattern as the auto-remediate two-gate from STEP 12. The script does NOT take a destination table — it requires explicit `--table cloudguard-dev-findings` so it's impossible to accidentally purge cost-data or remediation-log.
  - **Report shows "Seen N times since YYYY-MM-DD" only when `occurrence_count > 1`.** Caption is signal, not noise — a first-sight finding doesn't need a "Seen 1 times" stub. Caption uses `first_seen` (or falls back to `timestamp` for legacy rows) sliced to YYYY-MM-DD — full ISO timestamp is too long for the row.
  - **Did NOT add a 4th GSI on `last_seen`.** Same reasoning as STEP 13 (`Scan + FilterExpression` is O(table size) but acceptable at dev volume). Filtering on a non-indexed attribute via `Scan + Filter` is correct here; flip to a GSI Query when findings volume crosses ~10k.
- **Open TODOs (carried + new):**
  - **NEW:** Simplify the report's `Attr("last_seen") | Attr("timestamp")` OR clause to plain `Attr("last_seen").gte(cutoff_iso)` once the cleanup script has run and all legacy uuid rows are gone. Two-line change; deferred to keep this STEP's diff focused.
  - **NEW:** Backfill `first_seen` / `last_seen` / `occurrence_count` on any legacy uuid rows that escape the cleanup (none expected, but worth a one-line `aws dynamodb scan` audit after deploy). If any survive: re-run the cleanup script.
  - Carried (unchanged): all hardening items from STEP 18.5 slot (`ses:FromAddress`, `ec2:ResourceTag/AutoCleanup` Condition).
- **Manual setup required AFTER `terraform apply`:** *(all completed in-session 2026-05-31)*
  1. ✅ Ran `python scripts/cleanup_old_findings.py --table cloudguard-dev-findings --confirm` → **103 legacy uuid rows deleted** (more than estimated — the scanner had ticked 8+ times since the last PROGRESS update added rows).
  2. ✅ Triggered manual SFN execution from CLI (`aws stepfunctions start-execution`) → succeeded in ~13 s. Findings table repopulated.
  3. ✅ First post-apply scan wrote **11 rows** — but uncovered a SECOND bug: `sg-0f815d19630ec7643` had BOTH port 22 (CRITICAL) AND port 8080 (HIGH) flagged separately by the checker, but they collapsed into ONE row because the hash key was `(category, resource_id, check_name)` with `check_name = "sg_open_to_world"` — same value for both rules. The second rule's `UpdateItem` silently overwrote the first's severity/description.
  4. ✅ Fixed in-session: `sg_checker.py` now uses `check_name = f"sg_open_to_world:{proto}:{port_label}"` and `iam_checker.py` uses `check_name = f"iam_access_key_old:{key_id}"` / `iam_access_key_unused:{key_id}` — the port/protocol/key_id is the discriminator that makes the (resource, check) hash key truly unique. Repackaged, applied, re-cleanup'd the 11 stale rows, re-scanned.
  5. ✅ Second post-apply scan wrote **12 rows** (the missing port 8080 finding is back as its own row).
  6. ✅ Ran a third scan to test idempotency: **all 12 rows now have `occurrence_count: 2`** — proves the upsert path updates existing rows instead of inserting duplicates.
  7. ✅ Invoked `report_generator` with `{"report_window_hours": 24}` → `{"findings_count": 12, "email_sent": true}`. SES email in inbox. **50 noisy findings → 12 actionable findings, zero information lost.**
- **Final dedup tally — proof of the fix:**
  - Old report (2026-05-31 daily): 14 CRITICAL + 16 HIGH + 4 MEDIUM + 16 LOW = **50 findings**
  - New report (2026-05-31 post-fix manual invoke): 3 CRITICAL (3 SGs port 22) + 4 HIGH (1 EBS unencrypted + 2 IAM + 1 SG port 8080) + 1 MEDIUM (IAM access key) + 4 LOW (4 buckets) = **12 findings**
  - **76% reduction in row count, 100% retention of distinct real-world issues** (all the security exposures from the old report are still present — just no longer duplicated 4× per scan window).
- **Interview Prep Note:** "Tell me about a bug you found and fixed yourself, end-to-end." — When I opened my CloudGuard daily SES report I noticed 50 findings against an account I knew had ~14 real issues. The scanner runs every 6 hours and was generating a new `uuid` finding_id every run, so a 24h window picked up ~4 copies of every active finding. I refactored to a deterministic finding_id (SHA-256 of `category|resource_id|check_name`, 32-char hex, with a pipe delimiter to avoid concat-ambiguity) and an idempotent upsert path — `Query` by PK → `UpdateItem` on hit / `PutItem` on miss — modelled on AWS Security Hub's `FirstObservedAt` / `LastObservedAt`. The SK semantic shifted: `timestamp` is now the immutable `first_seen` value (set once on insert), and `last_seen` is the mutable "is this still active" attribute the report filters on. Reports now show `"sg-xxx open on port 22 — Seen 12 times since 2026-05-29"` — actionable instead of noisy. Trade-off: one extra `Query` per write instead of a batched `BatchWriteItem` — at ~50 findings × 4 scans/day = 200 ops/day, the cost is a rounding error against the alternative pain (destroy-and-recreate the table to drop the SK). Be ready to explain: (a) why I didn't change the DynamoDB schema (preserving the GSIs from STEP 5 is worth the upsert indirection); (b) why the report has an `Attr("last_seen") | Attr("timestamp")` OR clause — backward compat for the ~50 legacy rows that exist until the cleanup script removes them; (c) why `occurrence_count` increments even when no field exists yet — graceful migration without backfill; (d) why `UpdateItem` also bumps `severity` and `description` — a finding can reclassify between scans (e.g. a tag change shifts the rule path); (e) the cleanup script's two-gate safety (`--confirm` required) and why dry-run output is the default — matches the same "explicit opt-in for destruction" pattern as the cleanup Lambda's two-gate auto-remediate.


### ⏭️ STEP 22 — Add CloudWatch Dashboard *(NEXT)*
### ⬜ STEP 22.5 — AWS Config Terraform Module *(slotted from Open TODOs, 2026-05-25 — see Slotting Policy)*
### ⬜ STEP 23 — Write Documentation *(+ README "Known Limitations" section for the 4 dropped trigger-conditional items)*
### ⬜ STEP 24 — Add Resource Tagging Strategy
### ⬜ STEP 25 — *(post-DoD stretch)* Human-in-the-loop remediation with `.waitForTaskToken`

**Legend:** ✅ done · ⏭️ up next · 🛑 blocked · ⬜ not started · ⭐ stretch (post-Definition-of-Done)

---

## Decision Log

| Date | Decision | Reasoning | Alternative considered |
|------|----------|-----------|------------------------|
| 2026-05-14 | Separate folders per env (`dev/`, `prod/`) over Terraform workspaces | Impossible to accidentally apply prod; full config isolation per env | Workspaces — better for ephemeral/PR envs, not stable long-lived envs |
| 2026-05-14 | `terraform.tfvars.example` committed, `terraform.tfvars` gitignored | Prevents secrets in git; example documents required vars | Committing tfvars — rejected: leaks emails, bucket names, account context |
| 2026-05-14 | Python 3.13.7 instead of 3.12 | 3.12 unavailable; 3.12 is Lambda runtime — risk accepted, will catch in tests | Install 3.12 alongside 3.13 — unavailable on Cognizant machine |
| 2026-05-14 | S3 native locking (`use_lockfile = true`) instead of DynamoDB | Terraform >= 1.10 supports it natively; removes a dependency; user on 1.14.6 | DynamoDB lock table — still valid, gives visible lock state, but unnecessary overhead |
| 2026-05-14 | SSE-S3 (AES256) for state bucket, not SSE-KMS | Free, zero config; KMS adds audit trail but costs money — overkill for personal project | SSE-KMS — better for company accounts needing audit trails of state access |
| 2026-05-14 | Commit `.terraform.lock.hcl` | Terraform official recommendation; pins provider versions for reproducible inits | Gitignore it — rejected: then provider version can drift between machines |
| 2026-05-14 | Inline policies per role (not customer-managed) | Each policy is unique to one role; inline makes ownership obvious and auto-deletes with role | Customer-managed — only better if shared across roles |
| 2026-05-14 | Attach `AWSLambdaBasicExecutionRole` for Logs perms | AWS-blessed pattern, don't reinvent the wheel | Custom inline Logs policy — works but adds maintenance |
| 2026-05-14 | Build DynamoDB/SNS/S3 ARNs in IAM via naming convention (resources don't exist yet) | Tighter than `Resource = "*"`; deterministic ARN format; ARNs validated at apply not plan | Pass real ARNs as inputs — better long term but creates ordering complexity; revisit if drift |
| 2026-05-14 | `ec2:DeleteVolume`/`ReleaseAddress` with `Resource = "*"` for now | Can't know zombie resource IDs ahead of time | Tag-based Condition `ec2:ResourceTag/AutoCleanup = true` — hardening TODO |
| 2026-05-14 | DynamoDB PAY_PER_REQUEST billing mode | Bursty workload (scan every 6 hrs, idle between); provisioned would require capacity guessing | Provisioned — better for sustained high-throughput workloads where you know your RCU/WCU |
| 2026-05-14 | KMS using AWS-managed key (`aws/dynamodb`) not customer-managed | Free, zero config, still KMS-backed; CMK costs $1/month per key — overkill for personal dev | Customer-managed KMS key — better for regulated environments needing key policy control + audit trail |
| 2026-05-14 | PITR enabled on all 3 DynamoDB tables | 35-day rollback window; free on PAY_PER_REQUEST tables; cheap insurance during testing phase | Disable PITR — saves nothing (it's free), removes safety net |
| 2026-05-15 | Inserted new STEP 6 — Build the KMS CMK Module (renumbered subsequent steps; total 23 → 24) | Interview cred: must demonstrate CMK lifecycle, key policy with `kms:ViaService`, envelope encryption (Definition-of-Done interview Q). Original STEP 5 choice (AWS-managed key) was defensible for dev cost but gives no key policy/audit control. | (a) Leave AWS-managed keys — free, zero config, no audit. (b) Per-service CMKs ($3/month, better blast-radius isolation) — overkill for personal dev. Chose single shared CMK at $1/month. |
| 2026-05-16 | KMS key policy: one `Sid` per consuming service, not one combined statement | Each statement reads as a single purpose. Revoking S3's access later means removing one Sid — no risk of touching the DynamoDB or SNS grant by mistake. | One combined statement with `kms:ViaService` as a list of all 3 — terser but mixes concerns; harder to audit-diff. |
| 2026-05-16 | Lambda grants scoped with `kms:ViaService` Condition | Defense-in-depth: a compromised role can only call KMS *through* the consuming service. A leaked cost-scanner credential cannot call `kms:Decrypt` directly. | No Condition — works, but turns the role into a general KMS principal that can decrypt outside of DynamoDB/S3/SNS pathways. |
| 2026-05-16 | Used `aws_iam_policy_document` data source for the KMS policy, not inline JSON | Type-checked by Terraform, references variables cleanly, surfaces typos at plan time | Raw JSON heredoc — works but loses HCL validation and string-interp clarity |
| 2026-05-16 | `deletion_window_in_days = 30` (maximum) | If the key is scheduled for deletion by mistake, there is a full month to cancel before the material is destroyed. Cost: zero. | 7 days (minimum) — faster cleanup but tight recovery window for a personal project where mistakes are likely |
| 2026-05-16 | `multi_region = false` | Single-region deployment; multi-region CMKs are for cross-region replicas/DR | Multi-region — adds management overhead without a use case here |
| 2026-05-16 | S3 bucket name uniqueness via `bucket_suffix` tfvar, not `random_id` | S3 names are globally unique; suffix produces a deterministic name and avoids forcing IAM to consume the bucket ARN as an output (which would create a module-ordering cycle) | `random_id` — works but name changes on destroy/recreate; account-id suffix — leaks account-id into git |
| 2026-05-16 | Reports bucket name defined once as a `local` in `dev/main.tf`, consumed by both IAM and S3 | Single source of truth — if either module reconstructed the name from inputs, a future format drift would silently break IAM permissions | Reconstruct in each module — concise but fragile |
| 2026-05-16 | Reports bucket = SSE-KMS, logs bucket = SSE-S3 | Log delivery service would need `kms:GenerateDataKey` on the CMK to write to a KMS-encrypted target; access logs contain no payload data — SSE-S3 is AWS's recommended pattern for log destinations | SSE-KMS on logs bucket too — works but adds another grant to the key policy with no security upside |
| 2026-05-16 | Storage class `GLACIER_IR` for 90-day transition (not `GLACIER`/Flexible Retrieval) | Reports may be linked from an audit ticket months later — Flexible Retrieval (hours to restore) blocks that workflow. GLACIER_IR has the same archive pricing with millisecond retrieval. | `GLACIER` Flexible Retrieval — cheaper for write-once-never-read archives, but reports may be opened |
| 2026-05-16 | `Deny aws:SecureTransport = false` on both buckets | Forces HTTPS for every request — explicit deny is uncircumventable, modern SDK default does the same on the happy path | Rely on SDK defaults — works for our code, but no protection against a misconfigured client |
| 2026-05-16 | Reports bucket policy enumerates the 4 Lambda role ARNs (not `Principal = "*"` with Conditions) | Audit trail is one cat away; bucket policy + IAM policy = AND, so leaked credentials outside those 4 roles are blocked at the bucket | `Principal = "*"` + `aws:PrincipalArn` Condition — equally secure but reads worse |
| 2026-05-16 | `bucket_key_enabled = true` on the reports bucket | S3 bucket keys reuse one data key per ~5-min window; ~99% fewer `kms:GenerateDataKey` calls and ~99% lower KMS bill at zero security cost | Per-object KMS calls (default) — wastes money for no benefit |
| 2026-05-16 | Logs-bucket policy scoped with `aws:SourceArn` + `aws:SourceAccount` | Prevents the confused-deputy pattern where any account's S3 logging service could write here | Just allow `logging.s3.amazonaws.com` without source conditions — works, but is the textbook unsafe pattern |
| 2026-05-16 | Single SNS topic over per-severity / per-category topics | SNS's native fan-out model — CRITICAL-only routing later is a subscription `FilterPolicy`, not a second topic. Keeps IAM `local.alerts_topic_arn` to one ARN | Topic per severity (CRITICAL/HIGH/INFO) — works but multiplies IAM grants, KMS conditions, and topic policies for no functional gain at this scale |
| 2026-05-16 | Email-only alert subscription for STEP 8; Slack deferred | Slack drags Secrets Manager (not yet built) and an HTTPS subscription with retry/dead-letter policy into a STEP scoped to SNS. Defer until alert volume justifies the channel | Add Slack subscription now — would close the open TODO but bloat STEP 8's blast radius and force a Secrets Manager mini-STEP |
| 2026-05-16 | SNS topic policy enumerates 4 Lambda role ARNs as Publish principals | Defense-in-depth: identity-policy AND resource-policy must both Allow. Leaked credential outside those 4 roles is rejected at the topic. Audit is one `cat` away | `Principal = "*"` + `aws:PrincipalArn` Condition — equally secure but reads worse and obscures the audit |
| 2026-05-16 | Email regex validation on `alert_email` variable at the module boundary | Fails at `plan` time instead of `apply` time when AWS rejects malformed endpoints — tightens the feedback loop | No validation — Terraform accepts the value and AWS errors during apply, costing a round trip |
| 2026-05-16 | Lambda module — declare `aws_cloudwatch_log_group` explicitly with `depends_on` on the function | Lambda auto-creates a log group with no retention and no encryption on first invocation; explicit declaration is the only way to set retention + KMS, and the dependency ordering prevents a "log group already exists" failure on the second apply | Let Lambda auto-create the log group — fails our encryption requirement and our 30-day retention requirement |
| 2026-05-16 | Default `reserved_concurrent_executions = 5` on every Lambda | A misfiring EventBridge schedule + retry loop is the textbook runaway-bill scenario for serverless; capping concurrency is cheap insurance. Caller can raise per-function | Unbounded (Lambda default) — works fine until it doesn't; the cost of being wrong is real money |
| 2026-05-16 | X-Ray active tracing on by default | Free up to 100k traces/month; gives Step Functions execution graphs a per-Lambda timeline; trivial to demo in interview | `PassThrough` (only trace when called from a traced upstream) — saves nothing since we're under the free tier and lose visibility |
| 2026-05-16 | Encrypt Lambda env vars AND CloudWatch log groups with the shared CMK | Env vars often hold table names + topic ARNs which are not secrets but ARE infrastructure intel; log groups can leak request payloads. Both at-rest with the CMK = same audit/rotation/revocation story as DynamoDB/S3/SNS. Required two new statements in the KMS policy (different shapes for the Lambda role grant vs the CW Logs service grant) | Leave Lambda env vars + log groups on the AWS-managed default — works, costs nothing, but breaks the "everything under one auditable CMK" story that's the whole point of STEP 6 |
| 2026-05-16 | CloudWatch Logs grant in KMS policy scoped by `kms:EncryptionContext:aws:logs:arn` ArnLike pattern | Without the EncryptionContext condition, the grant lets CloudWatch Logs encrypt any log group in the account with our CMK. Restricting to `/aws/lambda/${project}-${env}-*` keeps the grant scoped to CloudGuard's Lambdas only | No EncryptionContext condition — works, but the grant is overly broad; same shape Checkov would flag |
| 2026-05-16 | `data "archive_file"` in the module (not a packaging-script-only flow) | Blueprint STEP 9 spec; gives Terraform a `source_code_hash` driver so code changes flow into plans without manual zip steps; coexists with the STEP 19 packaging script which can pre-populate a `package/` source_dir | Skip archive_file, rely entirely on the STEP 19 bash script — works but Terraform can't tell when code changed and won't update the function on `apply` unless the script ran first |
| 2026-05-16 | `dynamic "environment"` block on `aws_lambda_function` | AWS rejects an empty `environment {}` block; passing an empty map to a static block would error. Dynamic block omits the block entirely when no env vars | Always emit the block — fails for functions that legitimately have no env vars |
| 2026-05-16 | `AUTO_REMEDIATE = "false"` env var on resource_cleanup Lambda | Cleanup role has `ec2:DeleteVolume`/`ec2:ReleaseAddress` — destructive. Defaulting to dry-run mode means the function will scan and log findings but NOT delete until an explicit override (Step Functions input later) flips it. Belt-and-braces with the role-level Condition TODO | Default to true — IAM allows it, role is built for it. Rejected: a code bug shouldn't be able to release every EIP in the account. |
| 2026-05-17 | Cost scanner: pure helpers (`cost_analyzer.py`) + thin handler (`handler.py`) | Helpers take boto3 client as a parameter — STEP 15 mocks the client and tests logic without an AWS call. Handler is the wiring shell, nothing to test there | Single-file Lambda — simpler but every helper test would need to patch module-level `boto3.client(...)` initialization order |
| 2026-05-17 | Baseline = average of prior N–1 days, exclude the day being evaluated | Including today in its own baseline dilutes the spike (a 5x spike on day 30 with 29 days of $1 baseline still has avg $1.13, ratio drops from 5.0 to 4.4 — minor here but breaks at higher cardinality) | Include all days in baseline — simpler arithmetic, but a self-diluting signal |
| 2026-05-17 | Skip services with `len(day_costs) < 2` or `avg_cost <= 0` | Brand-new services or those at $0 baseline would either fail with div-by-zero or false-positive every first scan after enablement | Treat as anomaly when first non-zero spend appears — useful signal but high noise; can revisit when SNS routing has filters |
| 2026-05-17 | `uuid.uuid4()` finding_id over deterministic `(date, service_name)` | Blueprint specifies uuid. Duplicates on re-run cleaned up by 90-day TTL | Deterministic id `cost-<date>-<service>` — idempotent but deviates from blueprint |
| 2026-05-17 | `Decimal(str(amount))` not `Decimal(amount)` for DynamoDB | DynamoDB rejects floats; `str()` preserves Cost Explorer's textual precision and keeps the stored value human-readable | `Decimal(amount)` — produces ugly binary-float precision artifacts in the table |
| 2026-05-17 | Security scanner: one file per resource type (sg/s3/iam/ebs) + thin handler | Same pure-helpers-with-injected-client pattern as STEP 10. Each checker is mockable in STEP 15 without moto; handler is just orchestration. Adds a small fan-out cost in file count but keeps each file under 200 lines and single-responsibility | Single-file scanner — simpler, but every check shares boto3 state and every unit test patches module-level globals |
| 2026-05-17 | Per-resource try/except inside each checker | One unreadable bucket / one IAM user we can't introspect must not abort the whole scan. Wrap-and-log keeps the scan going over the successful resources and surfaces failures in CloudWatch | No wrapping — simpler code, but one cross-account-blocked bucket kills the entire S3 check on every scheduled run |
| 2026-05-17 | Stamp `finding_id`/`timestamp`/`expires_at` centrally in the handler, not in each checker | Checkers stay pure (resource_id + severity + check_name + description). Handler owns the cross-cutting schema invariant — same shape across cost / security / cleanup categories | Stamp inside each checker — duplicates the same 4 lines per checker; schema drift if one checker forgets a field |
| 2026-05-17 | Added EBS encryption checker + retrofitted IAM with `ec2:DescribeVolumes` (beyond literal blueprint spec) | Blueprint STEP 11 lambda_handler lists `check_ebs_encryption()` but doesn't give it a dedicated file/spec. Building it costs 1 file + 1 Describe action; skipping it leaves the handler call broken and drops a SOC2/PCI-relevant check | (a) Skip entirely — handler call would 404. (b) Stub with NotImplementedError — handler would 500 in production. Building it is the honest read of the blueprint |
| 2026-05-17 | STEP 11 hotfix — implemented `config_checker.py` (was left empty after main commit; user caught it in-session) | Blueprint section 5 lists the file but STEP 11 spec ignores it. IAM grant for `config:GetComplianceDetailsByConfigRule` was already in place from STEP 4, so the intent is clear. Building it (a) uses the IAM grant, (b) adds AWS Config + Security Hub coverage that's a 12-LPA interview must-know, (c) closes a real gap in the security scanner. Cost: 1 file + 1 additional IAM action (`DescribeComplianceByConfigRule`) | (a) Delete file + remove the IAM Config grant — least-privilege win but loses the interview-grade coverage. (b) Leave empty stub — would have been caught again at STEP 15 unit-test time |
| 2026-05-17 | Config checker: graceful no-op if AWS Config isn't enabled (catch NoSuchConfigurationRecorder + AccessDenied) | Most dev accounts don't enable Config (it costs ~$0.003/item recorded, can hit hundreds of dollars in a busy account); without the try/except, every scheduled scan in dev would fail loudly on this check. Catching the specific error codes preserves diagnostic signal — anything else still raises | Always raise — would fail every scan in dev/free-tier accounts. Always swallow all errors — would hide real misconfigurations |
| 2026-05-17 | Config severity = MEDIUM default with CRITICAL_RULES + HIGH_RULES sets for well-known managed rules | The Config API does not carry per-rule criticality — root-mfa-enabled and s3-bucket-logging-enabled both return the same payload. Marking everything MEDIUM is honest about the API limit but lets specific high-stakes rules (root MFA, public S3, encrypted volumes, VPC flow logs) be promoted explicitly | Mark everything as HIGH — alert fatigue, no signal-to-noise; pull rule criticality from Security Hub — better but adds another service dependency that this STEP doesn't scope |
| 2026-05-17 | Resource cleanup: two-gate auto-remediate (env var AND event flag, both must opt in) | Single-gate auto-remediate has bankrupted real teams via a buggy event payload or a misconfigured env var. Both gates means a Step Functions input that accidentally arms remediation can't fire in dev (env var still false), and a prod env var flipped to true won't run on every scheduled scan (EventBridge omits the event flag). Manual remediation is intentional, always | One gate — either works, but doubles the blast radius of a single misconfig; no gating — never sane for a role that holds DeleteVolume/ReleaseAddress/DeleteSnapshot |
| 2026-05-17 | Cleanup severity ladder by estimated monthly cost ($50 HIGH / $10 MEDIUM / else LOW), not by resource count or age | A 1 TB orphaned io1 volume ($142/mo) deserves a different page than a 10 GB gp3 volume ($0.91/mo) — they're both "available" but one is 150x more expensive. Cost-based severity matches what an on-call engineer actually cares about: the total bill, not the resource count | Count-based — would treat 100 small volumes the same as 1 huge one; age-based — would page on a long-idle but cheap resource and miss a recently-stranded expensive one |
| 2026-05-17 | Hardcoded ap-south-1 EBS/EIP/snapshot pricing constants vs the AWS Pricing API | Pricing API is us-east-1-endpoint-only (needs an extra IAM grant + region pin), adds ~500ms per call, and the prices change a few times per year at most. Documented constants with an effective date are auditable in CloudWatch logs and trivially updated | AWS Pricing API — better long term and required for multi-region scanning, but premature for an ap-south-1-only dev environment; pull from Cost Explorer — wrong API surface (CE is historical actuals, not list price) |
| 2026-05-17 | Dry-run remediation attempts still written to the remediation log as `SKIPPED_DRY_RUN` | The operator wants to see "we would have deleted vol-X today" — that's the value of dry-run, not silence. The STEP 5 `status-index` GSI lets a future ops dashboard query exactly the would-have-been-deleted set without a table scan | Write only successful/failed remediations — saves writes but hides what the system intended to do; would force engineers to read CloudWatch logs to see scan intent |
| 2026-05-17 | Snapshot age threshold as `SNAPSHOT_AGE_DAYS` env var (default 180), not a constant | Different environments want different retention policies — dev might purge at 30 days, prod might keep 365. Making it an env var means Step Functions input can override per-invocation without a code change | Hardcoded 180 — works for dev but loses the tunability; pull threshold from a Parameter Store value — overkill for a single tunable |
| 2026-05-17 | IAM retrofit added `ec2:DeleteSnapshot` to cleanup role (beyond literal STEP 4 spec) | Blueprint STEP 4 specifies `DeleteVolume` + `ReleaseAddress` for cleanup but STEP 12's `lambda_handler` and the section-4 spec both list snapshots as a remediation target. Without `DeleteSnapshot` the auto-remediate path for snapshots would silently fail at AWS. Same gap-fill pattern as STEP 11's EBS check + `ec2:DescribeVolumes` retrofit | Leave it out and keep snapshots detect-only — half-finished feature; harder for a future reader to discover the missing grant than to look at this row of the decision log |
| 2026-05-24 | Report generator: HTML for BOTH the archived report (S3) AND the email body (SES), one template family | Zero dependencies (no PDF library = no Lambda layer), renders identically across Gmail/Outlook/Apple Mail/browsers, Ctrl-F searchable in S3, and the same string is consumed by both channels via `build_report` (full) / `build_email_summary` (short with link button). Inline-`style` attributes only — `<style>` blocks get stripped by many email clients | PDF — better for "save and email externally" workflows that don't exist yet, but needs `weasyprint`/`reportlab` (~30–50 MB) → Lambda layer; plain text — loses the severity badges, the cost trend table, and the click-through to the full report |
| 2026-05-24 | Email = short summary + signed link, S3 = full archive (not "email everything") | Gmail clips inline HTML emails over ~100 KB; SES has a 10 MB hard message-size limit. A real CloudGuard report with 200 findings + 30 days × 50 services blows through both. Pre-signed URL TTL is the SigV4 7-day max — after expiry the object stays in S3 (lifecycle handles retention), re-run regenerates a fresh URL | Email the full HTML — works until volume grows, then silently truncates in Gmail with no warning; mail the S3 URL only with no summary — recipient has to click through to see if anything is on fire |
| 2026-05-24 | One report Lambda + two EventBridge rules (24h daily, 168h weekly) over two separate Lambdas | One code path to test, half the deployed-resources surface, EventBridge `Input` field passes `{"report_window_hours": N}` per rule. The Lambda treats event-input as authoritative; env var `REPORT_WINDOW_HOURS` is the direct-invocation fallback | Two Lambdas — clearer 1:1 schedule-to-function mapping but doubles the IAM/KMS/log-group/CloudWatch surface for no logic difference |
| 2026-05-24 | Scan + `FilterExpression` on `timestamp` for the findings/remediation tables (not a 4th GSI on `(environment, timestamp)`) | At dev-account volumes (≤a few hundred findings/day) Scan + filter is honest about the limit, costs less than maintaining an additional GSI, and the documented TODO surface makes the migration path explicit. STEP 5 GSIs on `severity` and `category` cover the other access patterns | 4th GSI on `(environment, timestamp)` for Query — better at scale but premature today; periodic export to S3 + Athena — bigger refactor, defer until findings volume justifies it |
| 2026-05-24 | IAM retrofit: added `s3:GetObject` to report_generator role | Pre-signed URLs are signed by the calling principal; S3 evaluates each URL request under the *signing principal's* identity policy at call time. Without `GetObject` here, every recipient clicking the email link gets `AccessDenied` even though the URL is signed and unexpired. STEP 4 spec for this role listed `s3:PutObject` only — a real gap once STEP 13's pre-signed flow lands | Drop pre-signed URLs and email a `s3://` URI — useless to non-AWS-CLI recipients; serve via API Gateway with a Lambda authorizer — adds two more resources for what `GetObject` + a 7-day signature already does |
| 2026-05-24 | SES sender defaults to `alert_email` (one verified identity covers From + To in sandbox) | Sandbox SES requires both endpoints to be verified — same address = one click, both ends covered. `ses_sender_email` tfvar override exists for the production case where From is a verified domain and To is a distribution list | Separate sender variable always required — bigger blast radius if forgotten; two verification clicks the user can't audit in tfvars |
| 2026-05-24 | SES / SNS failures caught and logged, not re-raised | S3 archive is the durable signal — if SES rejects (most likely sandbox: unverified recipient), the report is still in S3 and queryable. CloudWatch surfaces the SES exception with sender/recipient context for the operator. Re-raising would lose the archive on the first not-yet-verified attempt of a new environment | Re-raise on SES failure — fails the whole Lambda for a notification-channel issue; silently swallow — operator can't tell something's broken |
| 2026-05-24 | `html.escape()` on every dynamic value going into HTML | Resource IDs and descriptions come from DynamoDB and may contain `<`, `>`, `&` (e.g. a recommendation referencing `Principal = "*"`). Without escaping, a finding with an HTML-fragment description could break rendering or in principle inject `<script>`. None of CloudGuard's writers do this today, but the cost is one line per substitution | Trust the writers — fine until it isn't; one stray angle bracket from a future check breaks every report rendering |
| 2026-05-25 | Shared utilities take boto3 client as optional injectable parameter; module-level cache for production callers | One pattern across the codebase: `cost_analyzer.py`, `sg_checker.py`, `zombie_finder.py` already use the same dependency-injection seam. Test code passes a `Mock()` and asserts on `call_args`; production code passes nothing and gets the module-scope cached client (skips ~30 ms cold-start STS call on warm invocations) | (a) Always create new client per call — clean but wastes the warm-invocation reuse the Lambda runtime gives us. (b) Always read from module global, no injection — fast in prod but every unit test would have to monkey-patch `boto3.client` at the module level |
| 2026-05-25 | `paginate` refuses non-paginatable APIs by raising `ValueError` (does not silently fall back) | Cost Explorer uses `NextPageToken` (not `NextToken`) and `can_paginate` returns False. Hand-rolling lives in `cost_analyzer.py` for that one surface. Refusing here keeps the shared helper's contract narrow — caller knows immediately if they're using the wrong tool | Implement both paginator AND NextPageToken-loop paths in one function — works but muddies the interface for the one API that needs the alternative; would also hide which APIs are actually paginatable |
| 2026-05-25 | `query_findings_by_date` is Scan + FilterExpression on `timestamp` (carry-over of the documented STEP 13 limit) | Findings table PK is `finding_id` (uuid); no GSI has `timestamp` as a partition key, so real Query is not possible against the current schema. Scan is O(table size), acceptable at ≤ few hundred findings/day. Function signature stays the same when a `(environment, timestamp)` GSI lands later | Add the 4th GSI now — premature: GSI costs write amplification on every PutItem to support a query that runs at most twice a day from one consumer (report_generator) |
| 2026-05-25 | Slack webhook uses stdlib `urllib.request`, not `requests` | Adding `requests` would cost ~100 KB × 4 Lambda zips for a single JSON POST. stdlib `urllib.request` is in every Python runtime. Slack webhook payload is JSON-only, no advanced HTTP features that `requests` would justify | Use `requests` — prettier API but a dependency that propagates to every Lambda's `requirements.txt` for one network call shape |
| 2026-05-25 | `send_slack_webhook` catches bare `Exception` and returns False — never raises | Slack is a convenience notification channel. The Lambda's durable output is the DynamoDB write or S3 archive. A Slack outage must NOT fail the whole invocation. Same pattern as the SES/SNS error handling in STEP 13's `report_generator/handler.py` | Re-raise on Slack failure — couples a notification-channel issue to the durable write path; one Slack incident fails every scheduled scan in the channel-using environment |
| 2026-05-25 | STEP 15a: partial refactor — adopt only `batch_put_findings`, leave `paginate` / `send_sns_alert` / `send_slack_webhook` / `query_findings_by_*` unused by existing Lambdas | `batch_put_findings` consolidates 3 drifted Decimal-coercion styles into one recursive helper AND fixes a latent float bug in `resource_cleanup/handler` (boto3 would have raised `TypeError: Float types are not supported` on the first apply because `metadata.monthly_cost_usd` is a float). The other helpers save 1–2 lines per call site at the cost of an import + a runtime coupling — interface theatre. Helpers stay in `shared/`, unit-tested in 15b, available for future Lambdas | Full refactor — adopt all helpers everywhere. Symmetrical and "uses what we built" looks good in the codebase but pays no real dividend and grows the surface that any helper change would have to consider |
| 2026-05-25 | STEP 15a: `shared.dynamo_client.batch_put_findings` takes a table NAME (string), not a Table object — handlers no longer build Table objects | The helper caches a `boto3.resource("dynamodb")` at module scope for warm-invocation reuse; constructing `.Table(name)` inside the helper avoids leaking the resource handle to callers and matches the test-injection pattern used elsewhere (`dynamodb_resource=Mock()`). Handlers now pass env-var strings directly, removing 2–3 lines per Lambda | Helper takes Table object — symmetrical with old call sites but forces every caller to keep building Table handles, defeating the point of the module-scope cache |
| 2026-05-25 | STEP 15b: `unittest.mock` only — no `moto` dependency for unit tests | Every public helper already takes its boto3 client as an optional parameter (the DI seam from STEP 10). Mock injection is enough to assert on `call_args` for the contracts we care about. `moto`'s full AWS emulation is overkill for the unit-test layer and would add a CI dep; it becomes the right tool at the integration-test layer (post-STEP 20) | Use `moto` from day one — higher fidelity but slower runs, extra dep, and unnecessary when the production code is already injectable |
| 2026-05-25 | STEP 15b: load each Lambda's `handler.py` via `importlib.util.spec_from_file_location` under a unique module name | Four Lambdas each have their own `handler.py`. A naive `import handler` would cache the first-imported into `sys.modules['handler']` and the second test file would silently get the wrong code. Unique module names (`resource_cleanup_handler`, `report_generator_handler`) let one pytest run cover all four | One test file per pytest invocation — works but multiplies CI minutes, and the cross-Lambda fixtures in `conftest.py` would have to be duplicated |
| 2026-05-25 | STEP 15b: Slack alerting OUT for v1 (user has no Slack) — `send_slack_webhook` stays in `shared/` (tested, unused) | Zero Terraform / Lambda code references the Slack helper. Keeping the helper + its tests in `shared/` means a future "add Slack" change is a wire-up, not a re-write. The 4 unit tests for the Slack helper still pay off: they document the network-tolerance contract (returns False, never raises) that any future caller will depend on | Delete `send_slack_webhook` entirely until needed — would lose the unit-test record of the contract; re-adding it later would mean rebuilding both the helper and the tests |
| 2026-05-25 | STEP 16: STANDARD Step Functions workflow (not EXPRESS) | Up to 1-year execution history, complete graph in the SFN console for debugging (STEP 20 verification), per-state-transition billing — cheap at 1 execution / 6 hours. EXPRESS caps at 5 min, bills per request, optimised for high-TPS event-driven workloads | EXPRESS — wrong shape for a long-tail scheduled scan + report; would also lose the visual execution history that makes debugging trivial |
| 2026-05-25 | STEP 16: ASL written as HCL object → `jsonencode()`, not a `templatefile()`-loaded JSON file | Terraform validates structure at plan time, Lambda ARN refs interpolate natively (`var.x` not `${x}`), Retry parameters become module variables for per-env tuning | External JSON via `templatefile()` — closer to the blueprint's literal text but loses HCL validation and forces a `${}` interpolation layer for ARNs |
| 2026-05-25 | STEP 16: `ResultPath = "$.scanner_results"` on `ParallelScanners` (beyond blueprint) | Default Parallel output is an array that REPLACES state input. report_generator's input contract is `event.get("report_window_hours")` — a list event would crash. `$.scanner_results` is the standard SFN idiom to merge parallel output into the existing input shape | Modify report_generator to handle list input — breaks the STEP 13 contract and forces the Lambda to be SFN-aware; reshape with `Parameters` instead — same effect, more verbose |
| 2026-05-25 | STEP 16: Catch → Pass per scanner branch (not Catch → Fail) | A single scanner crash must not block the report. Pass emits `{status: FAILED, scanner: <name>}` so the report renders partial results with a "scanner X failed" banner. Matches the blueprint's ASL shape exactly | Catch → Fail — aborts the workflow on any failure, loses partial signal; no Catch at all — same as Fail but worse, propagates the error up to the SFN execution |
| 2026-05-25 | STEP 16: `lambda:InvokeFunction` enumerated on the 4 exact ARNs + `<arn>:*` (no wildcard prefix) | A future Lambda accidentally named `cloudguard-${env}-anything` cannot be invoked by THIS workflow without an IAM change. `:*` covers qualified versions/aliases per AWS docs | Wildcard ARN (`arn:aws:lambda:*:*:function:cloudguard-${env}-*`) — works today but quietly auto-grants invoke perms to any future same-prefix function |
| 2026-05-25 | STEP 16: `aws_iam_role` + 3 separate `aws_iam_role_policy` resources (lambda-invoke, logs-delivery, xray), not one combined inline policy | Concern separation: drop X-Ray later by deleting one resource; downgrade logging is one resource change. Matches the existing IAM module's per-role-per-concern pattern | One combined inline policy — fewer files, but mixes three independent grants and makes blast-radius reasoning harder |
| 2026-05-25 | STEP 16: `logging_configuration { level = "ALL", include_execution_data = true }` for dev | At ~120 executions/month, ALL stays inside the CloudWatch free tier. include_execution_data captures input/output for debugging; only PII risk in the payload is the alert email already exposed elsewhere | ERROR — saves no money at this volume and loses successful-run visibility; OFF — would be wrong for a system being actively debugged in STEP 20 |
| 2026-05-25 | STEP 16: KMS retrofit widened the existing `AllowCloudWatchLogsEncrypt` Sid's ArnLike values list (added SFN log-group pattern) — did NOT add a new Sid | The CW Logs service principal is the same; only the EncryptionContext ARN pattern differs. ArnLike with multiple values evaluates as OR — minimum change. Same Sid serves both Lambda and SFN log groups | New `AllowCloudWatchLogsEncryptSFN` Sid — works but duplicates the principal + actions block for no security benefit |
| 2026-05-25 | STEP 16: state machine `depends_on` the 3 inline policies | First execution can race policy propagation; without `depends_on` the apply succeeds but the first run can fail with AccessDenied. Cheap correctness | No depends_on — works most of the time; the failure mode is intermittent and confusing |
| 2026-05-25 | Execution-order reorder: STEP 19 runs BEFORE STEP 18 | STEP 15a made `src/shared/` a runtime import dependency for every Lambda. The Lambda module's `archive_file` only zips `src/<lambda>/`, so deploying without STEP 19's packaging script would produce Lambdas that ImportError on first invocation. STEP 19 builds a `package/` dir per Lambda containing `shared/` + pip deps; STEP 18 then points each `source_dir` at the pre-populated package | Keep blueprint order (18 → 19) — would deploy broken Lambdas, fail STEP 20, force a hotfix; modify the Lambda module to do `cp -r ../shared` inline — couples module to a specific source layout |
| 2026-05-27 | STEP 17: added a third (weekly) EventBridge rule beyond the literal blueprint | STEP 13's report_generator was designed for both 24h and 168h windows ("one Lambda + two EventBridge rules"). Blueprint STEP 17 spec'd only the daily rule. Adding the weekly rule completes STEP 13's matching half — same code path, half the resources two separate report Lambdas would cost | (a) Skip the weekly rule and leave the STEP 13 design half-built — code path still exists but never fires from scheduling; (b) build two report Lambdas — doubles the IAM/KMS/log surface for no logic difference |
| 2026-05-27 | STEP 17: periodic scan rule passes `auto_remediate=false`; keep config-gate model for v1 | The cleanup Lambda's two gates (STEP 12) mean an EventBridge-driven scheduled run can never delete resources regardless of the env var, as long as the EventBridge Input is false. Real remediation = human flips env var via PR AND human starts SFN manually with `auto_remediate=true`. No async approval needed for v1; the DoD does not call for one | (a) `auto_remediate=true` on the schedule with the env-var gate as the only stop — increases blast radius if env var ever defaults wrong; (b) implement `.waitForTaskToken` async approval flow now — adds API Gateway + 2 Lambdas + token storage, scoped as STEP 25 stretch |
| 2026-05-27 | STEP 17: EventBridge → SFN uses IAM role; EventBridge → Lambda uses `aws_lambda_permission` (resource policy) | AWS's two invocation models. SFN StartExecution is authorised through the target's `role_arn`; direct Lambda invocations are authorised by the function's resource policy. Using both is the AWS-documented pattern. Trying to use just one would have been wrong for one of the two target types | (a) Use IAM role for both — Lambda would still need `aws_lambda_permission` to allow the events service principal; (b) Use lambda_permission for both — no such concept for SFN |
| 2026-05-27 | STEP 17: `aws_lambda_permission.source_arn` scoped to the SPECIFIC rule ARN, plus `source_account` | Without `source_arn`, ANY EventBridge rule in the account could invoke the Lambda. Without `source_account`, the confused-deputy variant (another account's events service crafting a matching `source_arn`) is in scope. Both together = strictly intra-account, strictly THIS rule | (a) `principal = "events.amazonaws.com"` only — too permissive across accounts and across rules; (b) function-ARN-level `source_arn` — works today but would silently allow a future EventBridge rule to invoke without an IAM change |
| 2026-05-27 | STEP 17: per-rule `enabled` toggles (bool variables) over destroy/recreate for pause windows | EventBridge bills nothing for a disabled rule. Pausing scheduled runs during a deploy or maintenance window is one `terraform apply` away, and the rule's identity (ARN, history of past invocations in CloudWatch) stays intact | Destroy/recreate to pause — loses the rule's history, churns CloudWatch metrics naming, costs a plan + apply on both ends |
| 2026-05-27 | STEP 17.5: removed `GenerateReport` from the SFN workflow — scans run silently every 6 h, reports fire only from EventBridge (daily + weekly) | The 6-hourly SFN report + the daily EventBridge report both used a 24h findings window → content-identical emails. User-spotted before first apply: this would have flooded the inbox with ~36 emails/week of which only ~8 carried distinct information. Removing GenerateReport from the SFN aligns the architecture with the actual cadence (scan often, report rarely) and uses each schedule's window for a different report scope (daily 24h ≠ weekly 168h). Plan size unchanged — content-only edit to existing SFN resources | (a) Path 1 — drop both 6-hourly AND weekly, keep only daily: simpler, but loses the cadence/scope distinction interview value; (b) Path 4 — keep all three but make reports actually different (alert-style daily, executive-style weekly): real differentiation but needs `html_builder.py` rework, new IAM grants, new tests — over-engineered for v1 |
| 2026-05-27 | STEP 17.5: removed the SFN module's `report_generator_arn` and `report_retry_max_attempts` variables entirely (with tombstone comments) | Dead parameters mislead readers about the SFN's actual surface area and tempt a future user to re-introduce the report step without noticing the redundancy. Tombstone comments point back to STEP 17.5 so the audit trail isn't lost. Matches CLAUDE.md guidance: "no backwards-compat hacks like renaming unused vars; if you are certain that something is unused, you can delete it completely" | Keep the variables with `default = null` — invites a future caller to set them and unknowingly resurrect GenerateReport; mark them `deprecated` in description only — Terraform's deprecation surface for variables is weak (no plan-time warning) |
| 2026-05-28 | STEP 19: skip `pip install -r requirements.txt` in the packaging script (deviation from blueprint) | Every Lambda's `requirements.txt` is `boto3` only, and boto3 ships in the Python 3.12 Lambda runtime. Vendoring it would add ~15 MB × 4 of dead bytes per deploy, slow cold starts, and eat into the 50 MB zipped / 250 MB unzipped Lambda quotas. When a real third-party dep ever lands (e.g. `requests`), insert `pip install --target build/ <pkg>` at the documented script line. Trade-off: bundled runtime boto3 lags pip-latest by weeks; acceptable because no CloudGuard API call needs a brand-new SDK feature | (a) Blueprint-literal pip install — wastes 60 MB across 4 zips for zero functional gain; (b) Lambda layer for boto3 — adds a layer resource for something the runtime already provides; (c) Install only `requirements.txt - boto3` line by line — pointless complexity when the answer is "nothing to install" |
| 2026-05-28 | STEP 19: per-Lambda `src/<lambda>/build/` directory + script pre-copies `shared/` into it, vs a separate `zips/` tree or removing `archive_file` | Keeps Terraform's `archive_file` + `source_code_hash` drift detection intact — edit a `.py`, re-run script, `terraform plan` shows source_code_hash diff. The build/ folder is co-located with each Lambda's source so the directory layout reads as one self-contained unit | (a) Script makes the zip + Terraform just uploads via `filename` — removes archive_file, loses Terraform's automatic change detection if the script isn't re-run; (b) Single top-level `build/cost_scanner/` tree — slightly tidier but Terraform `source_dir` paths have to traverse upward |
| 2026-05-28 | STEP 19: strip `__pycache__/` AFTER copying, not via `--exclude`/`-Exclude` flags | PowerShell 5.1's `Copy-Item -Exclude` only filters at source-name level (doesn't traverse into directories); bash `cp` has no exclude flag at all and `rsync` isn't guaranteed in every CI environment. Copy-then-prune is the lowest-common-denominator pattern that works in both shells. Caught in-session: first dry-run produced 10 files for cost_scanner where 6 was expected — `__pycache__/` from local pytest runs got vendored | (a) `rsync --exclude='__pycache__'` — clean but adds an rsync dependency on the CI runner; (b) Live with the bloat — .pyc files compiled by local 3.13 are version-tagged in the filename, so Lambda 3.12 would ignore them anyway, but they still inflate the zip and waste S3-Lambda upload bandwidth |
| 2026-05-28 | STEP 19: two scripts (bash + PowerShell), not one polyglot/cross-shell wrapper | User's local loop is Windows PowerShell; CI is `ubuntu-latest` bash. Forcing WSL on Windows or PowerShell-Core on Linux would add a tool dependency for zero functional benefit. The two scripts duplicate ~30 lines of `cp`/`rm` plumbing — acceptable cost for an idempotent script that's read once per environment | (a) bash-only + tell the user to install Git Bash — adds a tool dependency to the local loop; (b) PowerShell-only + use pwsh on Linux CI — pwsh isn't pre-installed on `ubuntu-latest`, adds an install step to every CI run |
| 2026-05-28 | STEP 18: replaced em-dashes (`—`, U+2014) with hyphens in all AWS-bound description / tag / Comment strings (IAM, KMS, S3, SNS, SFN modules) | AWS IAM enforces `[	
 -~¡-ÿ]*` — printable ASCII + tab/CR/LF + Latin-1 supplement. Em-dash is U+2014, outside the allowed range. S3 tags reject non-Latin1 the same way. KMS happens to accept commas in description but the em-dash still fails. Caught at apply-time (server-side validation); `terraform plan` does not run AWS's per-service regex. Surface-only fix: HCL `#` comments and `variables.tf` `description` fields (Terraform-local help text) are unchanged — they never leave the machine | (a) Live with the rejection by accepting Latin-1 supplement only — em-dash isn't in that range either; (b) Use Unicode-aware version of every description — no AWS service accepts arbitrary Unicode in these fields |
| 2026-05-28 | STEP 18: SNS tag value rewritten from comma-separated list to slash-separated list | SNS tag-value rule (`[a-zA-Z0-9_.:/=+\-@\s]*`) is stricter than S3's, IAM's, or KMS's. Comma is not in the SNS set. Slash is — and reads identically as a list separator in prose. The same string with commas applied successfully to the KMS key in this same apply, which is the surprise worth flagging: per-service tag-value validators drift, and a tag that crosses one service can still fail at another | Use the same comma-style everywhere — fine on S3/IAM/KMS, fails on SNS; remove the Purpose tag entirely from SNS — loses a useful piece of inventory metadata for a 4-character format diff |
| 2026-05-28 | STEP 18: Lambda module default `reserved_concurrent_executions = 5 → -1` (no reservation) | Personal AWS accounts cap total Concurrent Executions at 10 (Service Quotas L-B99A9384). 4 functions × 5 reserved = 20 reserved → would push UnreservedConcurrentExecutions to -10, but AWS enforces a floor of 10 unreserved. `-1` means "no reservation" — Lambda's default behaviour, falls back to the account's unreserved pool. The account-wide cap of 10 becomes the runaway-bill protection until the quota is raised. Documented loudly in the variable description; per-function opt-in restores the safety rail at call sites once the quota grows | (a) Use `0` instead of `-1` — `0` means "throttle to zero, function bricked"; (b) Request quota raise to 1000 before STEP 18 — adds an out-of-band ticket dependency; (c) Reduce default to 2 per function (4 × 2 = 8 reserved, leaves 2 unreserved) — still violates the floor-of-10 rule |
| 2026-05-28 | STEP 18: SNS topic policy enumerates the 13 topic-scoped actions explicitly (not `sns:*`) | SNS rejects wildcards in resource-based topic policies because `sns:*` expands to include account-level actions (`sns:CreateTopic`, `sns:ListTopics`) that aren't topic-scoped — `InvalidParameter: Policy statement action out of service scope!`. Most other AWS services tolerate `service:*` in resource policies (e.g. S3, KMS); SNS does not. The 13 actions enumerated are the AWS-documented topic-scoped surface as of 2026. Also restored `sns:Subscribe` in the DenyInsecureTransport statement — my initial guess that Subscribe was the wildcard issue was wrong; the wildcard itself was the only problem | (a) Drop the root-admin statement entirely — root has implicit access anyway through the account-level identity; loses lockout protection if someone removes that implicit path; (b) Use `NotAction = ["sns:CreateTopic", "sns:ListTopics", ...]` to keep wildcard semantics — fragile against new SNS APIs that aren't topic-scoped |
| 2026-05-29 | STEP 20: fix IAM by adding `dynamodb:BatchWriteItem` to 3 writer-role Sids, NOT by rewriting `shared.dynamo_client.batch_put_findings` to loop with single `PutItem` calls | `BatchWriteItem` is the correct boto3 idiom — 25 items per call vs 25 round-trips, auto-retry for `UnprocessedItems`. Rewriting toward `PutItem` would be making correct code worse to match a stale IAM grant. The blueprint's STEP 4 IAM spec pre-dates STEP 14's shared-helpers introduction — the spec is the thing that's stale, not the code. IAM-only fix means no Lambda repackage / archive_file re-hash / Lambda redeploy — 2-second apply vs 30-second redeploy cycle | (a) Rewrite the helper to loop with PutItem — works but pessimises the API, breaks the 106 unit tests' contract, and would have to be re-reverted when the IAM gap was eventually documented anyway; (b) Add `dynamodb:*` for blanket coverage — over-grants, breaks the least-privilege story this project's interview narrative relies on |
| 2026-05-29 | STEP 20: defer Bug #2 (microscopic-cost false-positive in `detect_anomalies`) to STEP 21 instead of patching in-session | STEP 20's purpose is "verify the system end-to-end" — finding a bug satisfies the STEP; fixing every bug is not in scope. Rolling new code into STEP 20 would (a) violate the one-STEP-per-session rule, (b) blur the diff so a reviewer can't tell what was "verification" vs "new feature," (c) push the matching unit test into STEP 20 when STEP 21 (CI pipeline) is the more natural slot for "test was added because CI gap was discovered." Bug is now slotted with explicit acceptance criteria: `MIN_ANOMALY_DOLLARS` env-var floor + matching unit test | (a) Patch in-session and move on — would have made STEP 20 a verification-PLUS-feature change, mixing concerns. (b) Leave the bug unfixed beyond STEP 21 — the false-positive is in the live findings table now, would dirty every future report until fixed |
| 2026-05-29 | STEP 20: invoke `report_generator` manually with `{"report_window_hours": 24}` (Path B) rather than wait overnight for the 02:30 UTC EventBridge daily rule (Path A) | Same code path — EventBridge's `daily_report` rule passes the same JSON input. Path B closes the STEP in one session, exercises the SES verification completed at the end of STEP 18, and keeps PROGRESS.md to one coherent entry. The EventBridge schedule will fire on its own cadence from tomorrow — passive verification on top of the active one done here | (a) Wait for the schedule — would split STEP 20 across two days and force a coordinated session restart; (b) Skip the report verification and just verify the SFN scan succeeded — would leave the email/SES path untested in production |

---

## Problems Hit & Resolved

| Date | STEP | Problem | Resolution | Lesson |
|------|------|---------|------------|--------|
| 2026-05-14 | STEP 1 | Python 3.12 unavailable | Proceeded with 3.13.7; risk noted | Always pin runtime versions early |
| 2026-05-14 | STEP 2 | Bash in Claude Code resets cwd to git worktree | Use PowerShell for all file/dir ops | PowerShell is reliable; Bash is not in this env |
| 2026-05-14 | STEP 2 | `.terraform.lock.hcl` incorrectly added to `.gitignore` | Removed from gitignore in STEP 3; lock file committed | Lock file = commit; `.terraform/` dir = ignore |
| 2026-05-14 | STEP 3 | Terraform not in Claude Code's PATH at the time | Workaround: user ran terraform in their own terminal. **Corrected STEP 5:** Terraform IS in Claude Code's shell PATH — `terraform plan/init` can run directly. | PATH availability may depend on session startup order; always try directly first |
| 2026-05-14 | STEP 3 | GitHub Actions firing on every push (empty workflow files) | Changed trigger to `workflow_dispatch` until STEP 20 | Stub workflow files need a safe trigger |
| 2026-05-28 | STEP 18 | `terraform apply` rejected 5 IAM role descriptions + KMS key description + SFN role description + SFN ASL Comment with `ValidationError: 'description' failed to satisfy ...regex... [	
 -~¡-ÿ]*` | Replaced em-dash `—` (U+2014) with hyphen `-` in all 9 AWS-bound strings across IAM, KMS, S3, SNS, SFN modules. HCL `#` comments and `variables.tf` description fields (Terraform-local help text) untouched | AWS IAM accepts only printable ASCII + tab/CR/LF + Latin-1 supplement (`[	
 -~¡-ÿ]*`). Prose-style descriptions with em-dashes (very common in markdown-heavy projects) fail server-side at apply, never at plan. Stick to hyphens in description / tag / Comment fields. |
| 2026-05-28 | STEP 18 | `terraform apply` rejected S3 reports bucket tag with `InvalidTag: The TagValue you have provided is invalid` AND SNS topic tag with `Invalid parameter: Tags Reason: The given tag(s) contain invalid characters` | S3 rejected the em-dash; SNS rejected both em-dash AND commas. Replaced em-dash with hyphen in both, rewrote the SNS `Purpose` value from comma-separated to slash-separated (`"...for cost / security / cleanup / report-generator notifications"`) | Tag-value validation drifts per service. KMS accepted commas in its description in the same apply; SNS rejected them. When in doubt, slash-separate lists in tags and keep to hyphen for prose. |
| 2026-05-28 | STEP 18 | `terraform apply` rejected all 4 Lambda functions with `InvalidParameterValueException: Specified ReservedConcurrentExecutions for function decreases account's UnreservedConcurrentExecution below its minimum value of [10]` | Personal AWS accounts cap Concurrent Executions at 10 (Service Quotas L-B99A9384). Changed the Lambda module default from `reserved_concurrent_executions = 5` to `-1` (no reservation). The account-wide cap of 10 itself becomes the runaway-bill protection until a quota raise lands | Plan-time has no view of account-level quotas. The Lambda module's blueprint default was correct for an established account (1000 unreserved) and wrong for a new one (10). Document the constraint in the variable description so the next reader doesn't read `-1` as carelessness. |
| 2026-05-28 | STEP 18 | `terraform apply` rejected the SNS topic policy with `InvalidParameter: Policy statement action out of service scope!` | Statement 1's `actions = ["sns:*"]` expanded to include account-level SNS actions (`sns:CreateTopic`, `sns:ListTopics`) that aren't topic-scoped. Enumerated the 13 valid topic-scoped actions explicitly (`AddPermission`, `DeleteTopic`, `GetDataProtectionPolicy`, `GetTopicAttributes`, `ListSubscriptionsByTopic`, `ListTagsForResource`, `Publish`, `PutDataProtectionPolicy`, `RemovePermission`, `SetTopicAttributes`, `Subscribe`, `TagResource`, `UntagResource`). Also restored `sns:Subscribe` in DenyInsecureTransport — first guess that Subscribe was invalid was wrong; the wildcard was the only culprit | SNS is one of the few services that rejects `service:*` in resource-based policies, unlike S3/KMS/IAM. Enumerate topic-scoped actions explicitly. When debugging "action out of service scope", suspect the wildcard before any specific action. |
| 2026-05-28 | STEP 18 post-apply | SNS aggregate state APIs reported the email subscription as `0 confirmed / SubscriptionArn = "Deleted"` for several minutes after the user successfully clicked the AWS confirmation link. AWS Console correctly showed `✓ Confirmed`. False-alarmed the user; almost re-ran `terraform apply` to "fix" a working subscription | Queried the specific subscription directly with `aws sns get-subscription-attributes --subscription-arn <full-uuid-arn>` — got the authoritative answer (`PendingConfirmation: "false"`, real UUID returned). Subscription was genuinely confirmed all along. `terraform plan` also agreed (`No changes`) | Email-subscription state has TWO classes of API: aggregate (eventually consistent, `get-topic-attributes` / `list-subscriptions-by-topic`) and per-subscription (real-time, `get-subscription-attributes`). When the Console and aggregate-CLI disagree, the Console + per-subscription-CLI are the source of truth. Email subs go through a slow async confirmation pipeline; don't trust the counters for at least 15-30 min after click. |
| 2026-05-28 | Lambda module variable | Earlier-recorded lesson about `reserved_concurrent_executions` extended with the floor-of-10 detail in conversation | Account quota of 10 + AWS-enforced floor of 10 unreserved → ANY positive per-function reservation fails on a new account, not just `5`. Even `reserved_concurrent_executions = 1` × 1 function would push unreserved to 9 < floor → reject. `-1` (no reservation) is the only working value until quota is raised | "Reserved concurrency" is bounded by the *unreserved floor*, not by the account quota directly. The math is `quota - sum(per-function reservations) ≥ floor`. On a 10-quota / 10-floor account, the only feasible value of `sum(reservations)` is 0. Once quota is raised to 1000 (one Service Quotas ticket, auto-approved), the same blueprint default of `5` works for all 4 functions because `1000 - 20 = 980 ≥ 10`. |
| 2026-05-29 | STEP 20 | First live SFN execution: `CostScanner` and `SecurityScanner` branches both raised `AccessDeniedException` on `dynamodb:BatchWriteItem` (e.g. `User: arn:aws:sts::810278669055:assumed-role/cloudguard-dev-cost-scanner-role/... is not authorized to perform: dynamodb:BatchWriteItem on resource: arn:aws:dynamodb:ap-south-1:810278669055:table/cloudguard-dev-cost-data`). `ResourceCleanup` branch succeeded only because no zombies were found → `batch_put_findings([])` short-circuits without an IAM evaluation | Added `"dynamodb:BatchWriteItem"` to three inline policy Sids in `terraform/modules/iam/main.tf`: `DynamoDBWriteFindingsAndCostData` (cost_scanner), `DynamoDBWriteFindings` (security_scanner), `DynamoDBWriteFindingsAndRemediation` (resource_cleanup). `report_generator` is read-only, no change. `terraform plan` showed `0 add / 3 change / 0 destroy`; apply completed in <2 s; re-ran SFN → all 3 branches green | **IAM `PutItem` does NOT imply `BatchWriteItem`** — they are distinct API actions. The blueprint's STEP 4 IAM spec was written before STEP 14 introduced `shared.dynamo_client.batch_put_findings` (which uses boto3's `table.batch_writer()` → `BatchWriteItem`). Neither `terraform plan` (no runtime API view) nor the 106-test unit suite (mocked boto3, no real IAM evaluation) could catch this — only the first real invocation against AWS exposed the drift between spec and code. Integration testing is the only defense against this bug class. |
| 2026-05-29 | STEP 20 | First successful cost_scanner run flagged Amazon S3 service as a CRITICAL anomaly with `actual_cost=0, expected_cost=0, ratio=73.876x` ("7287.6% above prior-period average of $0.0") | Detector working as designed: real values are microscopic Decimals (tf-state bucket usage at fractions of a cent), which round to $0.00 when displayed but produce mathematically real 73x ratios. The `if avg_cost <= 0: skip` guard catches only exact-zero baselines, not positive-but-microscopic ones. **Logged as a slotted Open TODO for STEP 21 hardening sub-task**: add `MIN_ANOMALY_DOLLARS` env-var floor (default $1.00), skip findings where `actual_cost < MIN_ANOMALY_DOLLARS`, add unit test `test_detect_anomalies_skips_microscopic_costs`. NOT fixed in STEP 20 (verification STEP, not feature STEP) | Clean synthetic test fixtures hide real-world edge cases. STEP 15b's `test_cost_scanner.py` used `avg=100.0, today=200.0` style values where ratio math is intuitively bounded. Real-world AWS spend has long-tail microscopic values (fractional-cent activity) that no unit test thought to model. The right CI defense is to add a regression test the moment production data surfaces a case the existing tests didn't cover — which is exactly what the STEP 21 sub-task will do. |

---

## Open Questions / TODOs

> **Slotting policy (decided 2026-05-25):** Every open TODO must have an explicit slot in a remaining STEP, OR be classified as a stretch / post-Definition-of-Done extension, OR be dropped. Trigger-conditional micro-items (premature optimizations, accuracy improvements, code-organization tweaks) are dropped here and surface in the README as "Known Limitations" at STEP 23. None of the dropped items represent security gaps — all are performance, accuracy, or maintainability deferrals with documented trigger conditions.

### ✅ Completed

- [x] Pick alert email address — set in local `terraform.tfvars` during STEP 4
- [x] **STEP 6 retrofit:** swap STEP 5 DynamoDB tables from `aws/dynamodb` managed key to `module.kms.key_arn` — done in STEP 6 (2026-05-16)

### 🟡 Decision needed before STEP 18 starts

- [x] **Slack vs email-only for alerts** — **CLOSED 2026-05-25: email-only.** User does not have Slack, so v1 ships with the existing SNS email subscription only. No Secrets Manager / HTTPS subscription / Slack webhook wiring in any STEP. `shared.notification.send_slack_webhook` stays in `src/shared/` (covered by 4 unit tests in `tests/test_shared.py::TestSendSlackWebhook`) so a future "add Slack" feature is one wire-up away, but no Terraform or Lambda code references it today.

### 📍 Slotted to existing STEPs

- [x] **Refactor 4 existing Lambdas to consume `src/shared/`** → **STEP 15a, completed 2026-05-25 (partial scope).** Adopted only `batch_put_findings` (5 call sites across cost_scanner / security_scanner / resource_cleanup). Other helpers (`paginate`, `send_sns_alert`, `send_slack_webhook`, `query_findings_by_date`, `query_findings_by_severity`) remain in `src/shared/` for future use and will be unit-tested in 15b, but are NOT wired into existing Lambdas — they saved 1–2 lines per call site and adopting them would have been interface theatre. `batch_put_findings` was the high-value adoption: consolidates 3 drifted Decimal-coercion styles into one recursive helper, fixes a latent float bug in `resource_cleanup/handler` that would have crashed the cleanup Lambda on first invocation. See STEP 15a section above for full details, including the **new STEP 18 packaging blocker** the refactor introduced.
- [x] **Confirm free tier limits before first `terraform apply`** → **STEP 18 (pre-apply checklist)**. Done. Lambda, DynamoDB, S3, SNS, SES, EventBridge all stay free-tier at CloudGuard volumes. KMS CMK is the one recurring $1/month item. Apply consumed no quota-bound resources beyond what was already accepted in STEP 6.
- [ ] **IAM hardening — scope `ses:SendEmail` to a verified SES identity** → **DE-SLOTTED from STEP 18, NEW STEP 18.5 (between STEP 18 and STEP 20)**. Originally slotted as a same-session companion to STEP 18, but STEP 18 already ran 5 apply cycles with 4 hotfixes; adding more changes would have expanded scope and violated CLAUDE.md rule 1 ("one STEP per session"). The Condition (`StringEquals: { "ses:FromAddress": var.alert_email }`) is mechanically simple — single statement edit in `iam/main.tf`, single `terraform apply` cycle — but deserves its own scoped session so the diff is reviewable in isolation. Carries from STEPs 8 / 11 / 12 / 13.
- [ ] **IAM hardening — scope destructive EC2 actions with tag-Condition** → **DE-SLOTTED from STEP 18, NEW STEP 18.5 (same session as SES hardening above)**. Same reasoning as above. Adds `Condition: { StringEquals: { "ec2:ResourceTag/AutoCleanup": "true" } }` to the `resource_cleanup` role's `DeleteVolume` / `ReleaseAddress` / `DeleteSnapshot`. After this, the Lambda can only destroy resources explicitly tagged `AutoCleanup=true` — single mistagged prod volume can't be deleted even with both runtime gates flipped. Three-gate defense (env + event + tag) replaces two-gate. Carries from STEPs 4 / 12.
- [ ] **NEW — Cost detector microscopic-cost floor** → **slotted to STEP 21 hardening sub-task (first sub-task of CI work)**. Surfaced by STEP 20 Bug #2: live cost_scanner run flagged Amazon S3 as CRITICAL with `actual=$0, expected=$0, ratio=73.876x` because the underlying Decimal values are microscopic (tf-state bucket spends fractions of a cent). Acceptance criteria: (a) add `MIN_ANOMALY_DOLLARS` env var to `cost_scanner` Lambda (default `1.00`), (b) `cost_analyzer.detect_anomalies` skips a candidate finding when `actual_cost < MIN_ANOMALY_DOLLARS` regardless of ratio, (c) add unit test `test_detect_anomalies_skips_microscopic_costs` to `tests/test_cost_scanner.py` covering the case `avg=0.00001, today=0.0009, ratio=90x, actual<floor → no finding`, (d) update `terraform/environments/dev/main.tf` to set `MIN_ANOMALY_DOLLARS = "1.00"` on the cost_scanner Lambda's `environment_variables`. One-line code + one env var + one test. Closes the false-positive gap surfaced in production. Cleanup of the existing live false-positive row in `cloudguard-dev-findings` is a separate manual `aws dynamodb delete-item` post-fix step (optional — 90-day TTL will purge it anyway).
- [ ] **AWS Config Terraform module** → **NEW STEP 22.5 (between STEP 22 CloudWatch Dashboard and STEP 23 Documentation)**. Config compliance metrics belong on the dashboard, so 22.5 sits between them naturally. Provisions: (a) `aws_config_configuration_recorder` + `aws_config_delivery_channel` (the "Config is on" switch + S3 destination); (b) a curated set of ~10-15 `aws_config_config_rule` managed rules aligned to CIS / AWS Foundational Security Best Practices — root MFA, public S3 read/write prohibited, encrypted-volumes, rds-storage-encrypted, iam-password-policy, vpc-flow-logs-enabled, mfa-enabled-for-iam-console-access. Expected cost ~$2–$15/month on dev depending on resource-change churn. After 22.5, the `config_checker.py` (STEP 11 hotfix) stops no-opping in dev and actually returns findings. Strongest IaC-for-security interview story: "I write Config rules in Terraform" beats the canned conformance-pack answer.
- [ ] **Resource tagging strategy across all modules** → **STEP 24 (blueprint)**.
- [ ] **Manual: SES identity verification before first apply** → **STEP 18 (pre-apply checklist, manual AWS Console step)**. AWS sends a confirmation email; without verification, `report_generator` SES calls fail with `MessageRejected` (handler catches this so S3 archive still happens, but no email goes out). Same email used as both `From` and `To` in sandbox SES — one click covers both ends.

### 🌟 Stretch / Post-Definition-of-Done (interview-grade extensions, NOT gating)

These extend beyond the blueprint's 24-STEP scope but are top-tier 12 LPA interview talking points. Build only after STEP 24 — the base system must work first.

- [ ] **STEP 25 (post-DoD) — Human-in-the-loop remediation with Step Functions `.waitForTaskToken`:** Insert a `WaitForHumanApproval` Task state between `ParallelScanners` and a new `ExecuteRemediation` Task. Flow: scanners run → if any cleanup findings, publish SNS with the task token embedded in approve/reject links → API Gateway + Lambda calls `states:SendTaskSuccess`/`SendTaskFailure` with the human's decision → Choice state routes to `ExecuteRemediation` (only path where `auto_remediate=true` is set on the resource_cleanup invoke) or skips to `GenerateReport`. Sign the callback links with a Secrets Manager key so they can't be forged. Extend `cloudguard-remediation-log` schema with `approver`, `approved_at`, `decision`. **Why this matters:** the three-gate IAM safety (env + event + tag) added in STEP 18 is defense in depth at the application + IAM layer. `.waitForTaskToken` moves the gate into the workflow graph — auditable, paused executions visible in the Step Functions console, max wait 1 year, free while paused. This is the canonical answer to "Why Step Functions over chained Lambdas via SNS?" — exactly the thing you can't build cleanly with SNS fan-out.

### ❌ Dropped (trigger-conditional micro-items — surface in README "Known Limitations" at STEP 23)

None of these are security gaps. All are performance, accuracy, or code-organization deferrals with documented trigger conditions. Each gets a one-line entry in the README under "Known Limitations / Next Iterations" — that's where an interviewer asking "what would you do next?" gets the right pointer.

- ~~**4th GSI on findings table `(environment, timestamp)`**~~ — Performance only. Current Scan + FilterExpression is acceptable at ≤ few hundred findings/day. Trigger: findings volume > ~10k/day. The documented Scan limit IS the right answer today (STEP 13 / STEP 14 interview prep both make this case explicit).
- ~~**AWS Pricing API integration (replace hardcoded ap-south-1 constants)**~~ — Accuracy only. Today's cost estimates are within ~5% of list price for ap-south-1. Trigger: multi-region scanning feature (which is not in the blueprint).
- ~~**Archive-tier snapshot scanning**~~ — Completeness only. The standard-tier snapshots that dominate dev accounts ARE scanned; the rare Archive-tier snapshot is skipped. Trigger: same as Pricing API (depends on it).
- ~~**Move text body templating from `_send_email` into `html_builder.py`**~~ — Code organization only. Current 4-line f-string is fine. Trigger: when text templating grows beyond a one-liner (i.e. probably never within blueprint scope).

---

## Interview Prep Notes

- **STEP 2 — separate env folders over workspaces:** "Workspaces share code with different state files — fine for ephemeral environments like PR previews. For stable long-lived environments, separate folders give complete isolation: different configs, different backends, impossible to accidentally apply prod when you meant dev."

- **STEP 2 — why gitignore `*.tfstate`:** "State files contain real resource IDs, ARNs, and sometimes plaintext secrets. If state leaks to git, an attacker can map your entire infrastructure. Always use remote state with encryption — never in git."

- **STEP 3 — why S3 + `use_lockfile` over S3 + DynamoDB:** "Prior to Terraform 1.10, DynamoDB was required for locking because S3 had no atomic write primitive. From 1.10 onwards, the S3 backend uses S3's native conditional writes to create a `.tflock` file atomically — same guarantee, one fewer service to manage. I'm on 1.14.6 so I use native locking."

- **STEP 3 — why commit `.terraform.lock.hcl`:** "The lock file pins provider versions — in our case `hashicorp/aws v5.100.0`. Committing it means every developer and every CI run gets the exact same provider, not whatever is latest that day. It's the Terraform equivalent of a `package-lock.json`."

- **STEP 4 — why inline policy over managed:** "Each Lambda's policy is unique — nothing shared between roles. Inline policy makes the ownership relationship obvious (this policy belongs to this role, nothing else) and the policy is deleted automatically when the role is deleted. Customer-managed policies are better when the same permissions are attached to multiple roles — they have their own ARN, can be versioned, and updated independently."

- **STEP 4 — why I have `Resource = "*"` on some statements:** "Not all AWS actions support resource-level permissions. `ce:GetCostAndUsage`, `ec2:DescribeInstances`, `iam:ListUsers`, `ses:SendEmail` — none of these accept a Resource ARN. AWS publishes a service authorization reference table that lists this per action. For everything that COULD be scoped, I did — DynamoDB tables, SNS topic, S3 bucket — all scoped to specific ARNs built from a naming convention. For destructive EC2 actions that can't pre-scope, in production I'd add a tag-based Condition."

- **STEP 4 — least privilege vs least permissive:** "Least privilege isn't `Resource = "*"` — that's just permissive. Least privilege means the role can do exactly what it needs and nothing more. For cost scanner that means: Cost Explorer read, EC2/RDS describe (can't be scoped further), DynamoDB writes scoped to exactly two tables. The cost scanner cannot — and will never be able to — touch S3, SNS, IAM, or any other DynamoDB table."

- **STEP 5 — DynamoDB PAY_PER_REQUEST vs provisioned:** "Provisioned capacity requires you to predict read and write capacity units upfront. If you under-provision, DynamoDB throttles your requests. If you over-provision, you pay for idle capacity. For CloudGuard, the scanners run on a schedule — traffic is completely bursty: zero for 6 hours, then a burst of writes when a scan completes. PAY_PER_REQUEST handles that burst automatically, costs nothing when idle, and removes the operational burden of capacity planning. It's slightly more expensive per request than provisioned at high, sustained throughput — but for a scan-on-schedule system, it's the right call."

- **STEP 5 — Why GSIs on severity and category:** "DynamoDB is a key-value store. Without GSIs, fetching all CRITICAL findings would require a full table scan — reads every item, expensive and slow. A GSI on `severity` makes it a single Query call: `severity = CRITICAL`, sorted by `timestamp`. Same logic for `category` — lets the report generator pull all cost findings or all security findings without scanning everything else."

- **STEP 5 — Why TTL on findings but not the other two tables:** "TTL lets DynamoDB auto-delete items based on a Unix timestamp attribute, at no cost. Findings are time-bounded — a 90-day-old security finding is stale, the resource may have been fixed. Auto-expiry keeps the table lean and avoids manual cleanup jobs. Cost data and remediation logs are operational records — you want to keep them to spot trends and audit what was auto-deleted."

- **STEP 6 — Customer-managed KMS key (CMK) vs AWS-managed key:** "AWS-managed keys (`aws/dynamodb`, `aws/s3`, etc.) tick the encrypt-at-rest checkbox but give you zero control. With a CMK I can (a) write a key policy that says *exactly* which principals can Decrypt, (b) see every Decrypt call in CloudTrail under my key's ID, (c) rotate on my own schedule, and (d) revoke access instantly by disabling the key without touching the table or bucket. For a regulated workload you need every one of those — for a personal dev environment, the $1/month is the cost of building the muscle memory before doing it in prod."

- **STEP 6 — Envelope encryption explained:** "Symmetric encryption is fast on large data but you can't safely send the master key around. KMS solves this with envelope encryption: when DynamoDB writes an item, it calls `GenerateDataKey` on my CMK. KMS returns two copies of a fresh data key — one plaintext, one encrypted under the CMK. DynamoDB encrypts the row with the plaintext data key, stores the encrypted data key next to the ciphertext, and immediately discards the plaintext. To read, DynamoDB calls `Decrypt` on the encrypted data key, KMS hands back the plaintext, the row is decrypted, and the plaintext is discarded again. The CMK itself never touches the data — it only ever encrypts and decrypts other (much smaller) data keys. That's why it scales: one CMK can protect petabytes."

- **STEP 6 — Why `kms:ViaService` matters:** "Without the Condition, granting a role `kms:Decrypt` lets it call Decrypt directly against the key from anywhere — Lambda code, CLI, anywhere with credentials. With `kms:ViaService = dynamodb.<region>.amazonaws.com`, the key will only honor Decrypt calls that originate from the DynamoDB service on my behalf. If a cost-scanner credential leaks, the attacker can't just `aws kms decrypt` blobs — they'd have to be coming through DynamoDB, which means they'd already need DynamoDB read on the specific table. It's defense-in-depth at the cryptography layer."

- **STEP 6 — Why the root-account admin statement is non-negotiable:** "KMS key policies are evaluated *in addition to* IAM policies, but if a key policy doesn't grant access, IAM can't override that. If you write a key policy that omits root and accidentally locks out every principal you listed, you have created an unusable key that you also can't delete or modify — Terraform can't fix a policy it doesn't have permission to read. AWS's `bypass_policy_lockout_safety_check` defaults to `false` specifically to prevent this. The root statement is the escape hatch."

- **STEP 6 — Single shared CMK vs per-service CMKs:** "Per-service CMKs are the textbook answer for blast-radius isolation — compromise the DynamoDB key, the S3 bucket is still safe. They cost $1/month per key, so three services = $3/month plus key-policy duplication. For a personal dev project, $1 single shared key is the right trade. In a regulated production deployment — PCI, SOC2, HIPAA — the right call flips: per-service CMKs with tighter policies, possibly per-table or per-bucket, justified by the audit and isolation requirements."
- **STEP 7 — Why S3 bucket names need a suffix:** "S3 bucket names share one global namespace across every AWS account in the world — there is exactly one `cloudguard-dev-reports`, and whoever creates it first owns it. So bucket names need a per-account uniqueness suffix. I put it in a tfvars variable rather than letting the module generate a `random_id`, because a deterministic name means the same Terraform code produces the same bucket on every machine and the IAM policy that references the bucket ARN doesn't break when someone destroys-and-recreates the bucket."

- **STEP 7 — Identity policy vs resource policy (the AND rule):** "An S3 PutObject succeeds only if BOTH the caller's identity policy (their IAM role policy) AND the bucket's resource policy allow it — they're ANDed, not ORed. In CloudGuard the IAM module grants the 4 Lambda roles `s3:PutObject` on the reports bucket, AND the bucket policy enumerates those same 4 role ARNs. If a credential leaks to a fifth identity, the IAM side might be bypassed by an admin policy attached to that identity, but the bucket policy still rejects the write because that identity isn't in the principal list. That's defense-in-depth — two independent gates have to fail."

- **STEP 7 — Why `Deny aws:SecureTransport = false`:** "The condition `aws:SecureTransport = false` is true for any request that arrived over HTTP. An explicit `Deny` on `s3:*` when that condition is true means a plaintext request can't succeed even if every other policy on the bucket would allow it. Modern SDKs default to HTTPS, but `Deny` is uncircumventable — it covers a misconfigured CLI, a curl command someone tries during debugging, a future SDK version that changes its default. The cost is zero, the floor it sets is hard."

- **STEP 7 — How S3 bucket keys reduce KMS spend:** "Without bucket keys, every PutObject calls KMS `GenerateDataKey` to mint a unique data key, encrypts the object with it, and stores the encrypted data key alongside. At scale that becomes a real KMS bill — KMS charges per request, and bucket keys turn ~10,000 KMS calls into ~1. With `bucket_key_enabled = true`, S3 mints one data key per bucket per ~5-minute window and reuses it across uploads in that window. The encryption guarantee is unchanged — every object is still encrypted with a unique data key derived from the bucket key — but the KMS request count drops by roughly 99%."

- **STEP 7 — Confused deputy and `aws:SourceArn`:** "The classic confused-deputy problem: an AWS service can be granted permission to call into your account on someone else's behalf. The S3 logging service principal can write objects — without scoping, any other AWS customer could theoretically configure their bucket to deliver logs to mine. `aws:SourceArn = <my reports bucket ARN>` and `aws:SourceAccount = <my account>` on the Allow statement say: only honor PutObject calls from MY S3 logging service writing logs about MY bucket. AWS recommends this pattern on every service-principal Allow statement."

- **STEP 8 — Why a single SNS topic over per-severity topics:** "SNS is a fan-out service — one topic, many subscribers, with optional per-subscription filter policies. If I split CRITICAL and INFO into two topics, every Lambda needs two `sns:Publish` grants and two ARNs to remember, the KMS key policy needs two `kms:ViaService` Conditions to keep tight, and the topic-policy duplication doubles. The same routing is achievable with a `FilterPolicy` on a single subscription: `{ severity: [\"CRITICAL\"] }` on the on-call Slack subscriber, no filter on the email subscriber. One topic stays the right answer until the producer/consumer fan-out is genuinely heterogeneous — different services with different access patterns — and at that scale the right move is usually EventBridge, not multi-topic SNS."

- **STEP 8 — Why the topic policy enumerates 4 Lambda role ARNs explicitly:** "S3 reports bucket, SNS alerts topic, and (later) the Secrets Manager secret all follow the same rule: identity policy AND resource policy must both allow. The IAM module grants `sns:Publish` on the topic ARN; the topic policy lists those same 4 role ARNs in its Allow statement. A credential leaked outside those 4 roles is rejected at the topic — even if the leaked credential has an `sns:*` IAM policy attached. `Principal = \"*\"` plus an `aws:PrincipalArn` Condition is equivalent in coverage, but the explicit list is what an auditor wants to see — `cat topic-policy.json` answers 'who can publish?' in one read."

- **STEP 8 — Why email subscriptions stay PendingConfirmation:** "By design — Terraform cannot self-confirm an email subscription. If it could, an attacker with Terraform credentials could silently subscribe a victim's inbox to a high-volume topic and use SNS as a spam relay. AWS sends a confirmation email; until the recipient clicks the link (valid for 3 days), the subscription's `pending_confirmation = true` and no messages deliver to that endpoint. Same anti-confused-deputy logic as `aws:SourceArn`/`aws:SourceAccount` on the S3 logging bucket policy — the service refuses to be tricked into delivering somewhere it wasn't explicitly told to."

- **STEP 8 — KMS encryption on SNS and how it links to STEP 6:** "Topic-level SSE-KMS encrypts message bodies at rest. The shared CMK from STEP 6 already had `AllowLambdasViaSNS` Sid with `kms:ViaService = sns.<region>.amazonaws.com` — written ahead of time exactly so STEP 8 wouldn't need to touch the KMS policy. When a Lambda calls `sns:Publish`, SNS calls `kms:GenerateDataKey` on the CMK *on the Lambda's behalf* — the `kms:ViaService` Condition checks that the call's `userAgent`-equivalent path goes through SNS, so a leaked Lambda credential can't directly call `kms:Encrypt` outside the SNS path."

- **STEP 9 — Why a reusable Lambda module instead of 4 inline blocks:** "Four functions, same shape: a zip, a log group, the function itself, identical IAM/KMS wiring. Inlining means changing five things in five places when (not if) we add tracing, change retention, or rotate to a new runtime. A module makes 'add a Lambda' a 10-line call. The other reason is testability — a module's variables.tf is a typed interface, with regex validation on function_name and range validation on memory_size, so a typo at the call site fails at `terraform plan` instead of `terraform apply`."

- **STEP 9 — Why declare the CloudWatch log group in Terraform when Lambda auto-creates one:** "Lambda's auto-created log group has the worst defaults possible: no retention (logs live forever, billing forever) and no encryption (CloudWatch's default key, not yours). If you declare the log group explicitly with `retention_in_days = 30` and `kms_key_id = <CMK>`, you control both. The catch is ordering — if the function runs first, the auto-created group already exists and your Terraform resource conflicts with it. The fix is `depends_on = [aws_cloudwatch_log_group.lambda]` on the function — log group created first, function second, no race."

- **STEP 9 — `reserved_concurrent_executions` and why default to a low cap:** "Lambda's default is unbounded concurrency up to the account limit (1000+). A bug in an EventBridge schedule retry loop — say a Lambda that fails fast and triggers a `States.ALL` retry every second — can spawn hundreds of concurrent executions burning Cost Explorer API quota and writing to DynamoDB at full PAY_PER_REQUEST cost. Capping at 5 by default means the worst case is bounded to 5 concurrent executions per function. Production functions that legitimately need more concurrency can override; the safety rail is 'deny by default, raise on demand.'"

- **STEP 9 — X-Ray active tracing trade-off:** "Active tracing samples every invocation and emits trace segments to X-Ray. Free up to 100k traces/month — for a system that scans every 6 hours, that's roughly 4 scans × 4 Lambdas × 30 days = 480 traces a month, two orders of magnitude under the free tier. The benefit in the interview demo is the Step Functions execution graph: you click a parallel branch and see Cost Explorer pagination took 800ms, DynamoDB BatchWrite took 200ms — a real per-Lambda timeline. PassThrough only traces when called from a traced upstream — for ad-hoc invocations or initial development that's a worse default."

- **STEP 9 — Encrypting Lambda env vars + log groups with the same CMK as DynamoDB/S3/SNS:** "Env vars carry table names, topic ARNs, bucket names — not secrets, but infrastructure intel that an attacker reading them learns the shape of your data plane from. Log groups carry request payloads, AWS API responses, and sometimes accidental PII from misconfigured logging. Both go to the shared CMK so they sit under the same audit/rotation/revocation story as the data tables. Disable the CMK and the entire stack is read-protected as one unit — that's the architectural invariant STEP 6 was set up to give us."

- **STEP 9 — Two different KMS policy statement shapes for Lambda vs CloudWatch Logs:** "Lambda env-var decrypt is the function's execution role calling Decrypt via the Lambda service — so the principal is the role ARN and the Condition is `kms:ViaService = lambda.<region>.amazonaws.com`. CloudWatch Logs encryption is the Logs service principal doing its own encrypt on log events — the principal is `logs.<region>.amazonaws.com` and the Condition is `kms:EncryptionContext:aws:logs:arn` matching the log group ARN. Two statements, two different security models, both expressing 'this key can only be used by THIS service for THESE resources.'"

- **STEP 9 — Why dry-run-by-default on the resource cleanup Lambda:** "The cleanup role has `ec2:DeleteVolume` and `ec2:ReleaseAddress` — anything wrong with the scan logic could release every EIP and delete every available EBS volume in the account. The IAM hardening TODO is to scope those actions with a `Condition: ec2:ResourceTag/AutoCleanup = true`, but that requires tagging discipline we don't have yet. In the meantime: `AUTO_REMEDIATE = false` env var. Code scans, logs findings, sends SNS alerts — but doesn't actually delete. Step Functions input later can flip the flag to enable destruction once we trust the detection logic. Same defense pattern as the `Deny aws:SecureTransport = false` on S3: rely on multiple independent gates, default to safe."
- **STEP 10 — Why split `handler.py` from `cost_analyzer.py`:** "The handler is the I/O boundary — it reads env vars, builds boto3 clients, returns the Lambda response. The analyzer is pure logic — it takes a client as a parameter, takes a dict in, returns a dict out, never touches the environment. That split is what makes the logic testable: in STEP 15 I pass a `Mock()` for the boto3 client and assert exactly what `get_cost_and_usage` was called with, without ever needing AWS credentials or moto. Single-file Lambdas are simpler when there's no logic worth testing — for anything with branching, split."

- **STEP 10 — Why exclude the day being evaluated from its own baseline:** "If you average N days and compare day N against that average, the day's own value pulls the baseline toward itself. A genuine spike looks smaller than it is — the bigger the spike, the more the average is dragged up, the smaller the apparent ratio. Comparing day N against the average of days 1..N-1 keeps the baseline independent of the value under test. Same reason rolling forecasts in finance use trailing windows, not centered ones."

- **STEP 10 — Why `PAY_PER_REQUEST` table writes use `batch_writer()`:** "DynamoDB's `PutItem` is one request per item — 30 services × 30 days = 900 PutItem calls for the cost-data write alone. `batch_writer()` is a client-side context manager that buffers up to 25 items per `BatchWriteItem` call, automatically flushes when full, and retries any unprocessed items returned in the response. Same data, ~36 API calls instead of 900. On a PAY_PER_REQUEST table, every write is billed at the same per-request rate either way, but the throughput and the latency improve by an order of magnitude."

- **STEP 10 — Why floats become Decimal before going to DynamoDB:** "DynamoDB's number type is an arbitrary-precision decimal — it explicitly rejects Python floats because float-to-decimal conversion introduces binary-precision artifacts (`Decimal(0.1)` is not `0.1`). The boto3 SDK enforces this. The fix is `Decimal(str(amount))` — convert to a string first, then to Decimal, so the value the API sent (e.g. `'1.2345'`) becomes the exact value stored in the table. It's a small detail but it's the kind of thing that gets caught in code review when someone notices `1.0000000000000000055` in a cost report."

- **STEP 11 — Why split each checker into its own file:** "Same logic as the cost scanner split: each checker takes its boto3 client as a parameter and returns a list of finding dicts. The handler is the I/O boundary — env vars, real clients, DynamoDB batch write. The checkers are pure logic. That gives unit tests a single seam: pass in a `Mock()` for the client, assert the call args, assert the returned findings. The alternative — one monolithic Lambda — works but every test ends up patching module-level boto3 initialization, which is fragile and hides what's actually being verified."

- **STEP 11 — Why CRITICAL on SSH/RDP open to the world, HIGH on other public ports:** "SSH on port 22 and RDP on 3389 are the most common initial-access vectors for compromised AWS accounts — they're interactive shells, so a successful brute-force or credential-stuffing attack gives you an immediate command line. Verizon's DBIR has them at the top of cloud-breach root causes every year. Other ports open to 0.0.0.0/0 (Postgres on 5432, Mongo on 27017, etc.) are still wrong, but they require an additional exploitation step beyond authentication. The severity ladder reflects that — CRITICAL for direct shell paths, HIGH for the rest."

- **STEP 11 — Per-resource try/except around each AWS call:** "If `get_bucket_encryption` raises on one bucket — say cross-account ACL blocks our read — and we don't catch it, the entire S3 check aborts. That's the textbook 'one bad apple' failure mode. The pattern is: wrap each per-resource call, log the failure to CloudWatch so it's visible, return None from the inner function, continue with the next bucket. The scan always completes; failures are surfaced; the operator decides what to do about the unreadable resource. This is the same defensive pattern you'd apply to any batch-of-N pipeline where one item shouldn't kill the run."

- **STEP 11 — Why I added EBS encryption check beyond the literal blueprint:** "Blueprint section 4 lists `check_ebs_encryption()` in the `lambda_handler` orchestrator, but the per-checker spec section doesn't include EBS — that's an inconsistency in the blueprint. The honest read is that the author intended it but forgot the per-file detail. Two ways to handle that: skip the call and leave a broken orchestrator, or build the checker. I built it because EBS-at-rest encryption is a SOC2 control and PCI 3.4 requirement, and the cost was one file plus one extra `ec2:DescribeVolumes` action in the IAM role. The retrofit pattern is the same one we used in STEP 6 (DynamoDB → KMS) — flag the gap, propose the fix, get confirmation, retrofit, plan."

- **STEP 11 hotfix — Why AWS Config rules + Security Hub matters for 12 LPA:** "AWS Config is the data plane behind almost every cloud-security tool that vendors will sell you for six figures — Prisma Cloud, Wiz, CloudHealth Config integration, all of them. It continuously records the configuration state of every resource in your account and evaluates it against managed or custom rules. When a rule fails — say `s3-bucket-public-read-prohibited` flags a bucket — Config records the non-compliance, and that's what my `config_checker.py` queries. Combined with Security Hub (which aggregates findings from Config, GuardDuty, Inspector, Macie into a single dashboard with severity normalization), it's the AWS-native compliance posture stack. A 12 LPA cloud-security role will ask whether I can read a Config rule's compliance state, whether I understand the difference between detective controls and preventive controls, and whether I can build a remediation pipeline that consumes Config findings — my Lambda is exactly that."

- **STEP 11 hotfix — The graceful no-op pattern:** "AWS Config has to be explicitly enabled (it's an opt-in service with per-item recording cost), and most dev accounts don't have it on. If my Lambda just called `describe_compliance_by_config_rule` blindly, every scheduled scan in dev would fail with `NoSuchConfigurationRecorderException`. The fix is to catch that specific error code and return an empty list — the scan moves on, the four other checkers still run, no false alarms in CloudWatch. I also catch `AccessDeniedException` because Config has region-specific availability quirks. Anything outside that allowlist still raises — I want to know if it's a permissions bug, not Config-not-enabled."

- **STEP 11 hotfix — Why MEDIUM-default severity with rule-specific overrides:** "AWS Config's API surfaces compliance state but not severity — `root-account-mfa-enabled` failing and `s3-bucket-logging-enabled` failing return the same payload shape. Marking everything HIGH causes alert fatigue and your on-call engineer learns to ignore the channel; marking everything LOW means a critical control like 'root has MFA' gets buried. I default to MEDIUM and maintain a `CRITICAL_RULES` set (root MFA, public S3 read/write, root access keys) and a `HIGH_RULES` set (EBS encryption, RDS encryption, VPC flow logs, IAM password policy) — well-known managed rules that map to PCI/SOC2/CIS controls. As you onboard more rules in production you'd grow those sets. The right long-term answer is to pipe findings into Security Hub which has a normalized severity model, but that's an extension, not STEP 11 scope."

- **STEP 12 — Why two-gate auto-remediate (env var AND event flag):** "The cleanup Lambda holds three destructive permissions: DeleteVolume, ReleaseAddress, DeleteSnapshot. A single gate — say only checking an event flag — means one buggy Step Functions input can release every Elastic IP in the account. A single env-var gate means the moment we flip prod's `AUTO_REMEDIATE` to true, every scheduled EventBridge scan auto-deletes. Two gates: env var is the per-environment hard stop (dev stays false forever, prod only flips after we trust the detection), event flag is the per-invocation switch (manual Step Functions runs pass it when destruction is intended). Either gate alone is unsafe. Both required = the only invocation path that actually destroys is a manual run, in an environment that explicitly opts in, with an explicit per-invocation override. That's the same defense-in-depth pattern as `Deny aws:SecureTransport=false` on S3 — rely on multiple independent gates, default to safe."

- **STEP 12 — Why cost-based severity ladder for cleanup findings:** "A 1 TB orphaned io1 volume costs $142 a month. A 10 GB gp3 volume costs 91 cents. They're both `status=available`, both technically zombie resources, but they're 150x different in actual impact. If I severity-rate by resource count or by 'has it been orphaned for X days,' the on-call engineer gets paged equally for both — that's alert fatigue. By rating on estimated monthly waste — ≥$50/mo HIGH, ≥$10/mo MEDIUM, else LOW — the high-priority queue only contains things that actually matter to the AWS bill. It also lines up with how a manager would prioritize: 'show me what's costing us money,' not 'show me what's old.'"

- **STEP 12 — Why hardcoded pricing constants over the AWS Pricing API:** "The Pricing API is a us-east-1-only endpoint — every other-region Lambda has to make a cross-region call. It needs `pricing:GetProducts` in IAM, costs ~500ms per call, and the response is deeply nested JSON you have to walk to find the per-GB rate. EBS list prices change three or four times a year. For ap-south-1, today, hardcoded constants with an `effective 2026-05` comment is auditable in CloudWatch logs and trivially correctable when AWS updates the price page. When multi-region scanning lands (open TODO), we'll need the Pricing API — at that point one cold-start call per region per scan is acceptable. Today, it would be premature complexity."

- **STEP 12 — Why dry-run remediations still write to the remediation log:** "Dry-run is the operator's preview. If I just don't write to the log when AUTO_REMEDIATE is off, the operator has to read CloudWatch Logs to figure out what the scanner *would* have done — that's not a workflow you can build a dashboard on. By writing every dry-run as `status=SKIPPED_DRY_RUN` to the same remediation log table, the on-call engineer can query the STEP 5 `status-index` GSI for 'show me everything we'd have deleted in the last 24h' and decide whether to flip the gate. The data model treats dry-run as a first-class outcome, not an absence of outcome."

- **STEP 13 — Why HTML for both the email body and the archived report:** "The pattern is short summary in the inbox, full archive behind a 7-day pre-signed URL — same shape every real cloud-cost tool uses, and it's forced by hard limits: Gmail clips inline HTML over ~100 KB, SES rejects messages over 10 MB. So the email gets the executive-summary block plus a button-styled link, and the full document — every finding, every recommendation, the cost trend table, the remediation log — sits in S3 under `reports/<env>/<YYYY>/<MM>/<DD>/`. HTML is the format that lets one Python templating module serve both consumers identically. PDF would need `weasyprint` or `reportlab` — a 30–50 MB dependency that forces a Lambda layer for one feature. Plain text loses the severity badges, the per-service cost table, the click-through to the archive."

- **STEP 13 — How pre-signed URLs actually authorize:** "A pre-signed URL is the request — method, bucket, key, expiry, signing time — hashed with the calling principal's secret access key using SigV4. When someone clicks the link, S3 recomputes that signature from the URL's parameters and the *original signing principal's* current IAM policy at call time. If the signer no longer has `s3:GetObject` on the bucket, the link breaks immediately even if the signature is still mathematically valid. That's why STEP 13's IAM retrofit added `s3:GetObject` to the report_generator role — without it, every recipient clicking the email link gets AccessDenied with a perfectly valid signature, which is the exact kind of bug that looks like a sender issue when it's actually a permission issue at GET time. The 7-day TTL is the SigV4 hard cap, not a CloudGuard choice."

- **STEP 13 — Scan + FilterExpression vs adding a 4th GSI:** "Findings table has GSIs on `severity` and `category` from STEP 5 — neither has `timestamp` as a partition key. The report Lambda's only access pattern is 'all findings in the last N hours,' which is a time-range query. Two choices: Scan with `FilterExpression: timestamp >= cutoff` (returns the whole table from DynamoDB, filters client-side — cost is the full read), or add a 4th GSI on `(environment, timestamp)` and Query against it (returns only what matches — cost is the GSI write amplification on every PutItem). For a dev account with a few hundred findings a day, Scan is cheaper and simpler — the table is small enough that 'read everything and filter' is genuinely fine. I documented the migration as a TODO with a clear trigger: if findings volume exceeds ~10k, add the GSI. The honest interview answer is *I picked the cheaper option for today's volume and made the limit visible* — not *I picked the technically optimal solution.*"

- **STEP 13 — Why one Lambda + two EventBridge rules instead of two Lambdas:** "Daily and weekly digests are the same code with a different `report_window_hours` value. Two Lambdas would mean two function ARNs, two IAM role ARNs (or sharing one with two trust relationships), two CloudWatch log groups, two sets of CloudWatch alarms in STEP 22, and two function-level concurrency budgets — for code that's a single integer different. EventBridge's `Input` field on a target lets one rule pass `{\"report_window_hours\": 24}` and another pass `{\"report_window_hours\": 168}` to the same function. The function reads event-input first and falls back to the env var if absent — that gives you ad-hoc direct invocation (no event input → env-var default) plus the scheduled fan-out. Same Lambda, two schedules, half the operational surface."

- **STEP 13 — Why SES + SNS failures are caught, not re-raised:** "The Lambda's durable output is the S3 object — it's the archive, it survives across restarts, lifecycle handles retention. SES is a convenience channel; in sandbox SES the most likely error is the recipient not being verified yet, especially on the first apply of a new environment. If I re-raise on SES failure, the whole Lambda invocation fails and the report archive in S3 either never gets written (if SES is called before S3) or looks orphaned (if S3 is called first). Catching the SES `ClientError`, logging it with sender/recipient context for CloudWatch, and continuing means: report is in S3, the operator can see exactly why SES rejected it, the SNS notification still fires, and the next manual or scheduled run will retry SES after the verification click. Same pattern on SNS publish. The architecture treats archival and notification as independent fail-modes — one channel breaking doesn't cascade."

- **STEP 14 — Why `src/shared/` is about code reuse, NOT connection pooling:** "Two unrelated optimizations get conflated. `src/shared/` is a Python library pattern — write `paginate()` and `batch_put_findings()` once, import them into four Lambdas instead of copy-pasting. At runtime each Lambda's deployment zip contains its own bundled copy of `shared/` (the STEP 19 packaging script does `cp -r ../shared ./package/` per function), so there is no shared runtime — AWS Lambdas are isolation-by-design. Connection pooling is the unrelated module-scope-client pattern: declare `dynamodb = boto3.resource('dynamodb')` at module scope so it survives between warm invocations of the SAME container. That's already happening inside every handler.py — has nothing to do with `shared/`. The two patterns can stack: `dynamo_client.py` has a module-level `_DDB_RESOURCE` cache that the Lambda inherits when it imports the module, giving warm-invocation reuse PLUS code reuse."

- **STEP 14 — Why `paginate` refuses Cost Explorer instead of falling back:** "Cost Explorer's `get_cost_and_usage` is the one boto3 API in this project that doesn't support `client.get_paginator()` — it uses `NextPageToken` instead of the standard `NextToken`. I had two options for the shared helper: implement both paths (paginator AND a NextPageToken loop) in one function, or refuse the API and raise `ValueError` with the service/method name. Refusing is the right call because the one consumer (`cost_analyzer.get_cost_data`) already hand-rolls the loop correctly — if I silently swallowed Cost Explorer here, a future scanner might call `paginate(ce_client, 'get_cost_and_usage', 'ResultsByTime')` and get a wrong-looking result. Loud failure on a known-incompatible API is better than a shared helper with two divergent code paths."

- **STEP 14 — Why `query_findings_by_date` is Scan, not Query, and why I'm OK with that:** "The findings table from STEP 5 has PK `finding_id` (uuid) and GSIs on `severity` and `category` — none on `timestamp`. The report Lambda's access pattern is 'all findings in the last N hours' which is a time-range query, so a true Query against this schema isn't possible. Two choices: deviate from blueprint and add a 4th GSI on `(environment, timestamp)` now, or honour the schema and Scan + FilterExpression. At dev volumes (≤ a few hundred findings/day) Scan is genuinely cheaper — full table read costs less than the write amplification a 4th GSI would add on every PutItem. The migration trigger is documented: when findings volume exceeds ~10k, add the GSI; the function signature stays the same. This is the honest interview answer — *I picked the cheaper option for today's volume and made the limit visible* — not *I picked the technically optimal solution.*"

- **STEP 14 — Why the Slack webhook function never raises:** "A Lambda's durable output is the DynamoDB write or the S3 archive — those have to succeed or the system has no data. Slack is a convenience notification channel. If Slack goes down (or the webhook URL is wrong, or someone's firewall is blocking outbound HTTPS), re-raising the network error would fail the entire Lambda invocation and lose the durable write. The pattern is: catch `URLError` and bare `Exception`, log to CloudWatch with the URL host and error type so the operator sees it, return False so the caller's response payload says 'slack_delivered: False' — and continue with everything else. Same defensive pattern as STEP 13's SES/SNS error handling. The architecture treats archival and notification as independent fail modes; one channel breaking doesn't cascade."

- **STEP 14 — The blueprint gap I flagged:** "STEPs 10–13 built the four scanner Lambdas before `src/shared/` existed, so each one inlines its own `batch_writer` loop, its own `Decimal(str(...))` coercion, its own SNS publish. STEP 14 built `src/shared/` — but the blueprint has no explicit STEP to refactor the existing Lambdas to consume it. I flagged that as the post-STEP-14 TODO and slotted it as the first sub-task of STEP 15, before writing unit tests, so the new tests cover the refactored code path. Doing the tests first would mean throwing them away after refactor; doing the refactor first means one test suite, one code path. This is the kind of ordering judgment that an interviewer wants to hear — the blueprint isn't gospel, gaps get flagged, the fix gets slotted at the cheapest point in the dependency chain."

- **STEP 15 — Why `unittest.mock` instead of `moto`:** "Every helper in this codebase already takes its boto3 client as an optional parameter — the dependency-injection seam from STEP 10 onwards. That means I can pass a `MagicMock()` in tests and assert on `call_args` / `call_args_list` — I'm validating the contract (does the helper call the right API with the right shape?) without running an AWS emulator. `moto` becomes the right tool when I'm testing round-trip behaviour — e.g. an integration test that exercises the full DynamoDB write-then-query path. For unit tests of pure functions, full AWS emulation is overkill and slows the suite. The blueprint's CI pipeline in STEP 21 does install moto, which is the natural place for it: integration tests sit between unit tests and the live STEP 20 end-to-end run."

- **STEP 15 — The two-gate auto-remediate matrix test (the interview-ready safety story):** "The cleanup Lambda holds `DeleteVolume`, `ReleaseAddress`, `DeleteSnapshot` — three destructive EC2 actions. The safety model is defense in depth: an environment variable per environment (per-env hard stop, `'false'` in dev) AND an event input flag per invocation (per-call switch, EventBridge omits it for scheduled scans). Both must opt in for any deletion. The interview test in `test_resource_cleanup.py::TestAutoRemediateGate::test_two_gate_matrix` parametrises across all 4 env-var × event-flag combinations and asserts only `[true, true]` arms remediation. The point of writing that test is not to verify today's behaviour — it's to make sure that a future refactor flipping `and` to `or` shows up as a red CI run, not a quiet production incident. The IAM tag-Condition I'm adding in STEP 18 becomes a third gate, and the test will extend to a 2³=8 cell matrix."

- **STEP 15 — Why the fail-tolerance contract is the unique handler-level test for `report_generator`:** "Everything testable in the report Lambda lives in `html_builder.py` — pure functions, no I/O, easy to feed synthetic data. The handler's one unique surface is its failure model: the S3 archive is the durable output, SES is a convenience channel. If SES rejects (sandbox SES with an unverified recipient is the most likely real-world failure), the Lambda must NOT raise — it must log the exception, return False from `_send_email`, and still emit a success summary because the S3 object is the system of record. The unit test pins that contract: `_send_email` returns False on `ClientError`, `_publish_sns` swallows ClientError the same way. If a future refactor re-raises, the test breaks immediately. This is also the architectural answer to 'what happens if your alerting channel is down?' — the answer is 'the report is still archived to S3 and queryable; the operator sees the SES exception in CloudWatch with full sender/recipient context.'"

- **STEP 15 — Why my tests load handlers via `importlib.util.spec_from_file_location`:** "Four Lambdas, four files named `handler.py`. A normal `import handler` in pytest's shared process would cache the first-loaded file into `sys.modules['handler']`, and the next test file would silently get the wrong code. I load each handler under a unique module name (`resource_cleanup_handler`, `report_generator_handler`, …) via `importlib.util.spec_from_file_location`. That's also why `tests/conftest.py` has a `_reset_shared_caches` autouse fixture — `shared.aws_helpers._ACCOUNT_ID_CACHE`, `shared.dynamo_client._DDB_RESOURCE`, `shared.notification._SNS_CLIENT` are module-scope globals that production benefits from (one cached client per warm Lambda) but tests must reset between cases. Test isolation is a feature you pay for — most projects don't think about it until a flaky test surfaces."

- **STEP 15 — How the 106 tests prove the architecture, not just the code:** "Code coverage isn't the goal — contract coverage is. The 106 tests prove the architectural decisions: (a) helpers take clients as parameters (testable without AWS); (b) `_coerce_decimals` is recursive (latent float bugs in nested `metadata` dicts are caught); (c) Cost Explorer pagination is hand-rolled because the shared `paginate` deliberately refuses it; (d) destructive remediation requires both gates; (e) notification channels fail-tolerantly; (f) HTML is escaped before rendering. Each of those is a defensible architectural decision from earlier STEPs — the tests are the documentation that says 'and we still believe it.' That's what a 12 LPA interviewer is checking when they ask 'what's your test strategy?' — not the percentage number, but whether the tests guard the decisions you'd defend in the same interview."

- **STEP 16 (Step Functions over chained Lambdas):** _[fill in during STEP 16]_

- **Post-STEP 16 — Human-in-the-loop with `.waitForTaskToken` (be ready for this in interviews):** "The CloudGuard cleanup Lambda holds three destructive permissions — DeleteVolume, ReleaseAddress, DeleteSnapshot. Today I have a two-gate safety: an env var per environment and an event flag per invocation, both must opt in. That's defense in depth at the application layer, but anyone with `states:StartExecution` can flip the event flag silently — no audit trail beyond CloudTrail. The interview-grade answer is to move the gate from the code to the workflow graph. Step Functions has a `.waitForTaskToken` callback pattern: a Task state pauses the execution and emits a token, an SNS message goes out with the token embedded in approve/reject links, an API Gateway endpoint calls `states:SendTaskSuccess` or `SendTaskFailure` with the human's decision, and the state machine resumes — or fails to remediate, depending on which link was clicked. While paused you pay nothing (transitions, not wall-clock, are what you pay for), max wait is one year, and the paused execution is visible in the Step Functions console with full input/output history. This is *the* canonical answer to 'why Step Functions over chaining Lambdas via SNS/SQS': `.waitForTaskToken` is the thing you genuinely cannot build cleanly with SNS fan-out — you'd end up implementing your own state machine on top of DynamoDB just to track which tokens are outstanding. I designed CloudGuard knowing this was the next extension; I built the two-gate safety today as the minimum responsible default, with the human-approval workflow as the documented next step."
